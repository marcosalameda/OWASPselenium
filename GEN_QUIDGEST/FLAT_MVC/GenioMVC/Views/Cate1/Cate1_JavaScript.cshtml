@model GenioMVC.ViewModels.Cate1.Cate1_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS CATE1]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Cate1 !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Cate1 = new QForm($('[data-form="Cate1"]'), "Form_Cate1");
		}
		window.Form_Cate1.baseArea = "cate1";
		window.Form_Cate1.FormMode = '@Model.GetFormMode';
		window.Form_Cate1.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Cate1.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Cate1")',
			GetDependants:		'@Url.Action("GetDependants", "Cate1")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Cate1", "Cate1")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Cate1", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Cate1", "Cate1")',
		};
		window.Form_Cate1.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Cate1.formSaveEdit = '@Url.Action("Cate1_SaveEdit", "Cate1")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Cate1.formAction = 'Cate1_New';
		window.Form_Cate1.formRedirectURL = '@Url.Action("Cate1_New_Redirect", "Cate1")';
		window.Form_Cate1.formSubmitURL = '@Url.Action("Cate1_New", "Cate1")';
		window.Form_Cate1.formRedirectURL_repeatInsertion = '@Url.Action("Cate1_New_Redirect", "Cate1", new { repeatInsertion = true})';
		window.Form_Cate1.formSubmitURL_repeatInsertion = '@Url.Action("Cate1_New", "Cate1", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Cate1.formAction = 'Cate1_Edit';
		window.Form_Cate1.formRedirectURL = '@Url.Action("Cate1_Edit_Redirect", "Cate1")';
		window.Form_Cate1.formSubmitURL = '@Url.Action("Cate1_Edit", "Cate1")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Cate1.formAction = 'Cate1_Duplicate';
		window.Form_Cate1.formRedirectURL = '@Url.Action("Cate1_Duplicate_Redirect", "Cate1")';
		window.Form_Cate1.formSubmitURL = '@Url.Action("Cate1_Duplicate", "Cate1")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Cate1.formAction = 'Cate1_Delete';
		window.Form_Cate1.formRedirectURL = '@Url.Action("Cate1_Delete_Redirect", "Cate1")';
		window.Form_Cate1.formSubmitURL = '@Url.Action("Cate1_Delete", "Cate1")';
	</text>
}
		window.Form_Cate1.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_CATE1___CATE1ABBREVIA = new QTextControl($('[data-identifier="LED_CATE1___CATE1ABBREVIA"]', this.element), window.Form_Cate1);

			this.Controls.LED_CATE1___CATE1CATEGORY = new QTextControl($('[data-identifier="LED_CATE1___CATE1CATEGORY"]', this.element), window.Form_Cate1);




			this.Controls.LED_CATE1___CATE1CODCATEG = new QHiddenControl($('[data-identifier="LED_CATE1___CATE1CODCATEG"]', this.element), window.Form_Cate1);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Cate1.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Cate1.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI CATE1]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Cate1.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS CATE1]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Cate1.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS CATE1]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Cate1.Data.RelationKeysSelector = {
		};

		window.Form_Cate1.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Cate1.Data.RelationKeys = {
			get formContext() { return window.Form_Cate1; },
		};
	</text>
}

	window.Form_Cate1.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Cate1.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitCate1(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Cate1.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteCate1(event) {
			event.preventDefault();
			window.Form_Cate1.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS CATE1]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Cate1"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
