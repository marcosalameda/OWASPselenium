@model GenioMVC.ViewModels.Cav.GlobalViewModel

    <div class="execution-form">
        <!--
         <div class="editor-label">
            @Html.LabelFor(m => @Model.ReportTitle)
        </div>
        <div class="editor-field">
            @Html.TextBoxFor(m => m.ReportTitle)
            @Html.ValidationMessageFor(m => m.ReportTitle)
        </div>

    <br />
    -->
        @*<input type=hidden name="fields" id="fields" />
        <input type=hidden name="orderby" id="orderby" />
        <input type=hidden name="groupby" id="groupby" />
        <input type=hidden name="total" id="total" />
        <input type=hidden name="conditions" id="conditions" />
        <input type=hidden name="system" id="system" />
        <input type=hidden name="years" id="years" value="@ViewBag.Years" />
        <input type=hidden name="yearmode" id="yearmode" value="@ViewBag.Yearmode" />
        <input type=hidden name="relations" id="relations" value="@ViewBag.Relations" />*@
        <!--<input class="ibutton button big" type="submit" id="submit" value="Submeter Consulta"/>-->

        @Html.ActionLink("Submeter Consulta", "ExecuteQuery", "Query", new { table = ViewBag.Table }, new { @style = "display:none", @id = "execute-button", @class = "ibutton button big" })

        <!-- Hidden buttons that show up upon receiving results -->
        @* <a class="b-icon-text b-icon-text--secondary" href="#" onclick="window.print();return false;">@Resources.IMPRIMIR20813</a>*@
        <a class="b-icon-text b-icon-text--secondary" href="@Url.Action("GenerateExcel", "Cav")">@Resources.FICH__EXCEL25959</a>
        <a id="view-query" class="b-icon-text b-icon-text--secondary" href="#">@Resources.CONSULTA40695 (SQL)</a>

        <div id="results-section">
            <!-- bordered-table condensed-table zebra-striped -->
            <!-- isto não pode ser feito assim, no caso dos reports paginados por ano vão ser exibidas várias tabelas
        <table class="condensed-table zebra-striped results">
        </table>
        -->
            <h1 class="waiting-message">A carregar resultados...</h1>
        </div>

        <div id="results-info" style="display:flex;margin-top:0.3rem;">
            <div id="record-count">
            </div>

            <div id="pagination">
            </div>
        </div>
    </div>
 <script type="text/javascript">
    $(document).ready(function () {
         $("#view-query").on('click', function () {
             $.ajax({
                url: '@Url.Action("ObtainSQLQuery", "Cav")',                
                contentType: 'application/json',
                type: "GET",
                 success: function (data) {
                     bootbox.dialog({ message: data, title:'SQL',size: 'large',onEscape: true});                                   
                }
            })
         });
     }); 
 </script>

