@model GenioMVC.ViewModels.Cmpny.Wid_cola_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS WID_COLA]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Wid_cola !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Wid_cola = new QForm($('[data-form="Wid_cola"]'), "Form_Wid_cola");
		}
		window.Form_Wid_cola.baseArea = "cmpny";
		window.Form_Wid_cola.FormMode = '@Model.GetFormMode';
		window.Form_Wid_cola.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Wid_cola.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Cmpny")',
			GetDependants:		'@Url.Action("GetDependants", "Cmpny")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Wid_cola", "Cmpny")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Wid_cola", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Cmpny", "Cmpny")',
		};
		window.Form_Wid_cola.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Wid_cola.formSaveEdit = '@Url.Action("Wid_cola_SaveEdit", "Cmpny")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Wid_cola.formAction = 'Wid_cola_New';
		window.Form_Wid_cola.formRedirectURL = '@Url.Action("Wid_cola_New_Redirect", "Cmpny")';
		window.Form_Wid_cola.formSubmitURL = '@Url.Action("Wid_cola_New", "Cmpny")';
		window.Form_Wid_cola.formRedirectURL_repeatInsertion = '@Url.Action("Wid_cola_New_Redirect", "Cmpny", new { repeatInsertion = true})';
		window.Form_Wid_cola.formSubmitURL_repeatInsertion = '@Url.Action("Wid_cola_New", "Cmpny", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Wid_cola.formAction = 'Wid_cola_Edit';
		window.Form_Wid_cola.formRedirectURL = '@Url.Action("Wid_cola_Edit_Redirect", "Cmpny")';
		window.Form_Wid_cola.formSubmitURL = '@Url.Action("Wid_cola_Edit", "Cmpny")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Wid_cola.formAction = 'Wid_cola_Duplicate';
		window.Form_Wid_cola.formRedirectURL = '@Url.Action("Wid_cola_Duplicate_Redirect", "Cmpny")';
		window.Form_Wid_cola.formSubmitURL = '@Url.Action("Wid_cola_Duplicate", "Cmpny")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Wid_cola.formAction = 'Wid_cola_Delete';
		window.Form_Wid_cola.formRedirectURL = '@Url.Action("Wid_cola_Delete_Redirect", "Cmpny")';
		window.Form_Wid_cola.formSubmitURL = '@Url.Action("Wid_cola_Delete", "Cmpny")';
	</text>
}
		window.Form_Wid_cola.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_WID_COLACMPNYLOGO____ = new QImageControl($('[data-identifier="LED_WID_COLACMPNYLOGO____"]', this.element), window.Form_Wid_cola, '@Html.Raw(Url.Action("ImageDelete", "Cmpny", new { id=Model.ValCodempre, modelname="Cmpny", fldname="ValLogo" }))', '@Html.Raw(Url.Action("ImageCropper", "Cmpny", new { key=Model.ValCodempre, modelname= "Cmpny", fldname= "ValLogo", identifier= "FWID_COLA", FormName= "Wid_cola", FieldId= "LED_WID_COLACMPNYLOGO____" }))',@Html.IsImageEmpty(Model.ValLogo));

			this.Controls.LED_WID_COLACMPNYDESIGNAT = new QTextControl($('[data-identifier="LED_WID_COLACMPNYDESIGNAT"]', this.element), window.Form_Wid_cola);

			this.Controls.IFF_WID_COLAPSEUDPESSLIST = new QTableListControl($('[data-identifier="IFF_WID_COLAPSEUDPESSLIST"]', this.element), window.Form_Wid_cola);




@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	@*Hidden Foreign Keys Controls*@
			this.Controls.LED_WID_COLACNTRYCODCNTRY = new QHiddenControl($('[data-identifier="LED_WID_COLACNTRYCODCNTRY"]', this.element), window.Form_Wid_cola);
	</text>
}
			this.Controls.LED_WID_COLACMPNYCODEMPRE = new QHiddenControl($('[data-identifier="LED_WID_COLACMPNYCODEMPRE"]', this.element), window.Form_Wid_cola);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Wid_cola.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Wid_cola.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI WID_COLA]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Wid_cola.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS WID_COLA]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Wid_cola.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS WID_COLA]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Wid_cola.Data.RelationKeysSelector = {
				get cntry() { return $(':not([elem-identifier="Multiform"]) > #ValCodcntry', $('[data-form="Wid_cola"]')); /* Hidden */ },
		};

		window.Form_Wid_cola.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Wid_cola.Data.RelationKeys = {
			get formContext() { return window.Form_Wid_cola; },
			get cntry() { return (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value; /* Hidden FK */ },
			set cntry(newValue) { (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value = newValue; /* Hidden FK */ },
		};
	</text>
}

	window.Form_Wid_cola.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Wid_cola.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitWid_cola(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Wid_cola.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteWid_cola(event) {
			event.preventDefault();
			window.Form_Wid_cola.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS WID_COLA]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Wid_cola"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
