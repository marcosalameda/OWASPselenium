@model GenioMVC.ViewModels.Cntry.Pais_ValPropried_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
<div class="row">
	<div class="col">
		<div elem-identifier="List"  help-id="IFF_PAIS____PSEUDPROPRIED" style="padding-top: 0.5rem;"><label class = "i-text__label table-title c-table__title" > <h2 list-header>@Resources.REAL_STATE_MAP58776</h2 list-header> </label></div>
	</div>
</div>

@* USE /[MANUAL GQT TABLE_FOR PAIS_PROPRIED]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Propr;    
@{
    var data = Html.Leaflet<GenioMVC.Models.Propr>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodpropr, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_NAME]/
		column.Expression(p => p.ValName).Title(@Resources.PROPERTY_NAME18934).Field("ValName").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_PRECOEST]/
		column.Expression(p => p.ValPrecoest).Title(@Resources.ESTIMATED_PRICE02986).Field("ValPrecoest").Size(12);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_ENDERECO]/
		column.Expression(p => p.ValEndereco).Title(@Resources.ADDRESS04342).Field("ValEndereco").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_LOCALIDA]/
		column.Expression(p => p.ValLocalida).Title(@Resources.LOCALE34521).Field("ValLocalida").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_POSTALCO]/
		column.Expression(p => p.ValPostalco).Title(@Resources.ZIP_CODE56964).Field("ValPostalco").Size(20);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_POSTALLO]/
		column.Expression(p => p.ValPostallo).Title(@Resources.POSTAL_LOCATION08708).Field("ValPostallo").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_MOBILADA]/
		column.Expression(p => p.ValMobilada).Title(@Resources.FURNISHED37431).Field("ValMobilada").Size(1);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_QTD_WC]/
		column.Expression(p => p.ValQtd_wc).Title(@Resources.BATHROOMS54249).Field("ValQtd_wc").Size(6);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_QTDQUART]/
		column.Expression(p => p.ValQtdquart).Title(@Resources.ROOMS06809).Field("ValQtdquart").Size(6);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_M2]/
		column.Expression(p => p.ValM2).Title(@Resources.SQUARE_METERS28913).Field("ValM2").Size(6);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_DTDISPON]/
		column.Expression(p => p.ValDtdispon).Title(@Resources.AVAILABLE_FROM53703).Field("ValDtdispon").Size(8).Format("{0:d}");
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_PHOTOGRA]/
		column.Expression(p => Html.ImageMagnifierZoom(p.ValPhotogra, new {alt=@Resources.PHOTO51874,}, Url.Action("ImageHandlerGet", "Home", new { ticket = Helpers.GetFileTicket(UserContext.Current.User, "Propr", "ValPhotogra", "", p.ValCodpropr, null) }))).Title(@Resources.PHOTO51874).Field("ValPhotogra").Size(3);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_DESCRIPT]/
		column.Expression(p => p.ValDescript).Title(@Resources.DESCRIPTION07383).Field("ValDescript").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIED_PROPR_COORDGEO]/
		column.Expression(p => p.ValCoordgeo).Title(@Resources.GEOGRAPHIC_COORDINAT21394).Field("ValCoordgeo").Size(30);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Propr")
        .Id("ValPropried")
        .AjaxRequest("Pais_ValPropried")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
		// Temporary hack
        .ToLeaflet(hasFilters: false)
        .RequestLink(@Url.Action("Pais_ValPropried", new { id = Model.ValCodcntry, partialView = "Pais_ValPropried"}))
        .Form("Propr00", false, false)
        .SetFollowUp("Propr00_Show", "Propr" , (Func<GenioMVC.Models.Propr, object>)(item => new { id = item.ValCodpropr }), isRoutine: false, isSpecificPaths: false, isAjaxAction: false, htmlAttributes: new { }, openInPopup: false)
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
        // Temporary hack
        .ToLeaflet(hasFilters: false)
		;
        
}

@tbl.ToLeafletHtml()

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qValPropried, #ValPropried_complex_filter th input, #ValPropried_filters input").change(function() {
                    $("#pValPropried").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#ValPropried_tableFilters').val(false);")
                    @Html.Raw("$('#ValPropried_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#ValPropried_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $('#ValPropried_complex_filter button#applyComplexFilter').click();
                    @:      event.preventDefault();
                    @:  }
                    @:});
                }
                @Html.Raw("$('#ValPropried_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_ValPropried').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });


                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValPropried']"));
                    InitSpecialControls($('#ValPropried'));
                    $('#ValPropried').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS PAIS_VALPROPRIED]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>