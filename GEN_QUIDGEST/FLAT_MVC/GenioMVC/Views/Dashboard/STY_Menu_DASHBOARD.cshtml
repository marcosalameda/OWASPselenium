@model GenioMVC.ViewModels.Dashboard.STY_Menu_DASHBOARD_ViewModel
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@{ ViewBag.Title = Model.DashboardTitle; }
    
@using (Ajax.BeginForm("STY_Menu_DASHBOARD", "Dashboard", new AjaxOptions
{
    HttpMethod = "POST",
    InsertionMode = InsertionMode.Replace,
    UpdateTargetId = "Menu"
}, new { @class = "form-horizontal", area = "Dashboard", data_form = "STY_Menu_DASHBOARD" }))
{
    <div id="Menu">
        @{ Html.RenderPartial("Template/Dashboard"); }
    </div>
    @Html.Hidden("CurrentHistoryLevel", Model.Navigation.CurrentLevel.Level, new { data_interface_id = "STY_Menu_DASHBOARD" })
    @Html.Hidden("CurrentNavigationId", Model.Navigation.NavigationId, new { data_interface_id = "STY_Menu_DASHBOARD" })
    @Html.Hidden("NavigationFormHistLock", Model.Navigation.FormHistLock(), new { data_interface_id = "STY_Menu_DASHBOARD" })
    @Html.AntiForgeryToken()
}

@helper codeJavascript()
{
    <script>
        QLocalStorage.remLocalStorage("savedInfo");
    </script>
}


@section code_javascript {
    @codeJavascript()
}


