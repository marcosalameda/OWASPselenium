@model GenioMVC.ViewModels.Indoc.GQT_Menu_431_ViewModel
@using Newtonsoft.Json;
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Indoc;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Indoc>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCoddentr, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN GQT_MENU_431_INDOC_DOCUMENR]/
		column.Expression(p => p.ValDocumenr).Title(@Resources.NO_14817).Field("ValDocumenr").Size(10);
// USE /[MANUAL GQT CUSTOM_COLUMN GQT_MENU_431_INDOC_DHDOCUME]/
		column.Expression(p => p.ValDhdocume).Title(@Resources.DATE18475).Field("ValDhdocume").Size(16);
// USE /[MANUAL GQT CUSTOM_COLUMN GQT_MENU_431_WARE1_WAREHDES]/
		column.Expression(p => p.Ware1.ValWarehdes).Title(@Resources.WAREHOUSE51864).Field("Ware1.ValWarehdes").Size(30);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Indoc")
        .Id("GQT_Menu_431")
        .AjaxRequest("Menu")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .DBEdit()
        .Filters(this.Model.Menu.Filters)
        .Form("Dentr", false, false)
        .SetFollowUp("Dentr_Show", "Indoc" , (Func<GenioMVC.Models.Indoc, object>)(item => new { id = item.ValCoddentr }), isRoutine: false, isSpecificPaths: false, isAjaxAction: false, htmlAttributes: new { }, openInPopup: false)
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@tbl.ToHtml()


<script>



$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qGQT_Menu_431, #GQT_Menu_431_complex_filter th input, #GQT_Menu_431_filters input").change(function() {
                    $("#pGQT_Menu_431").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#GQT_Menu_431_tableFilters').val(false);")
                    @Html.Raw("$('#GQT_Menu_431_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#GQT_Menu_431_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $("[data-form='GQT_Menu_431']").submit();
                    @:      event.preventDefault();
                    @:      ClearSearchBox('qGQT_Menu_431');
                    @:  }
                    @:});
                }
                @Html.Raw("$('#GQT_Menu_431_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_GQT_Menu_431').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });
                 @if (this.Model.Menu.Filters.FiltersValues.Count >= 1) {
                    var activeFields = Newtonsoft.Json.JsonConvert.SerializeObject(this.Model.Menu.Filters.FiltersValues);
                    @: SetActiveFilters('GQT_Menu_431', 'DENTR', @Html.Raw(activeFields))
                  }


                setupHistoryClicks()
                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='Menu']"));
                    InitSpecialControls($('#Menu'));
                    $('#Menu').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS GQT_MENU_431]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
// USE /[MANUAL GQT LISTING_CODEJS GQT_MENU_431]/
</script>