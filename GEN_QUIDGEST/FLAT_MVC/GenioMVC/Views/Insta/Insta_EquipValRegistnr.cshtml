@model GenioMVC.ViewModels.Insta.Insta_EquipValRegistnr_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@{
    var searchStyle =  string.Empty;
}
<div class="search" @searchStyle>
    <div class="row">
        <div class="col">
            <h2 class="table-title c-table__title">@Resources.EQUIPMENT03632</h2>
        </div>
        <div class="col-auto normal-view" id="Insta_EquipValRegistnr_simple_filter">
            <div class="i-input-group" style="width:21rem; height: inherit;">
                @Html.TextBox("qInsta_EquipValRegistnr", this.Model.Menu.Query, new { @class = "i-input-group__field", @onkeydown = "ListSearchEnter(event)" })
                <div class="i-input-group--right">
                    <button type="button" elem-identifier="ListSearchButton" class="b-icon--secondary i-input-group__button--secondary" onclick="window.Insta_EquipValRegistnr.Search()"><i class="glyphicons glyphicons-search i-input-group__tag-icon i-input-group__button-icon"></i></button>

                    <button id="Insta_EquipValRegistnr_extra" class="b-icon--secondary i-input-group__button--secondary" type="button" onclick="changeFilters('#Insta_EquipValRegistnr_complex_filter', 'Insta_EquipValRegistnr_tableFilters')" title="@Resources.PESQUISA_AVANCADA06198"><span class="glyphicons glyphicons-zoom-in i-input-group__tag-icon i-input-group__button-icon"></span></button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="normal-view">
@{
    var data = Html.newTableFor<GenioMVC.Models.Equip>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: true)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodequip, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN INSTA_REGISTNR_EQUIP_DESIGNAT]/
		column.Expression(p => p.ValDesignat).Title(@Resources.DESIGNATION35876).Field("ValDesignat").Size(50);
// USE /[MANUAL GQT CUSTOM_COLUMN INSTA_REGISTNR_EQUIP_REGISTNR]/
		column.Expression(p => p.ValRegistnr).Title(@Resources.NO__REGISTER04207).Field("ValRegistnr").Size(6);
// USE /[MANUAL GQT CUSTOM_COLUMN INSTA_REGISTNR_EQUIP_SEQUENNR]/
		column.Expression(p => p.ValSequennr).Title(@Resources.SEQUENTIAL_NO_38590).Hidden().Field("ValSequennr").Size(6);
// USE /[MANUAL GQT CUSTOM_COLUMN INSTA_REGISTNR_EQUIP_DTAQUISI]/
		column.Expression(p => p.ValDtaquisi).Title(@Resources.ACQUISITION44180).Field("ValDtaquisi").Size(8).Format("{0:d}");
// USE /[MANUAL GQT CUSTOM_COLUMN INSTA_REGISTNR_EQUIP_DTDECO]/
		column.Expression(p => p.ValDtdeco).Title(@Resources.DECOMISSION14486).Field("ValDtdeco").Size(8).Format("{0:d}");
// USE /[MANUAL GQT CUSTOM_COLUMN INSTA_REGISTNR_EQUIP_PHOTOGRA]/
		column.Expression(p => Html.ImageMagnifierZoom(p.ValPhotogra, new {alt=@Resources.PHOTO51874,}, Url.Action("ImageHandlerGet", "Home", new { ticket = Helpers.GetFileTicket(UserContext.Current.User, "Equip", "ValPhotogra", "", p.ValCodequip, null) }))).Title(@Resources.PHOTO51874).Field("ValPhotogra").Size(3);
// USE /[MANUAL GQT CUSTOM_COLUMN INSTA_REGISTNR_EQUIP_VALORTOT]/
		column.Expression(p => p.ValValortot).Title(@Resources.TOTAL_VALUE30570).Field("ValValortot").Size(12);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
			//Hidden field used in JavaScript for return selected row text value
			column.Expression(p => p.ValRegistnr).Field("ValRegistnr").AddInlineStyle("display", "none", true).UsedForFilter(false);
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Equip")
        .Id("Insta_EquipValRegistnr")
        .AjaxRequest("Insta_EquipValRegistnr")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: true)
        .RequestLink(@Url.Action("Insta_EquipValRegistnr", new { id = Model.ValCodinsta, partialView = "Insta_EquipValRegistnr", TpequValCodtpequ = Model.ValCodtpequ}))
        .Filters(this.Model.Menu.Filters)
        .BackgroundColourOnCondition(p => p.ValZzstate != 0 ? "" : ((CSGenio.business.GlobalFunctions.emptyD((p.ValDtdeco))==1)?("RGB(255,255,255)"):("RGB(194,24,7)")))
        .Permissions(canView: false, canInsert: false, canEdit: false, canDelete: false, canDuplicate: false)
		;
        
}

@tbl.ToHtml()
</div>
<script>
    try {
        if (jQuery) {
            try {
                $("#qInsta_EquipValRegistnr, #TableEquipRegistnr_complex_filter th input").change(function() {
                    var context = $("#TableEquipRegistnr").parent();
                    $("#table-inputs input", context).val("");
                });

                var row = $("#Insta_EquipValRegistnr table tbody tr");
                row.mouseenter(function () {
                    $(this).addClass("highlighted");
                });
                row.mouseleave(function () {
                    $(this).removeClass("highlighted");
                });

                $('td:not([elem-identifier="RowActions"])', row).click(function () {
                    var row = $(this).parent();
                    var key = row.data("key");
                    var selectList = $('[data-identifier="IFF_INSTA___EQUIPREGISTNR"]');

                    selectList.trigger("DBEditSetValue", key);

                    $('#modal-dbedit').modal('hide');
                });

                $("td a").click(function (event) {
                    event.stopPropagation()
                });
                $('#Insta_EquipValRegistnr_complex_filter input').keypress(function (event) {
                    var keycode = (event.keyCode ? event.keyCode : event.which);
                    if(keycode == '13' || keycode == '10'){
                        $('#Insta_EquipValRegistnr_complex_filter button#applyComplexFilter').click();
                        event.preventDefault();
                    }
                });
@if(Model.Menu.Elements.Count() == 0 && !Model.Menu.Filters.ShowTableFilters)
{
<text>
                $('#Insta_EquipValRegistnr_extra').prop("disabled", true);
</text>
}


// USE /[MANUAL GQT LISTING_JS INSTA_MENU]/
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
</script>