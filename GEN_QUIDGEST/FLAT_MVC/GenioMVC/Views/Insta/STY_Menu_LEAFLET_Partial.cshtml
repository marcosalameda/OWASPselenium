@model GenioMVC.ViewModels.Insta.STY_Menu_LEAFLET_ViewModel
@using Newtonsoft.Json;
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Insta;    
@{
    var data = Html.Leaflet<GenioMVC.Models.Insta>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodinsta, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_TPEQU_TIPOEQUI]/
		column.Expression(p => p.Tpequ.ValTipoequi).Title(@Resources.TYPE_OF_EQUIPMENT18080).Field("Tpequ.ValTipoequi").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_EQUIP_REGISTNR]/
		column.Expression(p => p.Equip.ValRegistnr).Title(@Resources.NO__REGISTER04207).Field("Equip.ValRegistnr").Size(6);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_INSTA_DESIGNAT]/
		column.Expression(p => p.ValDesignat).Title(@Resources.SCHEDULING24801).Field("ValDesignat").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_INSTA_DTINIAGE]/
		column.Expression(p => p.ValDtiniage).Title(@Resources.BEGINNING18124).Field("ValDtiniage").Size(16);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_INSTA_DTFIMAGE]/
		column.Expression(p => p.ValDtfimage).Title(@Resources.END47577).Field("ValDtfimage").Size(16);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_INSTA_DESCRIPT]/
		column.Expression(p => p.ValDescript).Title(@Resources.DESCRIPTION07383).Field("ValDescript").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_INSTA_ALLDAY]/
		column.Expression(p => p.ValAllday).Title(@Resources.ALL_DAY18496).Field("ValAllday").Size(1);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_INSTA_SINCE]/
		column.Expression(p => p.ValSince).Title(@Resources.SINCE47259).Field("ValSince").Size(16);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_INSTA_UNTIL]/
		column.Expression(p => p.ValUntil).Title(@Resources.UNTIL39173).Field("ValUntil").Size(16);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_INSTA_HOURS]/
		column.Expression(p => p.ValHours).Title(@Resources.QTD_HOURS28684).Field("ValHours").Size(10);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_INSTA_PRECOHOR]/
		column.Expression(p => p.ValPrecohor).Title(@Resources.HOURLY_PRICE48005).Field("ValPrecohor").Size(12);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_INSTA_VALUE]/
		column.Expression(p => p.ValValue).Title(@Resources.VALUE10285).Field("ValValue").Size(12);
// USE /[MANUAL GQT CUSTOM_COLUMN STY_MENU_LEAFLET_INSTA_COORDGEO]/
		column.Expression(p => p.ValCoordgeo).Title(@Resources.GEOGRAPHIC_COORDINAT21394).Field("ValCoordgeo").Size(30);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Insta")
        .Id("STY_Menu_LEAFLET")
        .AjaxRequest("Menu")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
		// Temporary hack
        .ToLeaflet()
        .Filters(this.Model.Menu.Filters)
        .Form("Leaflett", false, false)
        .SetFollowUp("Leaflett_Show", "Insta" , (Func<GenioMVC.Models.Insta, object>)(item => new { id = item.ValCodinsta }), isRoutine: false, isSpecificPaths: false, isAjaxAction: false, htmlAttributes: new { }, openInPopup: false)
        .Permissions(canView: true, canInsert: false, canEdit: true, canDelete: true, canDuplicate: true)
        // Temporary hack
        .ToLeaflet(hasFilters: false)
		;
        
}

@tbl.ToLeafletHtml()


<script>



$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qSTY_Menu_LEAFLET, #STY_Menu_LEAFLET_complex_filter th input, #STY_Menu_LEAFLET_filters input").change(function() {
                    $("#pSTY_Menu_LEAFLET").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#STY_Menu_LEAFLET_tableFilters').val(false);")
                    @Html.Raw("$('#STY_Menu_LEAFLET_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#STY_Menu_LEAFLET_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $("[data-form='STY_Menu_LEAFLET']").submit();
                    @:      event.preventDefault();
                    @:      ClearSearchBox('qSTY_Menu_LEAFLET');
                    @:  }
                    @:});
                }
                @Html.Raw("$('#STY_Menu_LEAFLET_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_STY_Menu_LEAFLET').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });
                 @if (this.Model.Menu.Filters.FiltersValues.Count >= 1) {
                    var activeFields = Newtonsoft.Json.JsonConvert.SerializeObject(this.Model.Menu.Filters.FiltersValues);
                    @: SetActiveFilters('STY_Menu_LEAFLET', 'INSTA', @Html.Raw(activeFields))
                  }


                setupHistoryClicks()
                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='Menu']"));
                    InitSpecialControls($('#Menu'));
                    $('#Menu').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS STY_MENU_LEAFLET]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
// USE /[MANUAL GQT LISTING_CODEJS STY_MENU_LEAFLET]/
</script>