@model GenioMVC.ViewModels.Lnhag.Lnhag_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS LNHAG]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Lnhag !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Lnhag = new QForm($('[data-form="Lnhag"]'), "Form_Lnhag");
		}
		window.Form_Lnhag.baseArea = "lnhag";
		window.Form_Lnhag.FormMode = '@Model.GetFormMode';
		window.Form_Lnhag.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Lnhag.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Lnhag")',
			GetDependants:		'@Url.Action("GetDependants", "Lnhag")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Lnhag", "Lnhag")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Lnhag", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Lnhag", "Lnhag")',
		};
		window.Form_Lnhag.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Lnhag.formSaveEdit = '@Url.Action("Lnhag_SaveEdit", "Lnhag")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Lnhag.formAction = 'Lnhag_New';
		window.Form_Lnhag.formRedirectURL = '@Url.Action("Lnhag_New_Redirect", "Lnhag")';
		window.Form_Lnhag.formSubmitURL = '@Url.Action("Lnhag_New", "Lnhag")';
		window.Form_Lnhag.formRedirectURL_repeatInsertion = '@Url.Action("Lnhag_New_Redirect", "Lnhag", new { repeatInsertion = true})';
		window.Form_Lnhag.formSubmitURL_repeatInsertion = '@Url.Action("Lnhag_New", "Lnhag", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Lnhag.formAction = 'Lnhag_Edit';
		window.Form_Lnhag.formRedirectURL = '@Url.Action("Lnhag_Edit_Redirect", "Lnhag")';
		window.Form_Lnhag.formSubmitURL = '@Url.Action("Lnhag_Edit", "Lnhag")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Lnhag.formAction = 'Lnhag_Duplicate';
		window.Form_Lnhag.formRedirectURL = '@Url.Action("Lnhag_Duplicate_Redirect", "Lnhag")';
		window.Form_Lnhag.formSubmitURL = '@Url.Action("Lnhag_Duplicate", "Lnhag")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Lnhag.formAction = 'Lnhag_Delete';
		window.Form_Lnhag.formRedirectURL = '@Url.Action("Lnhag_Delete_Redirect", "Lnhag")';
		window.Form_Lnhag.formSubmitURL = '@Url.Action("Lnhag_Delete", "Lnhag")';
	</text>
}
		window.Form_Lnhag.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_LNHAG___PEDIDNRPEDIDO = new QDbeditControl($('[data-identifier="IFF_LNHAG___PEDIDNRPEDIDO"]', this.element), window.Form_Lnhag);

			this.Controls.IFF_LNHAG___TPEQ1TIPOEQUI = new QDbeditControl($('[data-identifier="IFF_LNHAG___TPEQ1TIPOEQUI"]', this.element), window.Form_Lnhag);

			this.Controls.LED_LNHAG___LNHAGQTDTPEQU = new QNumericControl($('[data-identifier="LED_LNHAG___LNHAGQTDTPEQU"]', this.element), window.Form_Lnhag);




			this.Controls.LED_LNHAG___LNHAGCODLNHAG = new QHiddenControl($('[data-identifier="LED_LNHAG___LNHAGCODLNHAG"]', this.element), window.Form_Lnhag);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Lnhag.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Lnhag.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI LNHAG]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Lnhag.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS LNHAG]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Lnhag.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS LNHAG]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Lnhag.Data.RelationKeysSelector = {
				get pedid() { return $(':not([elem-identifier="Multiform"]) > #ValCodpedid[pers-cs-area="Pedid"]', $('[data-form="Lnhag"]')); /* DBEdit */ },
				get tpeq1() { return $(':not([elem-identifier="Multiform"]) > #ValCodtpequ[pers-cs-area="Tpeq1"]', $('[data-form="Lnhag"]')); /* DBEdit */ },
		};

		window.Form_Lnhag.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Lnhag.Data.RelationKeys = {
			get formContext() { return window.Form_Lnhag; },
			get pedid() { return (this.formContext.Data.RelationKeysSelector.pedid.getQControl() || {}).Value; /* DBEdit */ },
			set pedid(newValue) { (this.formContext.Data.RelationKeysSelector.pedid.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get tpeq1() { return (this.formContext.Data.RelationKeysSelector.tpeq1.getQControl() || {}).Value; /* DBEdit */ },
			set tpeq1(newValue) { (this.formContext.Data.RelationKeysSelector.tpeq1.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Lnhag.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Lnhag.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitLnhag(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Lnhag.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteLnhag(event) {
			event.preventDefault();
			window.Form_Lnhag.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS LNHAG]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Lnhag"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
