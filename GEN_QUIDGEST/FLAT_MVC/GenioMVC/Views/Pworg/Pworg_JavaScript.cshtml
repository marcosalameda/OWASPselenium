@model GenioMVC.ViewModels.Pworg.Pworg_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS PWORG]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Pworg !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Pworg = new QForm($('[data-form="Pworg"]'), "Form_Pworg");
		}
		window.Form_Pworg.baseArea = "pworg";
		window.Form_Pworg.FormMode = '@Model.GetFormMode';
		window.Form_Pworg.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Pworg.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Pworg")',
			GetDependants:		'@Url.Action("GetDependants", "Pworg")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Pworg", "Pworg")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Pworg", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Pworg", "Pworg")',
		};
		window.Form_Pworg.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pworg.formSaveEdit = '@Url.Action("Pworg_SaveEdit", "Pworg")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Pworg.formAction = 'Pworg_New';
		window.Form_Pworg.formRedirectURL = '@Url.Action("Pworg_New_Redirect", "Pworg")';
		window.Form_Pworg.formSubmitURL = '@Url.Action("Pworg_New", "Pworg")';
		window.Form_Pworg.formRedirectURL_repeatInsertion = '@Url.Action("Pworg_New_Redirect", "Pworg", new { repeatInsertion = true})';
		window.Form_Pworg.formSubmitURL_repeatInsertion = '@Url.Action("Pworg_New", "Pworg", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Pworg.formAction = 'Pworg_Edit';
		window.Form_Pworg.formRedirectURL = '@Url.Action("Pworg_Edit_Redirect", "Pworg")';
		window.Form_Pworg.formSubmitURL = '@Url.Action("Pworg_Edit", "Pworg")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pworg.formAction = 'Pworg_Duplicate';
		window.Form_Pworg.formRedirectURL = '@Url.Action("Pworg_Duplicate_Redirect", "Pworg")';
		window.Form_Pworg.formSubmitURL = '@Url.Action("Pworg_Duplicate", "Pworg")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Pworg.formAction = 'Pworg_Delete';
		window.Form_Pworg.formRedirectURL = '@Url.Action("Pworg_Delete_Redirect", "Pworg")';
		window.Form_Pworg.formSubmitURL = '@Url.Action("Pworg_Delete", "Pworg")';
	</text>
}
		window.Form_Pworg.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_PWORG___PSW__NOME____ = new QDbeditControl($('[data-identifier="IFF_PWORG___PSW__NOME____"]', this.element), window.Form_Pworg);

			this.Controls.IFF_PWORG___ORGANORGANIZA = new QDbeditControl($('[data-identifier="IFF_PWORG___ORGANORGANIZA"]', this.element), window.Form_Pworg);




			this.Controls.LED_PWORG___PWORGCODPWORG = new QHiddenControl($('[data-identifier="LED_PWORG___PWORGCODPWORG"]', this.element), window.Form_Pworg);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Pworg.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pworg.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI PWORG]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Pworg.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS PWORG]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Pworg.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS PWORG]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Pworg.Data.RelationKeysSelector = {
				get psw() { return $(':not([elem-identifier="Multiform"]) > #ValCodpsw[pers-cs-area="Psw"]', $('[data-form="Pworg"]')); /* DBEdit */ },
				get organ() { return $(':not([elem-identifier="Multiform"]) > #ValCodorgan[pers-cs-area="Organ"]', $('[data-form="Pworg"]')); /* DBEdit */ },
		};

		window.Form_Pworg.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Pworg.Data.RelationKeys = {
			get formContext() { return window.Form_Pworg; },
			get psw() { return (this.formContext.Data.RelationKeysSelector.psw.getQControl() || {}).Value; /* DBEdit */ },
			set psw(newValue) { (this.formContext.Data.RelationKeysSelector.psw.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get organ() { return (this.formContext.Data.RelationKeysSelector.organ.getQControl() || {}).Value; /* DBEdit */ },
			set organ(newValue) { (this.formContext.Data.RelationKeysSelector.organ.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Pworg.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Pworg.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitPworg(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Pworg.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeletePworg(event) {
			event.preventDefault();
			window.Form_Pworg.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS PWORG]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Pworg"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
