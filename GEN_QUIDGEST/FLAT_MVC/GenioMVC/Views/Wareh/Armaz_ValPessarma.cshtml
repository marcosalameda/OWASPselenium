@model GenioMVC.ViewModels.Wareh.Armaz_ValPessarma_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
<div class="row">
	<div class="col">
		<div elem-identifier="List"  help-id="IFF_ARMAZ___PSEUDPESSARMA" style="padding-top: 0.5rem;"><label class = "i-text__label table-title c-table__title" > <h2 list-header>@Resources.EMPLOYEE55452</h2 list-header> </label></div>
	</div>
</div>

@* USE /[MANUAL GQT TABLE_FOR ARMAZ_PESSARMA]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Wpess;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Wpess>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodpess, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN ARMAZ_PESSARMA_WPESS_NAME]/
		column.Expression(p => p.ValName).Title(@Resources.NAME31974).Field("ValName").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN ARMAZ_PESSARMA_WPESS_SEX]/
		column.Expression(p => p.ArrayValsex).Title(@Resources.SEXO52099).Field("ValSex", "ArraySexo").Size(9);
// USE /[MANUAL GQT CUSTOM_COLUMN ARMAZ_PESSARMA_WPESS_NFUNC]/
		column.Expression(p => p.ValNfunc).Title(@Resources.NOFUNCIONARIO21429).Field("ValNfunc").Size(6);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Wpess")
        .Id("ValPessarma")
        .AjaxRequest("Armaz_ValPessarma")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: false)
        .RequestLink(@Url.Action("Armaz_ValPessarma", new { id = Model.ValCodwareh, partialView = "Armaz_ValPessarma"}))
        .Form("Pesspop", true, false)
        .AddTableAction(Model.IsEditableMode ? "Pesspop_Edit" : "Pesspop_Show", "Wpess" , (Func<GenioMVC.Models.Wpess, object>)(item => new { id = item.ValCodpess }), "LIST", @Resources.OPEN_FORM63276, isRoutine: false, isAjaxAction: false, openInPopup: true, isBootStrapIcon: true)
        .SetFollowUp(Model.IsEditableMode ? "Pesspop_Edit" : "Pesspop_Show", "Wpess" , (Func<GenioMVC.Models.Wpess, object>)(item => new { id = item.ValCodpess }), isRoutine: false, isAjaxAction: false, openInPopup: true, isSpecificPaths: false, htmlAttributes: new { })
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@tbl.ToHtml()

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qValPessarma, #ValPessarma_complex_filter th input, #ValPessarma_filters input").change(function() {
                    $("#pValPessarma").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#ValPessarma_tableFilters').val(false);")
                    @Html.Raw("$('#ValPessarma_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#ValPessarma_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $('#ValPessarma_complex_filter button#applyComplexFilter').click();
                    @:      event.preventDefault();
                    @:  }
                    @:});
                }
                @Html.Raw("$('#ValPessarma_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_ValPessarma').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });


                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValPessarma']"));
                    InitSpecialControls($('#ValPessarma'));
                    $('#ValPessarma').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS ARMAZ_VALPESSARMA]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>