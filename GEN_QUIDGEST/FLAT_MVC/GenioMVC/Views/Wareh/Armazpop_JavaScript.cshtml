@model GenioMVC.ViewModels.Wareh.Armazpop_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ARMAZPOP]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Armazpop !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Armazpop = new QForm($('[data-form="Armazpop"]'), "Form_Armazpop");
		}
		window.Form_Armazpop.baseArea = "wareh";
		window.Form_Armazpop.FormMode = '@Model.GetFormMode';
		window.Form_Armazpop.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Armazpop.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Wareh")',
			GetDependants:		'@Url.Action("GetDependants", "Wareh")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Armazpop", "Wareh")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Armazpop", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Wareh", "Wareh")',
		};
		window.Form_Armazpop.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Armazpop.formSaveEdit = '@Url.Action("Armazpop_SaveEdit", "Wareh")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Armazpop.formAction = 'Armazpop_New';
		window.Form_Armazpop.formRedirectURL = '@Url.Action("Armazpop_New_Redirect", "Wareh")';
		window.Form_Armazpop.formSubmitURL = '@Url.Action("Armazpop_New", "Wareh")';
		window.Form_Armazpop.formRedirectURL_repeatInsertion = '@Url.Action("Armazpop_New_Redirect", "Wareh", new { repeatInsertion = true})';
		window.Form_Armazpop.formSubmitURL_repeatInsertion = '@Url.Action("Armazpop_New", "Wareh", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Armazpop.formAction = 'Armazpop_Edit';
		window.Form_Armazpop.formRedirectURL = '@Url.Action("Armazpop_Edit_Redirect", "Wareh")';
		window.Form_Armazpop.formSubmitURL = '@Url.Action("Armazpop_Edit", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Armazpop.formAction = 'Armazpop_Duplicate';
		window.Form_Armazpop.formRedirectURL = '@Url.Action("Armazpop_Duplicate_Redirect", "Wareh")';
		window.Form_Armazpop.formSubmitURL = '@Url.Action("Armazpop_Duplicate", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Armazpop.formAction = 'Armazpop_Delete';
		window.Form_Armazpop.formRedirectURL = '@Url.Action("Armazpop_Delete_Redirect", "Wareh")';
		window.Form_Armazpop.formSubmitURL = '@Url.Action("Armazpop_Delete", "Wareh")';
	</text>
}
		window.Form_Armazpop.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_ARMAZPOPPSEUDARMAZ01_ = new QTabControl($('[data-identifier="IFF_ARMAZPOPPSEUDARMAZ01_"]', this.element), window.Form_Armazpop);

			this.Controls.IFF_ARMAZPOPPSEUDARMAZ02_ = new QTabControl($('[data-identifier="IFF_ARMAZPOPPSEUDARMAZ02_"]', this.element), window.Form_Armazpop);

			this.Controls.LED_ARMAZ01_WAREHWAREHCOD = new QTextControl($('[data-identifier="LED_ARMAZ01_WAREHWAREHCOD"]', this.element), window.Form_Armazpop);

			this.Controls.LED_ARMAZ01_WAREHACTIVITY = new QArrayLogicalControl($('[data-identifier="LED_ARMAZ01_WAREHACTIVITY"]', this.element), window.Form_Armazpop);

			this.Controls.LED_ARMAZ01_WAREHWAREHDES = new QTextControl($('[data-identifier="LED_ARMAZ01_WAREHWAREHDES"]', this.element), window.Form_Armazpop);

			this.Controls.IFF_ARMAZ02_PSEUDARTIGAPO = new QDateControl($('[data-identifier="IFF_ARMAZ02_PSEUDARTIGAPO"]', this.element), window.Form_Armazpop);

			this.Controls.IFF_ARMAZ02_PSEUDARTIGOS_ = new QTableListControl($('[data-identifier="IFF_ARMAZ02_PSEUDARTIGOS_"]', this.element), window.Form_Armazpop);




			this.Controls.LED_ARMAZPOPWAREHCODWAREH = new QHiddenControl($('[data-identifier="LED_ARMAZPOPWAREHCODWAREH"]', this.element), window.Form_Armazpop);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Armazpop.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Armazpop.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ARMAZPOP]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Armazpop.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ARMAZPOP]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Armazpop.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ARMAZPOP]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Armazpop.Data.RelationKeysSelector = {
		};

		window.Form_Armazpop.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Armazpop.Data.RelationKeys = {
			get formContext() { return window.Form_Armazpop; },
		};
	</text>
}

	window.Form_Armazpop.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Armazpop.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitArmazpop(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Armazpop.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteArmazpop(event) {
			event.preventDefault();
			window.Form_Armazpop.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
    modal_QuidgestGlobals = {
        UrlAction: {
            newWindow: '@Url.Action("newWindow")',
            ReloadDBEdit: '@Url.Action("ReloadDBEdit")',
            formArea: '	@Url.Action("form", "area")',
            UpdateValues: '@Url.Action("UpdateValues")',
            LogJavaScriptError: '@Url.Action("LogJavaScriptError", "ApplicationErrors")',
            syncFormKeys: '@Url.Action("syncFormKeys")',
            GetDependants: 		'@Url.Action("GetDependants")',
            ImageHandlerPut:		'@Url.Action("ImageHandlerPut")',
            ImageHandlerGet:		'@Url.Action("ImageHandlerGet")'
        }
    };
$(document).ready(function () {
	loaded($("#form-modal"));
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
	var ajaxCallsRemaining = $("#form-modal .flash object embed").length;
	$('#form-modal form [elem-identifier="ButtonPrimary"]').click(function() {
		var flashObjects = $("#form-modal .flash object embed");
		ajaxCallsRemaining = flashObjects.length;
		if(flashObjects.length == 0)
			return true;
		else {
			flashObjects.each(function() {
			   var flashObj = GetFlash($(this).attr('id'))
			   flashObj.UsedFlash.TriggerSave(flashObj);
			});
			return false;
		}
	});
    </text>
}
});
// USE /[MANUAL GQT FORM_CODEJS ARMAZPOP]/



 

    $($('[elem-identifier="Actions"]'), $('[data-form="Armazpop"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
