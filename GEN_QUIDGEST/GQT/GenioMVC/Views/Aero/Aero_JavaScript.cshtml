@model GenioMVC.ViewModels.Aero.Aero_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS AERO]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Aero !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Aero = new QForm($('[data-form="Aero"]'), "Form_Aero");
		}
		window.Form_Aero.baseArea = "aero";
		window.Form_Aero.FormMode = '@Model.GetFormMode';
		window.Form_Aero.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Aero.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Aero")',
			GetDependants:		'@Url.Action("GetDependants", "Aero")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Aero", "Aero")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Aero", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Aero", "Aero")',
		};
		window.Form_Aero.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Aero.formSaveEdit = '@Url.Action("Aero_SaveEdit", "Aero")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Aero.formAction = 'Aero_New';
		window.Form_Aero.formRedirectURL = '@Url.Action("Aero_New_Redirect", "Aero")';
		window.Form_Aero.formSubmitURL = '@Url.Action("Aero_New", "Aero")';
		window.Form_Aero.formRedirectURL_repeatInsertion = '@Url.Action("Aero_New_Redirect", "Aero", new { repeatInsertion = true})';
		window.Form_Aero.formSubmitURL_repeatInsertion = '@Url.Action("Aero_New", "Aero", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Aero.formAction = 'Aero_Edit';
		window.Form_Aero.formRedirectURL = '@Url.Action("Aero_Edit_Redirect", "Aero")';
		window.Form_Aero.formSubmitURL = '@Url.Action("Aero_Edit", "Aero")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Aero.formAction = 'Aero_Duplicate';
		window.Form_Aero.formRedirectURL = '@Url.Action("Aero_Duplicate_Redirect", "Aero")';
		window.Form_Aero.formSubmitURL = '@Url.Action("Aero_Duplicate", "Aero")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Aero.formAction = 'Aero_Delete';
		window.Form_Aero.formRedirectURL = '@Url.Action("Aero_Delete_Redirect", "Aero")';
		window.Form_Aero.formSubmitURL = '@Url.Action("Aero_Delete", "Aero")';
	</text>
}
		window.Form_Aero.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_AERO____AERO_NAME____ = new QTextControl($('[data-identifier="LED_AERO____AERO_NAME____"]', this.element), window.Form_Aero);

			this.Controls.LED_AERO____AERO_CODCMAER = new QNumericControl($('[data-identifier="LED_AERO____AERO_CODCMAER"]', this.element), window.Form_Aero);




			this.Controls.LED_AERO____AERO_CODAERO_ = new QHiddenControl($('[data-identifier="LED_AERO____AERO_CODAERO_"]', this.element), window.Form_Aero);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Aero.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Aero.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI AERO]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Aero.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS AERO]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Aero.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS AERO]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Aero.Data.RelationKeysSelector = {
		};

		window.Form_Aero.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Aero.Data.RelationKeys = {
			get formContext() { return window.Form_Aero; },
		};
	</text>
}

	window.Form_Aero.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Aero.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitAero(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Aero.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteAero(event) {
			event.preventDefault();
			window.Form_Aero.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS AERO]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Aero"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
