@model GenioMVC.ViewModels.Agreg.Agreg_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS AGREG]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Agreg !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Agreg = new QForm($('[data-form="Agreg"]'), "Form_Agreg");
		}
		window.Form_Agreg.baseArea = "agreg";
		window.Form_Agreg.FormMode = '@Model.GetFormMode';
		window.Form_Agreg.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Agreg.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Agreg")',
			GetDependants:		'@Url.Action("GetDependants", "Agreg")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Agreg", "Agreg")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Agreg", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Agreg", "Agreg")',
		};
		window.Form_Agreg.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Agreg.formSaveEdit = '@Url.Action("Agreg_SaveEdit", "Agreg")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Agreg.formAction = 'Agreg_New';
		window.Form_Agreg.formRedirectURL = '@Url.Action("Agreg_New_Redirect", "Agreg")';
		window.Form_Agreg.formSubmitURL = '@Url.Action("Agreg_New", "Agreg")';
		window.Form_Agreg.formRedirectURL_repeatInsertion = '@Url.Action("Agreg_New_Redirect", "Agreg", new { repeatInsertion = true})';
		window.Form_Agreg.formSubmitURL_repeatInsertion = '@Url.Action("Agreg_New", "Agreg", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Agreg.formAction = 'Agreg_Edit';
		window.Form_Agreg.formRedirectURL = '@Url.Action("Agreg_Edit_Redirect", "Agreg")';
		window.Form_Agreg.formSubmitURL = '@Url.Action("Agreg_Edit", "Agreg")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Agreg.formAction = 'Agreg_Duplicate';
		window.Form_Agreg.formRedirectURL = '@Url.Action("Agreg_Duplicate_Redirect", "Agreg")';
		window.Form_Agreg.formSubmitURL = '@Url.Action("Agreg_Duplicate", "Agreg")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Agreg.formAction = 'Agreg_Delete';
		window.Form_Agreg.formRedirectURL = '@Url.Action("Agreg_Delete_Redirect", "Agreg")';
		window.Form_Agreg.formSubmitURL = '@Url.Action("Agreg_Delete", "Agreg")';
	</text>
}
		window.Form_Agreg.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_AGREG___PROJEPROJECTO = new QDbeditControl($('[data-identifier="IFF_AGREG___PROJEPROJECTO"]', this.element), window.Form_Agreg);

			this.Controls.IFF_AGREG___YEAR_YEAR____ = new QDbeditControl($('[data-identifier="IFF_AGREG___YEAR_YEAR____"]', this.element), window.Form_Agreg);

			this.Controls.LED_AGREG___AGREGVALUE___ = new QNumericControl($('[data-identifier="LED_AGREG___AGREGVALUE___"]', this.element), window.Form_Agreg);




			this.Controls.LED_AGREG___AGREGCODAGGRE = new QHiddenControl($('[data-identifier="LED_AGREG___AGREGCODAGGRE"]', this.element), window.Form_Agreg);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Agreg.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Agreg.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI AGREG]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Agreg.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS AGREG]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Agreg.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS AGREG]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Agreg.Data.RelationKeysSelector = {
				get proje() { return $(':not([elem-identifier="Multiform"]) > #ValCodproje[pers-cs-area="Proje"]', $('[data-form="Agreg"]')); /* DBEdit */ },
				get year() { return $(':not([elem-identifier="Multiform"]) > #ValCodyear[trelate="Year"]', $('[data-form="Agreg"]')); /* DBEdit (Hidden) */ },
		};

		window.Form_Agreg.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Agreg.Data.RelationKeys = {
			get formContext() { return window.Form_Agreg; },
			get proje() { return (this.formContext.Data.RelationKeysSelector.proje.getQControl() || {}).Value; /* DBEdit */ },
			set proje(newValue) { (this.formContext.Data.RelationKeysSelector.proje.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get year() { return (this.formContext.Data.RelationKeysSelector.year.getQControl() || {}).Value; /* DBEdit */ },
			set year(newValue) { (this.formContext.Data.RelationKeysSelector.year.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Agreg.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Agreg.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitAgreg(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Agreg.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteAgreg(event) {
			event.preventDefault();
			window.Form_Agreg.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS AGREG]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Agreg"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
