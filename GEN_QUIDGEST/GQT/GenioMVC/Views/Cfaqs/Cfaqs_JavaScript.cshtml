@model GenioMVC.ViewModels.Cfaqs.Cfaqs_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS CFAQS]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Cfaqs !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Cfaqs = new QForm($('[data-form="Cfaqs"]'), "Form_Cfaqs");
		}
		window.Form_Cfaqs.baseArea = "cfaqs";
		window.Form_Cfaqs.FormMode = '@Model.GetFormMode';
		window.Form_Cfaqs.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Cfaqs.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Cfaqs")',
			GetDependants:		'@Url.Action("GetDependants", "Cfaqs")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Cfaqs", "Cfaqs")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Cfaqs", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Cfaqs", "Cfaqs")',
		};
		window.Form_Cfaqs.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Cfaqs.formSaveEdit = '@Url.Action("Cfaqs_SaveEdit", "Cfaqs")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Cfaqs.formAction = 'Cfaqs_New';
		window.Form_Cfaqs.formRedirectURL = '@Url.Action("Cfaqs_New_Redirect", "Cfaqs")';
		window.Form_Cfaqs.formSubmitURL = '@Url.Action("Cfaqs_New", "Cfaqs")';
		window.Form_Cfaqs.formRedirectURL_repeatInsertion = '@Url.Action("Cfaqs_New_Redirect", "Cfaqs", new { repeatInsertion = true})';
		window.Form_Cfaqs.formSubmitURL_repeatInsertion = '@Url.Action("Cfaqs_New", "Cfaqs", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Cfaqs.formAction = 'Cfaqs_Edit';
		window.Form_Cfaqs.formRedirectURL = '@Url.Action("Cfaqs_Edit_Redirect", "Cfaqs")';
		window.Form_Cfaqs.formSubmitURL = '@Url.Action("Cfaqs_Edit", "Cfaqs")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Cfaqs.formAction = 'Cfaqs_Duplicate';
		window.Form_Cfaqs.formRedirectURL = '@Url.Action("Cfaqs_Duplicate_Redirect", "Cfaqs")';
		window.Form_Cfaqs.formSubmitURL = '@Url.Action("Cfaqs_Duplicate", "Cfaqs")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Cfaqs.formAction = 'Cfaqs_Delete';
		window.Form_Cfaqs.formRedirectURL = '@Url.Action("Cfaqs_Delete_Redirect", "Cfaqs")';
		window.Form_Cfaqs.formSubmitURL = '@Url.Action("Cfaqs_Delete", "Cfaqs")';
	</text>
}
		window.Form_Cfaqs.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_CFAQS___CFAQSICON____ = new QImageControl($('[data-identifier="LED_CFAQS___CFAQSICON____"]', this.element), window.Form_Cfaqs, '@Html.Raw(Url.Action("ImageDelete", "Cfaqs", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Cfaqs", "ValIcon", "", Model.ValCodcfaqs, null) }))', '@Html.Raw(Url.Action("ImageCropper", "Cfaqs", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Cfaqs", "ValIcon", "", Model.ValCodcfaqs, null), identifier= "FCFAQS", FormName= "Cfaqs", FieldId= "LED_CFAQS___CFAQSICON____" }))',@Html.IsImageEmpty(Model.ValIcon));

			this.Controls.LED_CFAQS___CFAQSCATEGORY = new QGenericControl($('[data-identifier="LED_CFAQS___CFAQSCATEGORY"]', this.element), window.Form_Cfaqs);

			this.Controls.LED_CFAQS___CFAQSDESCRIPT = new QGenericControl($('[data-identifier="LED_CFAQS___CFAQSDESCRIPT"]', this.element), window.Form_Cfaqs);

			this.Controls.IFF_CFAQS___PSEUDEXPFAQS_ = new QTableListControl($('[data-identifier="IFF_CFAQS___PSEUDEXPFAQS_"]', this.element), window.Form_Cfaqs);




			this.Controls.LED_CFAQS___CFAQSCODCFAQS = new QHiddenControl($('[data-identifier="LED_CFAQS___CFAQSCODCFAQS"]', this.element), window.Form_Cfaqs);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Cfaqs.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Cfaqs.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI CFAQS]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Cfaqs.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS CFAQS]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Cfaqs.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS CFAQS]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Cfaqs.Data.RelationKeysSelector = {
		};

		window.Form_Cfaqs.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Cfaqs.Data.RelationKeys = {
			get formContext() { return window.Form_Cfaqs; },
		};
	</text>
}

	window.Form_Cfaqs.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Cfaqs.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitCfaqs(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Cfaqs.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteCfaqs(event) {
			event.preventDefault();
			window.Form_Cfaqs.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS CFAQS]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Cfaqs"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
