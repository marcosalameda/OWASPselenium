@model GenioMVC.ViewModels.Cfaqs.Cfaqs_ValExpfaqs_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
<div class="row">
	<div class="col">
		<div elem-identifier="List"  help-id="IFF_CFAQS___PSEUDEXPFAQS_" style="padding-top: 0.5rem;"><label class = "i-text__label table-title c-table__title" > <h2 list-header>@Resources.FAQS53959</h2 list-header> </label></div>
	</div>
</div>

@* USE /[MANUAL GQT TABLE_FOR CFAQS_EXPFAQS]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Faqs;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Faqs>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodfaqs, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN CFAQS_EXPFAQS_FAQS_QUESTION]/
		column.Expression(p => p.ValQuestion).Title(@Resources.QUESTION00194).Field("ValQuestion").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN CFAQS_EXPFAQS_FAQS_ANSWER]/
		column.Expression(p => p.ValAnswer).Title(@Resources.ANSWER22961).Field("ValAnswer").Size(30);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Faqs")
        .Id("ValExpfaqs")
        .AjaxRequest("Cfaqs_ValExpfaqs")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: false)
        .RequestLink(@Url.Action("Cfaqs_ValExpfaqs", new { id = Model.ValCodcfaqs, partialView = "Cfaqs_ValExpfaqs"}))
        .Form("Faqs", false, false)
        .SetFollowUp("Faqs_Show", "Faqs" , (Func<GenioMVC.Models.Faqs, object>)(item => new { id = item.ValCodfaqs }), isRoutine: false, isSpecificPaths: false, isAjaxAction: false, htmlAttributes: new { }, openInPopup: false)
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@tbl.ToHtml()

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qValExpfaqs, #ValExpfaqs_complex_filter th input, #ValExpfaqs_filters input").change(function() {
                    $("#pValExpfaqs").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#ValExpfaqs_tableFilters').val(false);")
                    @Html.Raw("$('#ValExpfaqs_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#ValExpfaqs_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $('#ValExpfaqs_complex_filter button#applyComplexFilter').click();
                    @:      event.preventDefault();
                    @:  }
                    @:});
                }
                @Html.Raw("$('#ValExpfaqs_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_ValExpfaqs').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });


                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValExpfaqs']"));
                    InitSpecialControls($('#ValExpfaqs'));
                    $('#ValExpfaqs').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS CFAQS_VALEXPFAQS]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>