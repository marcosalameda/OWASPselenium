@model GenioMVC.ViewModels.Cmpki.Cmpki_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS CMPKI]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Cmpki !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Cmpki = new QForm($('[data-form="Cmpki"]'), "Form_Cmpki");
		}
		window.Form_Cmpki.baseArea = "cmpki";
		window.Form_Cmpki.FormMode = '@Model.GetFormMode';
		window.Form_Cmpki.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Cmpki.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Cmpki")',
			GetDependants:		'@Url.Action("GetDependants", "Cmpki")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Cmpki", "Cmpki")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Cmpki", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Cmpki", "Cmpki")',
		};
		window.Form_Cmpki.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Cmpki.formSaveEdit = '@Url.Action("Cmpki_SaveEdit", "Cmpki")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Cmpki.formAction = 'Cmpki_New';
		window.Form_Cmpki.formRedirectURL = '@Url.Action("Cmpki_New_Redirect", "Cmpki")';
		window.Form_Cmpki.formSubmitURL = '@Url.Action("Cmpki_New", "Cmpki")';
		window.Form_Cmpki.formRedirectURL_repeatInsertion = '@Url.Action("Cmpki_New_Redirect", "Cmpki", new { repeatInsertion = true})';
		window.Form_Cmpki.formSubmitURL_repeatInsertion = '@Url.Action("Cmpki_New", "Cmpki", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Cmpki.formAction = 'Cmpki_Edit';
		window.Form_Cmpki.formRedirectURL = '@Url.Action("Cmpki_Edit_Redirect", "Cmpki")';
		window.Form_Cmpki.formSubmitURL = '@Url.Action("Cmpki_Edit", "Cmpki")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Cmpki.formAction = 'Cmpki_Duplicate';
		window.Form_Cmpki.formRedirectURL = '@Url.Action("Cmpki_Duplicate_Redirect", "Cmpki")';
		window.Form_Cmpki.formSubmitURL = '@Url.Action("Cmpki_Duplicate", "Cmpki")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Cmpki.formAction = 'Cmpki_Delete';
		window.Form_Cmpki.formRedirectURL = '@Url.Action("Cmpki_Delete_Redirect", "Cmpki")';
		window.Form_Cmpki.formSubmitURL = '@Url.Action("Cmpki_Delete", "Cmpki")';
	</text>
}
		window.Form_Cmpki.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_CMPKI___TPEQUTIPOEQUI = new QDbeditControl($('[data-identifier="IFF_CMPKI___TPEQUTIPOEQUI"]', this.element), window.Form_Cmpki);

			this.Controls.LED_CMPKI___CMPKIORDER___ = new QNumericControl($('[data-identifier="LED_CMPKI___CMPKIORDER___"]', this.element), window.Form_Cmpki);

			this.Controls.IFF_CMPKI___TPEQ1TIPOEQUI = new QDbeditControl($('[data-identifier="IFF_CMPKI___TPEQ1TIPOEQUI"]', this.element), window.Form_Cmpki);

			this.Controls.LED_CMPKI___CMPKIQUANTIDA = new QNumericControl($('[data-identifier="LED_CMPKI___CMPKIQUANTIDA"]', this.element), window.Form_Cmpki);

			this.Controls.LED_CMPKI___CMPKICODE____ = new QTextControl($('[data-identifier="LED_CMPKI___CMPKICODE____"]', this.element), window.Form_Cmpki);

			this.Controls.LED_CMPKI___CMPKIDESCRIPT = new QGenericControl($('[data-identifier="LED_CMPKI___CMPKIDESCRIPT"]', this.element), window.Form_Cmpki);

			this.Controls.LED_CMPKI___CMPKIURL_____ = new QTextControl($('[data-identifier="LED_CMPKI___CMPKIURL_____"]', this.element), window.Form_Cmpki);




			this.Controls.LED_CMPKI___CMPKICODCMPKI = new QHiddenControl($('[data-identifier="LED_CMPKI___CMPKICODCMPKI"]', this.element), window.Form_Cmpki);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Cmpki.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Cmpki.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI CMPKI]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Cmpki.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS CMPKI]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Cmpki.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS CMPKI]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Cmpki.Data.RelationKeysSelector = {
				get tpequ() { return $(':not([elem-identifier="Multiform"]) > #ValCodtpequ[pers-cs-area="Tpequ"]', $('[data-form="Cmpki"]')); /* DBEdit */ },
				get tpeq1() { return $(':not([elem-identifier="Multiform"]) > #ValCodtpeq1[pers-cs-area="Tpeq1"]', $('[data-form="Cmpki"]')); /* DBEdit */ },
		};

		window.Form_Cmpki.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Cmpki.Data.RelationKeys = {
			get formContext() { return window.Form_Cmpki; },
			get tpequ() { return (this.formContext.Data.RelationKeysSelector.tpequ.getQControl() || {}).Value; /* DBEdit */ },
			set tpequ(newValue) { (this.formContext.Data.RelationKeysSelector.tpequ.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get tpeq1() { return (this.formContext.Data.RelationKeysSelector.tpeq1.getQControl() || {}).Value; /* DBEdit */ },
			set tpeq1(newValue) { (this.formContext.Data.RelationKeysSelector.tpeq1.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Cmpki.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>

		@* Default of the 'ValCodtpeq1' field *@
		window.Form_Cmpki.SetFormulaDefault('IFF_CMPKI___TPEQ1TIPOEQUI', function() {
						return ( getFieldValue($(':not([elem-identifier="Multiform"]) > #ValCodtpequ', $('[data-form="Cmpki"]'))));
		}, 'VALCODTPEQU_CHANGE');
		





	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Cmpki.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitCmpki(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Cmpki.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteCmpki(event) {
			event.preventDefault();
			window.Form_Cmpki.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS CMPKI]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Cmpki"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
