@model GenioMVC.ViewModels.Cntry.Pais_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS PAIS]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Pais !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Pais = new QForm($('[data-form="Pais"]'), "Form_Pais");
		}
		window.Form_Pais.baseArea = "cntry";
		window.Form_Pais.FormMode = '@Model.GetFormMode';
		window.Form_Pais.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Pais.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Cntry")',
			GetDependants:		'@Url.Action("GetDependants", "Cntry")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Pais", "Cntry")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Pais", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Cntry", "Cntry")',
		};
		window.Form_Pais.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pais.formSaveEdit = '@Url.Action("Pais_SaveEdit", "Cntry")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Pais.formAction = 'Pais_New';
		window.Form_Pais.formRedirectURL = '@Url.Action("Pais_New_Redirect", "Cntry")';
		window.Form_Pais.formSubmitURL = '@Url.Action("Pais_New", "Cntry")';
		window.Form_Pais.formRedirectURL_repeatInsertion = '@Url.Action("Pais_New_Redirect", "Cntry", new { repeatInsertion = true})';
		window.Form_Pais.formSubmitURL_repeatInsertion = '@Url.Action("Pais_New", "Cntry", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Pais.formAction = 'Pais_Edit';
		window.Form_Pais.formRedirectURL = '@Url.Action("Pais_Edit_Redirect", "Cntry")';
		window.Form_Pais.formSubmitURL = '@Url.Action("Pais_Edit", "Cntry")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pais.formAction = 'Pais_Duplicate';
		window.Form_Pais.formRedirectURL = '@Url.Action("Pais_Duplicate_Redirect", "Cntry")';
		window.Form_Pais.formSubmitURL = '@Url.Action("Pais_Duplicate", "Cntry")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Pais.formAction = 'Pais_Delete';
		window.Form_Pais.formRedirectURL = '@Url.Action("Pais_Delete_Redirect", "Cntry")';
		window.Form_Pais.formSubmitURL = '@Url.Action("Pais_Delete", "Cntry")';
	</text>
}
		window.Form_Pais.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_PAIS____PSEUDNOVOGR02 = new QGenericControl($('[data-identifier="IFF_PAIS____PSEUDNOVOGR02"]', this.element), window.Form_Pais);

			this.Controls.LED_PAIS____CNTRYCOUNTRY_ = new QTextControl($('[data-identifier="LED_PAIS____CNTRYCOUNTRY_"]', this.element), window.Form_Pais);

			this.Controls.LED_PAIS____CNTRYACTIVE__ = new QCheckBoxControl($('[data-identifier="LED_PAIS____CNTRYACTIVE__"]', this.element), window.Form_Pais);

			this.Controls.IFF_PAIS____PSEUDNOVOGR01 = new QGenericControl($('[data-identifier="IFF_PAIS____PSEUDNOVOGR01"]', this.element), window.Form_Pais);

			this.Controls.LED_PAIS____CNTRYCODIGONR = new QTextControl($('[data-identifier="LED_PAIS____CNTRYCODIGONR"]', this.element), window.Form_Pais);

			this.Controls.LED_PAIS____CNTRYALFA2___ = new QTextControl($('[data-identifier="LED_PAIS____CNTRYALFA2___"]', this.element), window.Form_Pais);

			this.Controls.LED_PAIS____CNTRYALFA3___ = new QTextControl($('[data-identifier="LED_PAIS____CNTRYALFA3___"]', this.element), window.Form_Pais);

			this.Controls.LED_PAIS____CNTRYFLAG____ = new QImageControl($('[data-identifier="LED_PAIS____CNTRYFLAG____"]', this.element), window.Form_Pais, '@Html.Raw(Url.Action("ImageDelete", "Cntry", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Cntry", "ValFlag", "", Model.ValCodcntry, null) }))', '@Html.Raw(Url.Action("ImageCropper", "Cntry", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Cntry", "ValFlag", "", Model.ValCodcntry, null), identifier= "FPAIS", FormName= "Pais", FieldId= "LED_PAIS____CNTRYFLAG____" }))',@Html.IsImageEmpty(Model.ValFlag));

			this.Controls.IFF_PAIS____PSEUDIMOVEL__ = new QDateControl($('[data-identifier="IFF_PAIS____PSEUDIMOVEL__"]', this.element), window.Form_Pais);

			this.Controls.IFF_PAIS____PSEUDNOVOGR04 = new QGenericControl($('[data-identifier="IFF_PAIS____PSEUDNOVOGR04"]', this.element), window.Form_Pais);

			this.Controls.IFF_PAIS____PSEUDNOVOGR03 = new QGenericControl($('[data-identifier="IFF_PAIS____PSEUDNOVOGR03"]', this.element), window.Form_Pais);

			this.Controls.IFF_PAIS____PSEUDPROPRIE1 = new QTableListControl($('[data-identifier="IFF_PAIS____PSEUDPROPRIE1"]', this.element), window.Form_Pais);

			this.Controls.IFF_PAIS____PSEUDPROPRIED = new QTableListControl($('[data-identifier="IFF_PAIS____PSEUDPROPRIED"]', this.element), window.Form_Pais);




			this.Controls.LED_PAIS____CNTRYCODCNTRY = new QHiddenControl($('[data-identifier="LED_PAIS____CNTRYCODCNTRY"]', this.element), window.Form_Pais);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Pais.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pais.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI PAIS]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Pais.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS PAIS]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Pais.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS PAIS]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Pais.Data.RelationKeysSelector = {
		};

		window.Form_Pais.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Pais.Data.RelationKeys = {
			get formContext() { return window.Form_Pais; },
		};
	</text>
}

	window.Form_Pais.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Pais.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitPais(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Pais.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeletePais(event) {
			event.preventDefault();
			window.Form_Pais.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS PAIS]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Pais"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
