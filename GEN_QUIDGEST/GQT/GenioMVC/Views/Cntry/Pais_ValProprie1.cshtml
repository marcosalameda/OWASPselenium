@model GenioMVC.ViewModels.Cntry.Pais_ValProprie1_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
@if (this.Model.Menu.Elements.Count() >= 1)
{
<div class="row">
	<div class="col">
		<div elem-identifier="List"  help-id="IFF_PAIS____PSEUDPROPRIE1" style="padding-top: 0.5rem;"><label class = "i-text__label table-title c-table__title" > <h2 list-header>@Resources.REAL_ESTATE_LIST36497</h2 list-header> </label></div>
	</div>
	<div class="col-auto search">
	</div>
	<div class="col-auto"  style="padding: 0; padding-right: 15px">
		<div class="b-btn-group">
			@if(!GenioMVC.Models.Navigation.UserContext.Current.User.IsGuest())
			{
				<button class="b-btn b-icon b-icon--secondary" type="button" data-modal-form="Lstusr" data-modal-id="lstusr-edit-modal" data-table="ValProprie1" data-modal-url="@Url.Action("ChangeListProperties", "Lstusr", new { idlist = "Pais_ValProprie1", idlistController = "Cntry", idlistArea = "Propr" , codlista =""  })" title="@Resources.ALTERAR_A_APRESENTAC58401"><i class="glyphicons glyphicons-list e-icon"></i></button>
			}
		</div>
	</div>
</div>

}
@* USE /[MANUAL GQT TABLE_FOR PAIS_PROPRIE1]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Propr;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Propr>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodpropr, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIE1_PROPR_NAME]/
		column.Expression(p => p.ValName).Title(@Resources.PROPERTY_NAME18934).Field("ValName").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN PAIS_PROPRIE1_PROPR_PRECOEST]/
		column.Expression(p => p.ValPrecoest).Title(@Resources.ESTIMATED_PRICE02986).Field("ValPrecoest").Size(12);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Propr")
        .Id("ValProprie1")
        .AjaxRequest("Pais_ValProprie1")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: false)
        .RequestLink(@Url.Action("Pais_ValProprie1", new { id = Model.ValCodcntry, partialView = "Pais_ValProprie1"}))
        .Form("Propr00", false, false)
        .ExtendedForm("Propr", "IMOVEL")
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@tbl.ToHtml()

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                extendedAjaxForm('#ValProprie1');
                $("#qValProprie1, #ValProprie1_complex_filter th input, #ValProprie1_filters input").change(function() {
                    $("#pValProprie1").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#ValProprie1_tableFilters').val(false);")
                    @Html.Raw("$('#ValProprie1_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#ValProprie1_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $('#ValProprie1_complex_filter button#applyComplexFilter').click();
                    @:      event.preventDefault();
                    @:  }
                    @:});
                }
                @Html.Raw("$('#ValProprie1_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_ValProprie1').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });


                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValProprie1']"));
                    InitSpecialControls($('#ValProprie1'));
                    $('#ValProprie1').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS PAIS_VALPROPRIE1]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>