@model GenioMVC.ViewModels.Decom.Abate_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ABATE]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Abate !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Abate = new QForm($('[data-form="Abate"]'), "Form_Abate");
		}
		window.Form_Abate.baseArea = "decom";
		window.Form_Abate.FormMode = '@Model.GetFormMode';
		window.Form_Abate.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Abate.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Decom")',
			GetDependants:		'@Url.Action("GetDependants", "Decom")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Abate", "Decom")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Abate", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Decom", "Decom")',
		};
		window.Form_Abate.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Abate.formSaveEdit = '@Url.Action("Abate_SaveEdit", "Decom")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Abate.formAction = 'Abate_New';
		window.Form_Abate.formRedirectURL = '@Url.Action("Abate_New_Redirect", "Decom")';
		window.Form_Abate.formSubmitURL = '@Url.Action("Abate_New", "Decom")';
		window.Form_Abate.formRedirectURL_repeatInsertion = '@Url.Action("Abate_New_Redirect", "Decom", new { repeatInsertion = true})';
		window.Form_Abate.formSubmitURL_repeatInsertion = '@Url.Action("Abate_New", "Decom", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Abate.formAction = 'Abate_Edit';
		window.Form_Abate.formRedirectURL = '@Url.Action("Abate_Edit_Redirect", "Decom")';
		window.Form_Abate.formSubmitURL = '@Url.Action("Abate_Edit", "Decom")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Abate.formAction = 'Abate_Duplicate';
		window.Form_Abate.formRedirectURL = '@Url.Action("Abate_Duplicate_Redirect", "Decom")';
		window.Form_Abate.formSubmitURL = '@Url.Action("Abate_Duplicate", "Decom")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Abate.formAction = 'Abate_Delete';
		window.Form_Abate.formRedirectURL = '@Url.Action("Abate_Delete_Redirect", "Decom")';
		window.Form_Abate.formSubmitURL = '@Url.Action("Abate_Delete", "Decom")';
	</text>
}
		window.Form_Abate.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_ABATE___DECOMDECOMNR_ = new QNumericControl($('[data-identifier="LED_ABATE___DECOMDECOMNR_"]', this.element), window.Form_Abate);

			this.Controls.LED_ABATE___DECOMDTDECO__ = new QDateControl($('[data-identifier="LED_ABATE___DECOMDTDECO__"]', this.element), window.Form_Abate);

			this.Controls.LED_ABATE___DECOMNOTE____ = new QGenericControl($('[data-identifier="LED_ABATE___DECOMNOTE____"]', this.element), window.Form_Abate);




			this.Controls.LED_ABATE___DECOMCODDECO_ = new QHiddenControl($('[data-identifier="LED_ABATE___DECOMCODDECO_"]', this.element), window.Form_Abate);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Abate.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Abate.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ABATE]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Abate.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ABATE]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Abate.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ABATE]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Abate.Data.RelationKeysSelector = {
		};

		window.Form_Abate.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Abate.Data.RelationKeys = {
			get formContext() { return window.Form_Abate; },
		};
	</text>
}

	window.Form_Abate.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>

		@* Default of the 'ValDtdeco' field *@
		window.Form_Abate.SetFormulaDefault('LED_ABATE___DECOMDTDECO__', function() {
						return (qApi.Now());
		}, '');
		





	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Abate.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitAbate(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Abate.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteAbate(event) {
			event.preventDefault();
			window.Form_Abate.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS ABATE]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Abate"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
