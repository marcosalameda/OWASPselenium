@model GenioMVC.ViewModels.Decom.Abatereq_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ABATEREQ]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Abatereq !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Abatereq = new QForm($('[data-form="Abatereq"]'), "Form_Abatereq");
		}
		window.Form_Abatereq.baseArea = "decom";
		window.Form_Abatereq.FormMode = '@Model.GetFormMode';
		window.Form_Abatereq.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Abatereq.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Decom")',
			GetDependants:		'@Url.Action("GetDependants", "Decom")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Abatereq", "Decom")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Abatereq", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Decom", "Decom")',
		};
		window.Form_Abatereq.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Abatereq.formSaveEdit = '@Url.Action("Abatereq_SaveEdit", "Decom")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Abatereq.formAction = 'Abatereq_New';
		window.Form_Abatereq.formRedirectURL = '@Url.Action("Abatereq_New_Redirect", "Decom")';
		window.Form_Abatereq.formSubmitURL = '@Url.Action("Abatereq_New", "Decom")';
		window.Form_Abatereq.formRedirectURL_repeatInsertion = '@Url.Action("Abatereq_New_Redirect", "Decom", new { repeatInsertion = true})';
		window.Form_Abatereq.formSubmitURL_repeatInsertion = '@Url.Action("Abatereq_New", "Decom", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Abatereq.formAction = 'Abatereq_Edit';
		window.Form_Abatereq.formRedirectURL = '@Url.Action("Abatereq_Edit_Redirect", "Decom")';
		window.Form_Abatereq.formSubmitURL = '@Url.Action("Abatereq_Edit", "Decom")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Abatereq.formAction = 'Abatereq_Duplicate';
		window.Form_Abatereq.formRedirectURL = '@Url.Action("Abatereq_Duplicate_Redirect", "Decom")';
		window.Form_Abatereq.formSubmitURL = '@Url.Action("Abatereq_Duplicate", "Decom")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Abatereq.formAction = 'Abatereq_Delete';
		window.Form_Abatereq.formRedirectURL = '@Url.Action("Abatereq_Delete_Redirect", "Decom")';
		window.Form_Abatereq.formSubmitURL = '@Url.Action("Abatereq_Delete", "Decom")';
	</text>
}
		window.Form_Abatereq.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_ABATEREQPSEUDREQTEXT_ = new QTextControl($('[data-identifier="IFF_ABATEREQPSEUDREQTEXT_"]', this.element), window.Form_Abatereq);

			this.Controls.LED_ABATEREQDECOMDECOMNR_ = new QNumericControl($('[data-identifier="LED_ABATEREQDECOMDECOMNR_"]', this.element), window.Form_Abatereq);

			this.Controls.LED_ABATEREQDECOMNOTE____ = new QGenericControl($('[data-identifier="LED_ABATEREQDECOMNOTE____"]', this.element), window.Form_Abatereq);

			this.Controls.IFF_ABATEREQPSEUDCOLLAPSE = new QGenericControl($('[data-identifier="IFF_ABATEREQPSEUDCOLLAPSE"]', this.element), window.Form_Abatereq);

			this.Controls.IFF_ABATEREQPSEUDABATETAB = new QTabControl($('[data-identifier="IFF_ABATEREQPSEUDABATETAB"]', this.element), window.Form_Abatereq);

			this.Controls.LED_ABATETABDECOMDTDECO__ = new QDateControl($('[data-identifier="LED_ABATETABDECOMDTDECO__"]', this.element), window.Form_Abatereq);




			this.Controls.LED_ABATEREQDECOMCODDECO_ = new QHiddenControl($('[data-identifier="LED_ABATEREQDECOMCODDECO_"]', this.element), window.Form_Abatereq);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Abatereq.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Abatereq.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ABATEREQ]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Abatereq.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ABATEREQ]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Abatereq.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ABATEREQ]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Abatereq.Data.RelationKeysSelector = {
		};

		window.Form_Abatereq.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Abatereq.Data.RelationKeys = {
			get formContext() { return window.Form_Abatereq; },
		};
	</text>
}

	window.Form_Abatereq.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>

		@* Default of the 'ValDtdeco' field *@
		window.Form_Abatereq.SetFormulaDefault('LED_ABATETABDECOMDTDECO__', function() {
						return (qApi.Now());
		}, '');
		





	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Abatereq.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitAbatereq(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Abatereq.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteAbatereq(event) {
			event.preventDefault();
			window.Form_Abatereq.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS ABATEREQ]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Abatereq"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
