@model GenioMVC.ViewModels.Equip.Equdocum_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS EQUDOCUM]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Equdocum !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Equdocum = new QForm($('[data-form="Equdocum"]'), "Form_Equdocum");
		}
		window.Form_Equdocum.baseArea = "equip";
		window.Form_Equdocum.FormMode = '@Model.GetFormMode';
		window.Form_Equdocum.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Equdocum.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Equip")',
			GetDependants:		'@Url.Action("GetDependants", "Equip")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Equdocum", "Equip")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Equdocum", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Equip", "Equip")',
		};
		window.Form_Equdocum.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Equdocum.formSaveEdit = '@Url.Action("Equdocum_SaveEdit", "Equip")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Equdocum.formAction = 'Equdocum_New';
		window.Form_Equdocum.formRedirectURL = '@Url.Action("Equdocum_New_Redirect", "Equip")';
		window.Form_Equdocum.formSubmitURL = '@Url.Action("Equdocum_New", "Equip")';
		window.Form_Equdocum.formRedirectURL_repeatInsertion = '@Url.Action("Equdocum_New_Redirect", "Equip", new { repeatInsertion = true})';
		window.Form_Equdocum.formSubmitURL_repeatInsertion = '@Url.Action("Equdocum_New", "Equip", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Equdocum.formAction = 'Equdocum_Edit';
		window.Form_Equdocum.formRedirectURL = '@Url.Action("Equdocum_Edit_Redirect", "Equip")';
		window.Form_Equdocum.formSubmitURL = '@Url.Action("Equdocum_Edit", "Equip")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Equdocum.formAction = 'Equdocum_Duplicate';
		window.Form_Equdocum.formRedirectURL = '@Url.Action("Equdocum_Duplicate_Redirect", "Equip")';
		window.Form_Equdocum.formSubmitURL = '@Url.Action("Equdocum_Duplicate", "Equip")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Equdocum.formAction = 'Equdocum_Delete';
		window.Form_Equdocum.formRedirectURL = '@Url.Action("Equdocum_Delete_Redirect", "Equip")';
		window.Form_Equdocum.formSubmitURL = '@Url.Action("Equdocum_Delete", "Equip")';
	</text>
}
		window.Form_Equdocum.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_EQUDOCUMEQUIPDESIGNAT = new QTextControl($('[data-identifier="LED_EQUDOCUMEQUIPDESIGNAT"]', this.element), window.Form_Equdocum);

			this.Controls.IFF_EQUDOCUMPSEUDBTN_ANEX = new QButtonControl($('[data-identifier="IFF_EQUDOCUMPSEUDBTN_ANEX"]', this.element), window.Form_Equdocum);

			this.Controls.IFF_EQUDOCUMPSEUDLISANEX_ = new QTableListControl($('[data-identifier="IFF_EQUDOCUMPSEUDLISANEX_"]', this.element), window.Form_Equdocum);




@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	@*Hidden Foreign Keys Controls*@
			this.Controls.LED_EQUDOCUMROOM1CODROOMS = new QHiddenControl($('[data-identifier="LED_EQUDOCUMROOM1CODROOMS"]', this.element), window.Form_Equdocum);
			this.Controls.LED_EQUDOCUMDECOMCODDECO_ = new QHiddenControl($('[data-identifier="LED_EQUDOCUMDECOMCODDECO_"]', this.element), window.Form_Equdocum);
			this.Controls.LED_EQUDOCUMPESS1CODPESSO = new QHiddenControl($('[data-identifier="LED_EQUDOCUMPESS1CODPESSO"]', this.element), window.Form_Equdocum);
			this.Controls.LED_EQUDOCUMTPEQUCODTPEQU = new QHiddenControl($('[data-identifier="LED_EQUDOCUMTPEQUCODTPEQU"]', this.element), window.Form_Equdocum);
			this.Controls.LED_EQUDOCUMWAREHCODWAREH = new QHiddenControl($('[data-identifier="LED_EQUDOCUMWAREHCODWAREH"]', this.element), window.Form_Equdocum);
			this.Controls.LED_EQUDOCUMITEM_CODITEM_ = new QHiddenControl($('[data-identifier="LED_EQUDOCUMITEM_CODITEM_"]', this.element), window.Form_Equdocum);
			this.Controls.LED_EQUDOCUMCMPNYCODEMPRE = new QHiddenControl($('[data-identifier="LED_EQUDOCUMCMPNYCODEMPRE"]', this.element), window.Form_Equdocum);
	</text>
}
			this.Controls.LED_EQUDOCUMEQUIPCODEQUIP = new QHiddenControl($('[data-identifier="LED_EQUDOCUMEQUIPCODEQUIP"]', this.element), window.Form_Equdocum);

	@* Hidden Required Form Fields Controls for Formulas *@
			this.Controls.LED_EQUDOCUMEQUIPREGISTNR = new QSpecialControl($('[data-identifier="LED_EQUDOCUMEQUIPREGISTNR"]', this.element), window.Form_Equdocum);
			this.Controls.LED_EQUDOCUMEQUIPREGISTNR.InitData(@JavaScriptConversion.ToString(@Model.ValRegistnr));
			this.Controls.LED_EQUDOCUMITEM_ITEMDES_ = new QSpecialControl($('[data-identifier="LED_EQUDOCUMITEM_ITEMDES_"]', this.element), window.Form_Equdocum);
			this.Controls.LED_EQUDOCUMITEM_ITEMDES_.InitData(@JavaScriptConversion.ToString(@Model.ItemValItemdes));

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Equdocum.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Equdocum.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI EQUDOCUM]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Equdocum.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS EQUDOCUM]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Equdocum.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS EQUDOCUM]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Equdocum.Data.RelationKeysSelector = {
				get room1() { return $(':not([elem-identifier="Multiform"]) > #ValCodrooms', $('[data-form="Equdocum"]')); /* Hidden */ },
				get decom() { return $(':not([elem-identifier="Multiform"]) > #ValCoddeco', $('[data-form="Equdocum"]')); /* Hidden */ },
				get pess1() { return $(':not([elem-identifier="Multiform"]) > #ValCodpess1', $('[data-form="Equdocum"]')); /* Hidden */ },
				get tpequ() { return $(':not([elem-identifier="Multiform"]) > #ValCodtpequ', $('[data-form="Equdocum"]')); /* Hidden */ },
				get wareh() { return $(':not([elem-identifier="Multiform"]) > #ValCodwareh', $('[data-form="Equdocum"]')); /* Hidden */ },
				get item() { return $(':not([elem-identifier="Multiform"]) > #ValCoditem', $('[data-form="Equdocum"]')); /* Hidden */ },
				get cmpny() { return $(':not([elem-identifier="Multiform"]) > #ValCodempre', $('[data-form="Equdocum"]')); /* Hidden */ },
		};

		window.Form_Equdocum.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Equdocum.Data.RelationKeys = {
			get formContext() { return window.Form_Equdocum; },
			get room1() { return (this.formContext.Data.RelationKeysSelector.room1.getQControl() || {}).Value; /* Hidden FK */ },
			set room1(newValue) { (this.formContext.Data.RelationKeysSelector.room1.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get decom() { return (this.formContext.Data.RelationKeysSelector.decom.getQControl() || {}).Value; /* Hidden FK */ },
			set decom(newValue) { (this.formContext.Data.RelationKeysSelector.decom.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get pess1() { return (this.formContext.Data.RelationKeysSelector.pess1.getQControl() || {}).Value; /* Hidden FK */ },
			set pess1(newValue) { (this.formContext.Data.RelationKeysSelector.pess1.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get tpequ() { return (this.formContext.Data.RelationKeysSelector.tpequ.getQControl() || {}).Value; /* Hidden FK */ },
			set tpequ(newValue) { (this.formContext.Data.RelationKeysSelector.tpequ.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get wareh() { return (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value; /* Hidden FK */ },
			set wareh(newValue) { (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get item() { return (this.formContext.Data.RelationKeysSelector.item.getQControl() || {}).Value; /* Hidden FK */ },
			set item(newValue) { (this.formContext.Data.RelationKeysSelector.item.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get cmpny() { return (this.formContext.Data.RelationKeysSelector.cmpny.getQControl() || {}).Value; /* Hidden FK */ },
			set cmpny(newValue) { (this.formContext.Data.RelationKeysSelector.cmpny.getQControl() || {}).Value = newValue; /* Hidden FK */ },
		};
	</text>
}

	window.Form_Equdocum.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>

		@* Default of the 'ValDesignat' field *@
		window.Form_Equdocum.SetFormulaDefault('LED_EQUDOCUMEQUIPDESIGNAT', function() {
						return ( getFieldValue($(':not([elem-identifier="Multiform"]) > #ItemValItemdes', $('[data-form="Equdocum"]'))));
		}, 'ITEMVALITEMDES_CHANGE VALCODITEM_CHANGE');
		





	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Equdocum.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitEqudocum(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Equdocum.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteEqudocum(event) {
			event.preventDefault();
			window.Form_Equdocum.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS EQUDOCUM]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Equdocum"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
