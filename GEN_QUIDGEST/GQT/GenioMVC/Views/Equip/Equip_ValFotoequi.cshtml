@model GenioMVC.ViewModels.Equip.Equip_ValFotoequi_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
@if (this.Model.Menu.Elements.Count() >= 1)
{
<div class="row">
	<div class="col">
		<div elem-identifier="List"  help-id="IFF_EQUIP___PSEUDFOTOEQUI" style="padding-top: 0.5rem;"><label class = "i-text__label table-title c-table__title" > <h2 list-header>@Resources.PHOTOS39221</h2 list-header> </label></div>
	</div>
</div>

}
@* USE /[MANUAL GQT TABLE_FOR EQUIP_FOTOEQUI]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Photo;    
@{
    var data = Html.GridSlideShow<GenioMVC.Models.Photo>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodphoto, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP_FOTOEQUI_PHOTO_TITLE]/
		column.Expression(p => p.ValTitle).Title(@Resources.TITLE21885).Field("ValTitle").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP_FOTOEQUI_PHOTO_PHOTOGRA]/
		column.Expression(p => p.ValPhotogra).Title(@Resources.PHOTO51874).Field("ValPhotogra").Size(3);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Photo")
        .Id("ValFotoequi")
        .AjaxRequest("Equip_ValFotoequi")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
		// Temporary hack
        .ToGridSlideShow(hasFilters: false)
        .RequestLink(@Url.Action("Equip_ValFotoequi", new { id = Model.ValCodequip, partialView = "Equip_ValFotoequi"}))
        .Form("Fotos", false, false)
        .SetFollowUp("Fotos_Show", "Photo" , (Func<GenioMVC.Models.Photo, object>)(item => new { id = item.ValCodphoto }), isRoutine: false, isSpecificPaths: false, isAjaxAction: false, htmlAttributes: new { }, openInPopup: false)
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
        // Temporary hack
        .ToGridSlideShow(hasFilters: false)
		;
        
}

@tbl.ToGridSlideShowHtml()

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qValFotoequi, #ValFotoequi_complex_filter th input, #ValFotoequi_filters input").change(function() {
                    $("#pValFotoequi").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#ValFotoequi_tableFilters').val(false);")
                    @Html.Raw("$('#ValFotoequi_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#ValFotoequi_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $('#ValFotoequi_complex_filter button#applyComplexFilter').click();
                    @:      event.preventDefault();
                    @:  }
                    @:});
                }
                @Html.Raw("$('#ValFotoequi_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_ValFotoequi').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });


                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValFotoequi']"));
                    InitSpecialControls($('#ValFotoequi'));
                    $('#ValFotoequi').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS EQUIP_VALFOTOEQUI]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>