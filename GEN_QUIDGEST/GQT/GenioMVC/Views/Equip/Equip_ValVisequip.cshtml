@model GenioMVC.ViewModels.Equip.Equip_ValVisequip_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
<div class="row">
	<div class="col">
		<div elem-identifier="List"  help-id="IFF_EQUIP___PSEUDVISEQUIP" style="padding-top: 0.5rem;"><label class = "i-text__label table-title c-table__title" > <h2 list-header>@Resources.VISITS_63312</h2 list-header> </label></div>
	</div>
	<div class="col-auto search">
	</div>
	@{
		var listBtnCssClass = Model.IsViewModeHidden("LIST") ? "b-icon--secondary" : "b-icon--primary";
		var alternativeViewBtnCssClass = Model.IsViewModeHidden("LIST") ? "b-icon--primary" : "b-icon--secondary";
		
		<div class="col-auto" style="padding: 0; padding-right: 15px">
			<div class="b-btn-group">
				<button class="b-btn b-icon @listBtnCssClass" type="button" onclick="window.ValVisequip.ToggleViewMode('Equip_ValVisequip', 'Equip', 'Visit', codlista ='', 1)" title="@Resources.MUDAR_PARA_VISTA_EM_03626"><i class="glyphicons glyphicons-show-lines e-icon"></i></button>
				<button class="b-btn b-icon @alternativeViewBtnCssClass" type="button" onclick="window.ValVisequip.ToggleViewMode('Equip_ValVisequip', 'Equip', 'Visit', codlista ='', 2)" title="@Resources.MUDAR_PARA_VISTA_ALT46064"><i class="glyphicons glyphicons-show-big-thumbnails e-icon"></i></button>
			</div>
		</div>
	}
</div>

@* USE /[MANUAL GQT TABLE_FOR EQUIP_VISEQUIP]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Visit;    
@{
    var data = Html.FullCalendar<GenioMVC.Models.Visit>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodvisit, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP_VISEQUIP_VISIT_TITLE]/
		column.Expression(p => p.ValTitle).Title(@Resources.TITLE21885).Field("ValTitle").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP_VISEQUIP_VISIT_STARTDT]/
		column.Expression(p => p.ValStartdt).Title(@Resources.BEGINNING18124).Field("ValStartdt").Size(16);
// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP_VISEQUIP_VISIT_DTFIM]/
		column.Expression(p => p.ValDtfim).Title(@Resources.END47577).Field("ValDtfim").Size(16);
// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP_VISEQUIP_VISIT_DESCRIPT]/
		column.Expression(p => p.ValDescript).Title(@Resources.DESCRIPTION07383).Field("ValDescript").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP_VISEQUIP_VISIT_TODOODIA]/
		column.Expression(p => p.ValTodoodia).Title(@Resources.DAY27593).Field("ValTodoodia").Size(1);
// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP_VISEQUIP_VISIT_COLOR]/
		column.Expression(p => p.ValColor).Title(@Resources.COLOR55628).Field("ValColor").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP_VISEQUIP_VISIT_BACK]/
		column.Expression(p => p.ValBack).Title(@Resources.BACKGROUND45121).Field("ValBack").Size(1);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Visit")
        .Id("ValVisequip")
        .AjaxRequest("Equip_ValVisequip")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
		// Temporary hack
        .ToFullCalendar(hasFilters: false)
        .RequestLink(@Url.Action("Equip_ValVisequip", new { id = Model.ValCodequip, partialView = "Equip_ValVisequip"}))
        .Form("Visit", false, false)
        .SetFollowUp("Visit_Show", "Visit" , (Func<GenioMVC.Models.Visit, object>)(item => new { id = item.ValCodvisit }), isRoutine: false, isSpecificPaths: false, isAjaxAction: false, htmlAttributes: new { }, openInPopup: false)
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
        // Temporary hack
        .ToFullCalendar(hasFilters: false)
		;
        
    var calendar = data
        .ToSpecialRendering()
		.SetSpecialRendering(this.Model.ViewModesCfg.SpecialRenderings.FirstOrDefault(sr => sr.Id == "CALENDAR"))
        .RequestLink(@Url.Action("Equip_ValVisequip", new { id = Model.ValCodequip, partialView = "Equip_ValVisequip"}))
        .Form("Visit", false, false)
        .SetFollowUp("Visit_Show", "Visit" , (Func<GenioMVC.Models.Visit, object>)(item => new { id = item.ValCodvisit }), isRoutine: false, isSpecificPaths: false, isAjaxAction: false, htmlAttributes: new { }, openInPopup: false)
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@{ renderings.Clear(); }
@{ renderings.Add(Model.GetViewModePosition("LIST"), @tbl.ToFullCalendarHtml()); }
@{ renderings.Add(Model.GetViewModePosition("CALENDAR"), calendar.ToSpecialRenderingHtml(hidden: Model.IsViewModeHidden("CALENDAR"))); }


@foreach (var render in renderings.ToList().OrderBy(r => r.Key))
{
    @Html.Raw(render.Value.ToHtmlString());
}

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qValVisequip, #ValVisequip_complex_filter th input, #ValVisequip_filters input").change(function() {
                    $("#pValVisequip").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#ValVisequip_tableFilters').val(false);")
                    @Html.Raw("$('#ValVisequip_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#ValVisequip_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $('#ValVisequip_complex_filter button#applyComplexFilter').click();
                    @:      event.preventDefault();
                    @:  }
                    @:});
                }
                @Html.Raw("$('#ValVisequip_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_ValVisequip').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });


                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValVisequip']"));
                    InitSpecialControls($('#ValVisequip'));
                    $('#ValVisequip').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS EQUIP_VALVISEQUIP]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>