@model GenioMVC.ViewModels.Equip.Fullcale_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
<script src="@Url.Content("~/Scripts/FullCalendar/packages/core/main.min.js")" type="application/javascript"></script>
<script src="@Url.Content("~/Scripts/FullCalendar/packages/bootstrap/main.min.js")" type="application/javascript"></script>
<script src="@Url.Content("~/Scripts/FullCalendar/packages/interaction/main.min.js")" type="application/javascript"></script>
 <script src="@Url.Content("~/Scripts/FullCalendar/packages/daygrid/main.min.js")" type="application/javascript"></script>
   <script src="@Url.Content("~/Scripts/FullCalendar/packages/timegrid/main.min.js")" type="application/javascript"></script>
     <script src="@Url.Content("~/Scripts/popper.min.js")" type="application/javascript"></script>
<script src="@Url.Content("~/Scripts/FullCalendar/quidgest/quidgest.Calendar.js")" type="application/javascript"></script>
   @if (FullCalendarHelper.FullCallendarLocale.Key != "en")
{
    <script src="@Url.Content(FullCalendarHelper.FullCallendarLocale.Value)"></script>
}
 
@* USE /[MANUAL GQT FORM_INCLUDEJS FULLCALE]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Fullcale !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Fullcale = new QForm($('[data-form="Fullcale"]'), "Form_Fullcale");
		}
		window.Form_Fullcale.baseArea = "equip";
		window.Form_Fullcale.FormMode = '@Model.GetFormMode';
		window.Form_Fullcale.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Fullcale.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Equip")',
			GetDependants:		'@Url.Action("GetDependants", "Equip")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Fullcale", "Equip")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Fullcale", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Equip", "Equip")',
		};
		window.Form_Fullcale.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Fullcale.formSaveEdit = '@Url.Action("Fullcale_SaveEdit", "Equip")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Fullcale.formAction = 'Fullcale_New';
		window.Form_Fullcale.formRedirectURL = '@Url.Action("Fullcale_New_Redirect", "Equip")';
		window.Form_Fullcale.formSubmitURL = '@Url.Action("Fullcale_New", "Equip")';
		window.Form_Fullcale.formRedirectURL_repeatInsertion = '@Url.Action("Fullcale_New_Redirect", "Equip", new { repeatInsertion = true})';
		window.Form_Fullcale.formSubmitURL_repeatInsertion = '@Url.Action("Fullcale_New", "Equip", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Fullcale.formAction = 'Fullcale_Edit';
		window.Form_Fullcale.formRedirectURL = '@Url.Action("Fullcale_Edit_Redirect", "Equip")';
		window.Form_Fullcale.formSubmitURL = '@Url.Action("Fullcale_Edit", "Equip")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Fullcale.formAction = 'Fullcale_Duplicate';
		window.Form_Fullcale.formRedirectURL = '@Url.Action("Fullcale_Duplicate_Redirect", "Equip")';
		window.Form_Fullcale.formSubmitURL = '@Url.Action("Fullcale_Duplicate", "Equip")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Fullcale.formAction = 'Fullcale_Delete';
		window.Form_Fullcale.formRedirectURL = '@Url.Action("Fullcale_Delete_Redirect", "Equip")';
		window.Form_Fullcale.formSubmitURL = '@Url.Action("Fullcale_Delete", "Equip")';
	</text>
}
		window.Form_Fullcale.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_FULLCALEPSEUDFULLCALE = new QTableListControl($('[data-identifier="IFF_FULLCALEPSEUDFULLCALE"]', this.element), window.Form_Fullcale);




@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	@*Hidden Foreign Keys Controls*@
			this.Controls.LED_FULLCALETPEQUCODTPEQU = new QHiddenControl($('[data-identifier="LED_FULLCALETPEQUCODTPEQU"]', this.element), window.Form_Fullcale);
			this.Controls.LED_FULLCALEROOM1CODROOMS = new QHiddenControl($('[data-identifier="LED_FULLCALEROOM1CODROOMS"]', this.element), window.Form_Fullcale);
			this.Controls.LED_FULLCALEDECOMCODDECO_ = new QHiddenControl($('[data-identifier="LED_FULLCALEDECOMCODDECO_"]', this.element), window.Form_Fullcale);
			this.Controls.LED_FULLCALECMPNYCODEMPRE = new QHiddenControl($('[data-identifier="LED_FULLCALECMPNYCODEMPRE"]', this.element), window.Form_Fullcale);
			this.Controls.LED_FULLCALEWAREHCODWAREH = new QHiddenControl($('[data-identifier="LED_FULLCALEWAREHCODWAREH"]', this.element), window.Form_Fullcale);
			this.Controls.LED_FULLCALEITEM_CODITEM_ = new QHiddenControl($('[data-identifier="LED_FULLCALEITEM_CODITEM_"]', this.element), window.Form_Fullcale);
			this.Controls.LED_FULLCALEPESS1CODPESSO = new QHiddenControl($('[data-identifier="LED_FULLCALEPESS1CODPESSO"]', this.element), window.Form_Fullcale);
	</text>
}
			this.Controls.LED_FULLCALEEQUIPCODEQUIP = new QHiddenControl($('[data-identifier="LED_FULLCALEEQUIPCODEQUIP"]', this.element), window.Form_Fullcale);

	@* Hidden Required Form Fields Controls for Formulas *@
			this.Controls.LED_FULLCALEEQUIPREGISTNR = new QSpecialControl($('[data-identifier="LED_FULLCALEEQUIPREGISTNR"]', this.element), window.Form_Fullcale);
			this.Controls.LED_FULLCALEEQUIPREGISTNR.InitData(@JavaScriptConversion.ToString(@Model.ValRegistnr));

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Fullcale.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Fullcale.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI FULLCALE]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Fullcale.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS FULLCALE]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Fullcale.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS FULLCALE]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Fullcale.Data.RelationKeysSelector = {
				get tpequ() { return $(':not([elem-identifier="Multiform"]) > #ValCodtpequ', $('[data-form="Fullcale"]')); /* Hidden */ },
				get room1() { return $(':not([elem-identifier="Multiform"]) > #ValCodrooms', $('[data-form="Fullcale"]')); /* Hidden */ },
				get decom() { return $(':not([elem-identifier="Multiform"]) > #ValCoddeco', $('[data-form="Fullcale"]')); /* Hidden */ },
				get cmpny() { return $(':not([elem-identifier="Multiform"]) > #ValCodempre', $('[data-form="Fullcale"]')); /* Hidden */ },
				get wareh() { return $(':not([elem-identifier="Multiform"]) > #ValCodwareh', $('[data-form="Fullcale"]')); /* Hidden */ },
				get item() { return $(':not([elem-identifier="Multiform"]) > #ValCoditem', $('[data-form="Fullcale"]')); /* Hidden */ },
				get pess1() { return $(':not([elem-identifier="Multiform"]) > #ValCodpess1', $('[data-form="Fullcale"]')); /* Hidden */ },
		};

		window.Form_Fullcale.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Fullcale.Data.RelationKeys = {
			get formContext() { return window.Form_Fullcale; },
			get tpequ() { return (this.formContext.Data.RelationKeysSelector.tpequ.getQControl() || {}).Value; /* Hidden FK */ },
			set tpequ(newValue) { (this.formContext.Data.RelationKeysSelector.tpequ.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get room1() { return (this.formContext.Data.RelationKeysSelector.room1.getQControl() || {}).Value; /* Hidden FK */ },
			set room1(newValue) { (this.formContext.Data.RelationKeysSelector.room1.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get decom() { return (this.formContext.Data.RelationKeysSelector.decom.getQControl() || {}).Value; /* Hidden FK */ },
			set decom(newValue) { (this.formContext.Data.RelationKeysSelector.decom.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get cmpny() { return (this.formContext.Data.RelationKeysSelector.cmpny.getQControl() || {}).Value; /* Hidden FK */ },
			set cmpny(newValue) { (this.formContext.Data.RelationKeysSelector.cmpny.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get wareh() { return (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value; /* Hidden FK */ },
			set wareh(newValue) { (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get item() { return (this.formContext.Data.RelationKeysSelector.item.getQControl() || {}).Value; /* Hidden FK */ },
			set item(newValue) { (this.formContext.Data.RelationKeysSelector.item.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get pess1() { return (this.formContext.Data.RelationKeysSelector.pess1.getQControl() || {}).Value; /* Hidden FK */ },
			set pess1(newValue) { (this.formContext.Data.RelationKeysSelector.pess1.getQControl() || {}).Value = newValue; /* Hidden FK */ },
		};
	</text>
}

	window.Form_Fullcale.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Fullcale.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitFullcale(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Fullcale.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteFullcale(event) {
			event.preventDefault();
			window.Form_Fullcale.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS FULLCALE]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Fullcale"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
