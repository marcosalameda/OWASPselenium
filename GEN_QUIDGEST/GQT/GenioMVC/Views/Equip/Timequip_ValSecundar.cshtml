@model GenioMVC.ViewModels.Equip.Timequip_ValSecundar_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
<div class="row">
	<div class="col-12 col-md-6">
		<label class = "i-text__label table-title c-table__title">@Resources.TIMELINE_SECUNDARY50965</label>
	</div>
	<div class="col-12 col-md-6  mt-1">
		<div class="float-right">
			<span class="e-badge e-badge--info mr-2">Escala: Diária</span>
			<button type="button" elem-identifier="timeline-reset" class="b-icon b-icon--secondary"><i class="glyphicons glyphicons-refresh e-icon"></i></button>
		</div>
	</div>
</div>

@* USE /[MANUAL GQT TABLE_FOR TIMEQUIP_SECUNDAR]/ *@
@{ 

    
	var tbl = Html.TimelineFor<GenioMVC.Models.TimelineItem>(false)
        .Name("ValSecundar")
        .DataSource(this.Model.Menu.Elements)        
;}
@tbl.ToHtml()
<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {



                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValSecundar']"));
                    InitSpecialControls($('#ValSecundar'));
                    $('#ValSecundar').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS TIMEQUIP_VALSECUNDAR]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>