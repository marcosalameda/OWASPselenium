@model GenioMVC.ViewModels.Esppe.Esppe_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ESPPE]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Esppe !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Esppe = new QForm($('[data-form="Esppe"]'), "Form_Esppe");
		}
		window.Form_Esppe.baseArea = "esppe";
		window.Form_Esppe.FormMode = '@Model.GetFormMode';
		window.Form_Esppe.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Esppe.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Esppe")',
			GetDependants:		'@Url.Action("GetDependants", "Esppe")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Esppe", "Esppe")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Esppe", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Esppe", "Esppe")',
		};
		window.Form_Esppe.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Esppe.formSaveEdit = '@Url.Action("Esppe_SaveEdit", "Esppe")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Esppe.formAction = 'Esppe_New';
		window.Form_Esppe.formRedirectURL = '@Url.Action("Esppe_New_Redirect", "Esppe")';
		window.Form_Esppe.formSubmitURL = '@Url.Action("Esppe_New", "Esppe")';
		window.Form_Esppe.formRedirectURL_repeatInsertion = '@Url.Action("Esppe_New_Redirect", "Esppe", new { repeatInsertion = true})';
		window.Form_Esppe.formSubmitURL_repeatInsertion = '@Url.Action("Esppe_New", "Esppe", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Esppe.formAction = 'Esppe_Edit';
		window.Form_Esppe.formRedirectURL = '@Url.Action("Esppe_Edit_Redirect", "Esppe")';
		window.Form_Esppe.formSubmitURL = '@Url.Action("Esppe_Edit", "Esppe")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Esppe.formAction = 'Esppe_Duplicate';
		window.Form_Esppe.formRedirectURL = '@Url.Action("Esppe_Duplicate_Redirect", "Esppe")';
		window.Form_Esppe.formSubmitURL = '@Url.Action("Esppe_Duplicate", "Esppe")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Esppe.formAction = 'Esppe_Delete';
		window.Form_Esppe.formRedirectURL = '@Url.Action("Esppe_Delete_Redirect", "Esppe")';
		window.Form_Esppe.formSubmitURL = '@Url.Action("Esppe_Delete", "Esppe")';
	</text>
}
		window.Form_Esppe.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_ESPPE___PESSONAME____ = new QDbeditControl($('[data-identifier="IFF_ESPPE___PESSONAME____"]', this.element), window.Form_Esppe);

			this.Controls.IFF_ESPPE___SPECIESPECIAL = new QDbeditControl($('[data-identifier="IFF_ESPPE___SPECIESPECIAL"]', this.element), window.Form_Esppe);




	@*Hidden Required Foreign Keys for Formulas*@
			this.Controls.LED_ESPPE___PESSOCODPESSO = new QHiddenControl($('[data-identifier="LED_ESPPE___PESSOCODPESSO"]', this.element), window.Form_Esppe);
			this.Controls.LED_ESPPE___SPECICODESPEC = new QHiddenControl($('[data-identifier="LED_ESPPE___SPECICODESPEC"]', this.element), window.Form_Esppe);
			this.Controls.LED_ESPPE___ESPPECODESPPE = new QHiddenControl($('[data-identifier="LED_ESPPE___ESPPECODESPPE"]', this.element), window.Form_Esppe);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Esppe.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Esppe.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ESPPE]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Esppe.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ESPPE]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Esppe.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ESPPE]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Esppe.Data.RelationKeysSelector = {
				get pesso() { return $(':not([elem-identifier="Multiform"]) > #ValCodpesso[pers-cs-area="Pesso"]', $('[data-form="Esppe"]')); /* DBEdit */ },
				get speci() { return $(':not([elem-identifier="Multiform"]) > #ValCodespec[pers-cs-area="Speci"]', $('[data-form="Esppe"]')); /* DBEdit */ },
		};

		window.Form_Esppe.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Esppe.Data.RelationKeys = {
			get formContext() { return window.Form_Esppe; },
			get pesso() { return (this.formContext.Data.RelationKeysSelector.pesso.getQControl() || {}).Value; /* DBEdit */ },
			set pesso(newValue) { (this.formContext.Data.RelationKeysSelector.pesso.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get speci() { return (this.formContext.Data.RelationKeysSelector.speci.getQControl() || {}).Value; /* DBEdit */ },
			set speci(newValue) { (this.formContext.Data.RelationKeysSelector.speci.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Esppe.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Esppe.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitEsppe(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Esppe.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteEsppe(event) {
			event.preventDefault();
			window.Form_Esppe.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS ESPPE]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Esppe"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
