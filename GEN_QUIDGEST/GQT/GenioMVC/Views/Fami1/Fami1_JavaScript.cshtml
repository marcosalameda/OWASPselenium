@model GenioMVC.ViewModels.Fami1.Fami1_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS FAMI1]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Fami1 !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Fami1 = new QForm($('[data-form="Fami1"]'), "Form_Fami1");
		}
		window.Form_Fami1.baseArea = "fami1";
		window.Form_Fami1.FormMode = '@Model.GetFormMode';
		window.Form_Fami1.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Fami1.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Fami1")',
			GetDependants:		'@Url.Action("GetDependants", "Fami1")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Fami1", "Fami1")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Fami1", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Fami1", "Fami1")',
		};
		window.Form_Fami1.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Fami1.formSaveEdit = '@Url.Action("Fami1_SaveEdit", "Fami1")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Fami1.formAction = 'Fami1_New';
		window.Form_Fami1.formRedirectURL = '@Url.Action("Fami1_New_Redirect", "Fami1")';
		window.Form_Fami1.formSubmitURL = '@Url.Action("Fami1_New", "Fami1")';
		window.Form_Fami1.formRedirectURL_repeatInsertion = '@Url.Action("Fami1_New_Redirect", "Fami1", new { repeatInsertion = true})';
		window.Form_Fami1.formSubmitURL_repeatInsertion = '@Url.Action("Fami1_New", "Fami1", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Fami1.formAction = 'Fami1_Edit';
		window.Form_Fami1.formRedirectURL = '@Url.Action("Fami1_Edit_Redirect", "Fami1")';
		window.Form_Fami1.formSubmitURL = '@Url.Action("Fami1_Edit", "Fami1")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Fami1.formAction = 'Fami1_Duplicate';
		window.Form_Fami1.formRedirectURL = '@Url.Action("Fami1_Duplicate_Redirect", "Fami1")';
		window.Form_Fami1.formSubmitURL = '@Url.Action("Fami1_Duplicate", "Fami1")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Fami1.formAction = 'Fami1_Delete';
		window.Form_Fami1.formRedirectURL = '@Url.Action("Fami1_Delete_Redirect", "Fami1")';
		window.Form_Fami1.formSubmitURL = '@Url.Action("Fami1_Delete", "Fami1")';
	</text>
}
		window.Form_Fami1.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_FAMI1___FAMI1FAMILY__ = new QTextControl($('[data-identifier="LED_FAMI1___FAMI1FAMILY__"]', this.element), window.Form_Fami1);

			this.Controls.IFF_FAMI1___PSEUDTIPOSEQU = new QTableListControl($('[data-identifier="IFF_FAMI1___PSEUDTIPOSEQU"]', this.element), window.Form_Fami1);

			this.Controls.IFF_FAMI1___PSEUDTIPOSEQ1 = new QDateControl($('[data-identifier="IFF_FAMI1___PSEUDTIPOSEQ1"]', this.element), window.Form_Fami1);




			this.Controls.LED_FAMI1___FAMI1CODFAMIL = new QHiddenControl($('[data-identifier="LED_FAMI1___FAMI1CODFAMIL"]', this.element), window.Form_Fami1);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Fami1.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Fami1.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI FAMI1]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Fami1.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS FAMI1]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Fami1.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS FAMI1]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Fami1.Data.RelationKeysSelector = {
		};

		window.Form_Fami1.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Fami1.Data.RelationKeys = {
			get formContext() { return window.Form_Fami1; },
		};
	</text>
}

	window.Form_Fami1.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Fami1.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitFami1(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Fami1.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteFami1(event) {
			event.preventDefault();
			window.Form_Fami1.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS FAMI1]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Fami1"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
