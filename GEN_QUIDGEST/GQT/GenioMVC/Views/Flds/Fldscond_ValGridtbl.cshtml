@model GenioMVC.ViewModels.Flds.Fldscond_ValGridtbl_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
<div class="row">
	<div class="col">
	</div>
</div>

@* USE /[MANUAL GQT TABLE_FOR FLDSCOND_GRIDTBL]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@{
    var data = Html.newTableFor<GenioMVC.Models.Feeca>(edit: Model.IsEditableMode, canPage: true, canSort: false, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodfeeca, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN FLDSCOND_GRIDTBL_FEECA_FEEDBACK]/
		column.Expression(p => p.ValFeedback).Title(@Resources.FEEDBACK52855).Field("ValFeedback").Size(30);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Feeca")
        .Id("ValGridtbl")
        .AjaxRequest("Fldscond_ValGridtbl")
        .DataSource(this.Model.Menu.Elements)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .GridTableList()
        .RequestLink(@Url.Action("Fldscond_ValGridtbl", new { id = Model.ValCodflds, partialView = "Fldscond_ValGridtbl"}))
        .Form<GenioMVC.ViewModels.Feeca.Fldscondpseudgridtbl__ViewModel>("../Feeca/Fldscondpseudgridtbl_")
        .DataKeyName("ValCodfeeca")
        .ForeignKeyData("ValCodflds", Model.ValCodflds)
        .SaveActionLink(@Url.Action("Fldscondpseudgridtbl__Edit", "Feeca"))
        .DeleteActionLink(@Url.Action("Fldscondpseudgridtbl__Delete", "Feeca"))
        .InsertActionLink(@Url.Action("Fldscondpseudgridtbl__New", "Feeca"))
		;
        
}

@tbl.ToHtml()

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qValGridtbl, #ValGridtbl_complex_filter th input, #ValGridtbl_filters input").change(function() {
                    $("#pValGridtbl").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }



                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValGridtbl']"));
                    InitSpecialControls($('#ValGridtbl'));
                    $('#ValGridtbl').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS FLDSCOND_VALGRIDTBL]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>