@model GenioMVC.ViewModels.Flds.Fldscond_ValListtbl_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
<div class="row">
	<div class="col">
	</div>
</div>

@* USE /[MANUAL GQT TABLE_FOR FLDSCOND_LISTTBL]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Feeca;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Feeca>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodfeeca, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN FLDSCOND_LISTTBL_FEECA_FEEDBACK]/
		column.Expression(p => p.ValFeedback).Title(@Resources.FEEDBACK52855).Field("ValFeedback").Size(30);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Feeca")
        .Id("ValListtbl")
        .AjaxRequest("Fldscond_ValListtbl")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: false)
        .RequestLink(@Url.Action("Fldscond_ValListtbl", new { id = Model.ValCodflds, partialView = "Fldscond_ValListtbl"}))
        .Form("Feeca", false, false)
        .AddTableAction(Model.IsEditableMode ? "Feeca_Edit" : "Feeca_Show", "Feeca" , (Func<GenioMVC.Models.Feeca, object>)(item => new { id = item.ValCodfeeca }), "", @Resources.TEST37369, isRoutine: false, isAjaxAction: false, openInPopup: false, isBootStrapIcon: false)
        .SetFollowUp(Model.IsEditableMode ? "Feeca_Edit" : "Feeca_Show", "Feeca" , (Func<GenioMVC.Models.Feeca, object>)(item => new { id = item.ValCodfeeca }), isRoutine: false, isAjaxAction: false, openInPopup: false, isSpecificPaths: false, htmlAttributes: new { })
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@tbl.ToHtml()

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qValListtbl, #ValListtbl_complex_filter th input, #ValListtbl_filters input").change(function() {
                    $("#pValListtbl").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#ValListtbl_tableFilters').val(false);")
                    @Html.Raw("$('#ValListtbl_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#ValListtbl_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $('#ValListtbl_complex_filter button#applyComplexFilter').click();
                    @:      event.preventDefault();
                    @:  }
                    @:});
                }
                @Html.Raw("$('#ValListtbl_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_ValListtbl').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });


                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValListtbl']"));
                    InitSpecialControls($('#ValListtbl'));
                    $('#ValListtbl').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS FLDSCOND_VALLISTTBL]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>