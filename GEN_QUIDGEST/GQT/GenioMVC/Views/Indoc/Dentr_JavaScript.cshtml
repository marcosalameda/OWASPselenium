@model GenioMVC.ViewModels.Indoc.Dentr_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS DENTR]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Dentr !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Dentr = new QForm($('[data-form="Dentr"]'), "Form_Dentr");
		}
		window.Form_Dentr.baseArea = "indoc";
		window.Form_Dentr.FormMode = '@Model.GetFormMode';
		window.Form_Dentr.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Dentr.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Indoc")',
			GetDependants:		'@Url.Action("GetDependants", "Indoc")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Dentr", "Indoc")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Dentr", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Indoc", "Indoc")',
		};
		window.Form_Dentr.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Dentr.formSaveEdit = '@Url.Action("Dentr_SaveEdit", "Indoc")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Dentr.formAction = 'Dentr_New';
		window.Form_Dentr.formRedirectURL = '@Url.Action("Dentr_New_Redirect", "Indoc")';
		window.Form_Dentr.formSubmitURL = '@Url.Action("Dentr_New", "Indoc")';
		window.Form_Dentr.formRedirectURL_repeatInsertion = '@Url.Action("Dentr_New_Redirect", "Indoc", new { repeatInsertion = true})';
		window.Form_Dentr.formSubmitURL_repeatInsertion = '@Url.Action("Dentr_New", "Indoc", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Dentr.formAction = 'Dentr_Edit';
		window.Form_Dentr.formRedirectURL = '@Url.Action("Dentr_Edit_Redirect", "Indoc")';
		window.Form_Dentr.formSubmitURL = '@Url.Action("Dentr_Edit", "Indoc")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Dentr.formAction = 'Dentr_Duplicate';
		window.Form_Dentr.formRedirectURL = '@Url.Action("Dentr_Duplicate_Redirect", "Indoc")';
		window.Form_Dentr.formSubmitURL = '@Url.Action("Dentr_Duplicate", "Indoc")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Dentr.formAction = 'Dentr_Delete';
		window.Form_Dentr.formRedirectURL = '@Url.Action("Dentr_Delete_Redirect", "Indoc")';
		window.Form_Dentr.formSubmitURL = '@Url.Action("Dentr_Delete", "Indoc")';
	</text>
}
		window.Form_Dentr.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_DENTR___CNTRYCOUNTRY_ = new QDbeditControl($('[data-identifier="IFF_DENTR___CNTRYCOUNTRY_"]', this.element), window.Form_Dentr);

			this.Controls.IFF_DENTR___CMPNYDESIGNAT = new QDbeditControl($('[data-identifier="IFF_DENTR___CMPNYDESIGNAT"]', this.element), window.Form_Dentr);

			this.Controls.IFF_DENTR___PESSONAME____ = new QDbeditControl($('[data-identifier="IFF_DENTR___PESSONAME____"]', this.element), window.Form_Dentr);

			this.Controls.IFF_DENTR___WARE1WAREHDES = new QDbeditControl($('[data-identifier="IFF_DENTR___WARE1WAREHDES"]', this.element), window.Form_Dentr);

			this.Controls.LED_DENTR___INDOCDATE____ = new QDateControl($('[data-identifier="LED_DENTR___INDOCDATE____"]', this.element), window.Form_Dentr);

			this.Controls.LED_DENTR___INDOCDOCUMENR = new QNumericControl($('[data-identifier="LED_DENTR___INDOCDOCUMENR"]', this.element), window.Form_Dentr);

			this.Controls.LED_DENTR___INDOCDHDOCUME = new QDateControl($('[data-identifier="LED_DENTR___INDOCDHDOCUME"]', this.element), window.Form_Dentr);

			this.Controls.IFF_DENTR___PSEUDENTRADAS = new QTableListControl($('[data-identifier="IFF_DENTR___PSEUDENTRADAS"]', this.element), window.Form_Dentr);

			this.Controls.IFF_DENTR___PSEUDNORMAL__ = new QButtonControl($('[data-identifier="IFF_DENTR___PSEUDNORMAL__"]', this.element), window.Form_Dentr);




	@*Hidden Required Foreign Keys for Formulas*@
			this.Controls.LED_DENTR___CNTRYCODCNTRY = new QHiddenControl($('[data-identifier="LED_DENTR___CNTRYCODCNTRY"]', this.element), window.Form_Dentr);
			this.Controls.LED_DENTR___CMPNYCODEMPRE = new QHiddenControl($('[data-identifier="LED_DENTR___CMPNYCODEMPRE"]', this.element), window.Form_Dentr);
			this.Controls.LED_DENTR___PESSOCODPESSO = new QHiddenControl($('[data-identifier="LED_DENTR___PESSOCODPESSO"]', this.element), window.Form_Dentr);
			this.Controls.LED_DENTR___WARE1CODWAREH = new QHiddenControl($('[data-identifier="LED_DENTR___WARE1CODWAREH"]', this.element), window.Form_Dentr);
			this.Controls.LED_DENTR___INDOCCODDENTR = new QHiddenControl($('[data-identifier="LED_DENTR___INDOCCODDENTR"]', this.element), window.Form_Dentr);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	@* DBEdit Limits *@
			this.Controls.IFF_DENTR___CMPNYDESIGNAT.AddLimit(this.Controls.IFF_DENTR___CNTRYCOUNTRY_, 'A');
	@* DBEdit Limits *@
			this.Controls.IFF_DENTR___PESSONAME____.AddLimit(this.Controls.IFF_DENTR___CNTRYCOUNTRY_, 'A');
			this.Controls.IFF_DENTR___PESSONAME____.AddLimit(this.Controls.IFF_DENTR___CMPNYDESIGNAT, 'A');
	</text>
}

		window.Form_Dentr.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Dentr.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI DENTR]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Dentr.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS DENTR]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Dentr.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS DENTR]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Dentr.Data.RelationKeysSelector = {
				get cntry() { return $(':not([elem-identifier="Multiform"]) > #ValCodcntry[pers-cs-area="Cntry"]', $('[data-form="Dentr"]')); /* DBEdit */ },
				get cmpny() { return $(':not([elem-identifier="Multiform"]) > #ValCodempre[pers-cs-area="Cmpny"]', $('[data-form="Dentr"]')); /* DBEdit */ },
				get pesso() { return $(':not([elem-identifier="Multiform"]) > #ValCodpesso[pers-cs-area="Pesso"]', $('[data-form="Dentr"]')); /* DBEdit */ },
				get ware1() { return $(':not([elem-identifier="Multiform"]) > #ValCodwareh[pers-cs-area="Ware1"]', $('[data-form="Dentr"]')); /* DBEdit */ },
		};

		window.Form_Dentr.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Dentr.Data.RelationKeys = {
			get formContext() { return window.Form_Dentr; },
			get cntry() { return (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value; /* DBEdit */ },
			set cntry(newValue) { (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get cmpny() { return (this.formContext.Data.RelationKeysSelector.cmpny.getQControl() || {}).Value; /* DBEdit */ },
			set cmpny(newValue) { (this.formContext.Data.RelationKeysSelector.cmpny.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get pesso() { return (this.formContext.Data.RelationKeysSelector.pesso.getQControl() || {}).Value; /* DBEdit */ },
			set pesso(newValue) { (this.formContext.Data.RelationKeysSelector.pesso.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get ware1() { return (this.formContext.Data.RelationKeysSelector.ware1.getQControl() || {}).Value; /* DBEdit */ },
			set ware1(newValue) { (this.formContext.Data.RelationKeysSelector.ware1.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Dentr.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>

		@* Formula of the 'ValDate' field *@
		$(qForm).bind('VALCODWAREH_CHANGE VALDHDOCUME_CHANGE', function(event, eTarget) {
				try {
						var newValue = (qApi.iif(qApi.emptyG( getFieldValue($(':not([elem-identifier="Multiform"]) > #ValCodwareh', $('[data-form="Dentr"]'))))==1,"__/__/____", getFieldValue($(':not([elem-identifier="Multiform"]) > #ValDhdocume', $('[data-form="Dentr"]')))));
						$.when(newValue).done(function (result) {
								setFieldValue($(':not([elem-identifier="Multiform"]) > #ValDate', $('[data-form="Dentr"]')), result);
								if($(':not([elem-identifier="Multiform"]) > #ValDate', $('[data-form="Dentr"]')).data('QObject') === undefined) $(':not([elem-identifier="Multiform"]) > #ValDate', $('[data-form="Dentr"]')).change(); //TODO: Remove this later...
						});
				} catch (err) { QError.AppendError('Formula of field ValDate: ' + err.message, err.stack, window.location.href); }
		});
		





	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Dentr.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitDentr(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Dentr.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteDentr(event) {
			event.preventDefault();
			window.Form_Dentr.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS DENTR]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Dentr"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
