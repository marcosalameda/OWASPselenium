@model GenioMVC.ViewModels.Item.Artiginv_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ARTIGINV]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Artiginv !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Artiginv = new QForm($('[data-form="Artiginv"]'), "Form_Artiginv");
		}
		window.Form_Artiginv.baseArea = "item";
		window.Form_Artiginv.FormMode = '@Model.GetFormMode';
		window.Form_Artiginv.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Artiginv.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Item")',
			GetDependants:		'@Url.Action("GetDependants", "Item")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Artiginv", "Item")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Artiginv", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Item", "Item")',
		};
		window.Form_Artiginv.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Artiginv.formSaveEdit = '@Url.Action("Artiginv_SaveEdit", "Item")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Artiginv.formAction = 'Artiginv_New';
		window.Form_Artiginv.formRedirectURL = '@Url.Action("Artiginv_New_Redirect", "Item")';
		window.Form_Artiginv.formSubmitURL = '@Url.Action("Artiginv_New", "Item")';
		window.Form_Artiginv.formRedirectURL_repeatInsertion = '@Url.Action("Artiginv_New_Redirect", "Item", new { repeatInsertion = true})';
		window.Form_Artiginv.formSubmitURL_repeatInsertion = '@Url.Action("Artiginv_New", "Item", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Artiginv.formAction = 'Artiginv_Edit';
		window.Form_Artiginv.formRedirectURL = '@Url.Action("Artiginv_Edit_Redirect", "Item")';
		window.Form_Artiginv.formSubmitURL = '@Url.Action("Artiginv_Edit", "Item")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Artiginv.formAction = 'Artiginv_Duplicate';
		window.Form_Artiginv.formRedirectURL = '@Url.Action("Artiginv_Duplicate_Redirect", "Item")';
		window.Form_Artiginv.formSubmitURL = '@Url.Action("Artiginv_Duplicate", "Item")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Artiginv.formAction = 'Artiginv_Delete';
		window.Form_Artiginv.formRedirectURL = '@Url.Action("Artiginv_Delete_Redirect", "Item")';
		window.Form_Artiginv.formSubmitURL = '@Url.Action("Artiginv_Delete", "Item")';
	</text>
}
		window.Form_Artiginv.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_ARTIGINVITEM_IMAGE___ = new QImageControl($('[data-identifier="LED_ARTIGINVITEM_IMAGE___"]', this.element), window.Form_Artiginv, '@Html.Raw(Url.Action("ImageDelete", "Item", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Item", "ValImage", "", Model.ValCoditem, null) }))', '@Html.Raw(Url.Action("ImageCropper", "Item", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Item", "ValImage", "", Model.ValCoditem, null), identifier= "FARTIGINV", FormName= "Artiginv", FieldId= "LED_ARTIGINVITEM_IMAGE___" }))',@Html.IsImageEmpty(Model.ValImage));

			this.Controls.IFF_ARTIGINVGITEMITEMDES_ = new QDbeditControl($('[data-identifier="IFF_ARTIGINVGITEMITEMDES_"]', this.element), window.Form_Artiginv);

			this.Controls.IFF_ARTIGINVWAREHWAREHDES = new QDbeditControl($('[data-identifier="IFF_ARTIGINVWAREHWAREHDES"]', this.element), window.Form_Artiginv);

			this.Controls.LED_ARTIGINVITEM_ITEMTYPE = new QArrayControl($('[data-identifier="LED_ARTIGINVITEM_ITEMTYPE"]', this.element), window.Form_Artiginv);

			this.Controls.LED_ARTIGINVITEM_ITEMCOD_ = new QTextControl($('[data-identifier="LED_ARTIGINVITEM_ITEMCOD_"]', this.element), window.Form_Artiginv);

			this.Controls.LED_ARTIGINVITEM_ITEMDES_ = new QTextControl($('[data-identifier="LED_ARTIGINVITEM_ITEMDES_"]', this.element), window.Form_Artiginv);

			this.Controls.LED_ARTIGINVITEM_VALID___ = new QCheckBoxControl($('[data-identifier="LED_ARTIGINVITEM_VALID___"]', this.element), window.Form_Artiginv);




	@*Hidden Required Foreign Keys for Formulas*@
			this.Controls.LED_ARTIGINVGITEMCODGITEM = new QHiddenControl($('[data-identifier="LED_ARTIGINVGITEMCODGITEM"]', this.element), window.Form_Artiginv);
			this.Controls.LED_ARTIGINVWAREHCODWAREH = new QHiddenControl($('[data-identifier="LED_ARTIGINVWAREHCODWAREH"]', this.element), window.Form_Artiginv);
			this.Controls.LED_ARTIGINVITEM_CODITEM_ = new QHiddenControl($('[data-identifier="LED_ARTIGINVITEM_CODITEM_"]', this.element), window.Form_Artiginv);

	@* Hidden Required Form Fields Controls for Formulas *@
			this.Controls.LED_ARTIGINVGITEMITEMGCOD = new QSpecialControl($('[data-identifier="LED_ARTIGINVGITEMITEMGCOD"]', this.element), window.Form_Artiginv);
			this.Controls.LED_ARTIGINVGITEMITEMGCOD.InitData(@JavaScriptConversion.ToString(@Model.GitemValItemgcod));

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Artiginv.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Artiginv.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ARTIGINV]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Artiginv.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ARTIGINV]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Artiginv.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ARTIGINV]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Artiginv.Data.RelationKeysSelector = {
				get gitem() { return $(':not([elem-identifier="Multiform"]) > #ValCodgitem[pers-cs-area="Gitem"]', $('[data-form="Artiginv"]')); /* DBEdit */ },
				get wareh() { return $(':not([elem-identifier="Multiform"]) > #ValCodwareh[pers-cs-area="Wareh"]', $('[data-form="Artiginv"]')); /* DBEdit */ },
		};

		window.Form_Artiginv.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Artiginv.Data.RelationKeys = {
			get formContext() { return window.Form_Artiginv; },
			get gitem() { return (this.formContext.Data.RelationKeysSelector.gitem.getQControl() || {}).Value; /* DBEdit */ },
			set gitem(newValue) { (this.formContext.Data.RelationKeysSelector.gitem.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get wareh() { return (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value; /* DBEdit */ },
			set wareh(newValue) { (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Artiginv.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>

		@* Default of the 'ValItemcod' field *@
		window.Form_Artiginv.SetFormulaDefault('LED_ARTIGINVITEM_ITEMCOD_', function() {
						return ( getFieldValue($(':not([elem-identifier="Multiform"]) > #GitemValItemgcod', $('[data-form="Artiginv"]'))));
		}, 'GITEMVALITEMGCOD_CHANGE VALCODGITEM_CHANGE');
		
		@* Default of the 'ValItemdes' field *@
		window.Form_Artiginv.SetFormulaDefault('LED_ARTIGINVITEM_ITEMDES_', function() {
						return ( getFieldValue($(':not([elem-identifier="Multiform"]) > #ValCodgitem', $('[data-form="Artiginv"]')), true));
		}, 'GITEMVALITEMDES_CHANGE VALCODGITEM_CHANGE');
		





	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Artiginv.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitArtiginv(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Artiginv.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteArtiginv(event) {
			event.preventDefault();
			window.Form_Artiginv.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
    modal_QuidgestGlobals = {
        UrlAction: {
            newWindow: '@Url.Action("newWindow")',
            ReloadDBEdit: '@Url.Action("ReloadDBEdit")',
            formArea: '	@Url.Action("form", "area")',
            UpdateValues: '@Url.Action("UpdateValues")',
            LogJavaScriptError: '@Url.Action("LogJavaScriptError", "ApplicationErrors")',
            syncFormKeys: '@Url.Action("syncFormKeys")',
            GetDependants: 		'@Url.Action("GetDependants")',
            ImageHandlerPut:		'@Url.Action("ImageHandlerPut")',
            ImageHandlerGet:		'@Url.Action("ImageHandlerGet")'
        }
    };
$(document).ready(function () {
	loaded($("#form-modal"));
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
	var ajaxCallsRemaining = $("#form-modal .flash object embed").length;
	$('#form-modal form [elem-identifier="ButtonPrimary"]').click(function() {
		var flashObjects = $("#form-modal .flash object embed");
		ajaxCallsRemaining = flashObjects.length;
		if(flashObjects.length == 0)
			return true;
		else {
			flashObjects.each(function() {
			   var flashObj = GetFlash($(this).attr('id'))
			   flashObj.UsedFlash.TriggerSave(flashObj);
			});
			return false;
		}
	});
    </text>
}
});
// USE /[MANUAL GQT FORM_CODEJS ARTIGINV]/



 

    $($('[elem-identifier="Actions"]'), $('[data-form="Artiginv"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
