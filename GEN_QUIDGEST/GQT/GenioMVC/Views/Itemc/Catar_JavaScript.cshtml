@model GenioMVC.ViewModels.Itemc.Catar_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS CATAR]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Catar !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Catar = new QForm($('[data-form="Catar"]'), "Form_Catar");
		}
		window.Form_Catar.baseArea = "itemc";
		window.Form_Catar.FormMode = '@Model.GetFormMode';
		window.Form_Catar.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Catar.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Itemc")',
			GetDependants:		'@Url.Action("GetDependants", "Itemc")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Catar", "Itemc")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Catar", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Itemc", "Itemc")',
		};
		window.Form_Catar.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Catar.formSaveEdit = '@Url.Action("Catar_SaveEdit", "Itemc")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Catar.formAction = 'Catar_New';
		window.Form_Catar.formRedirectURL = '@Url.Action("Catar_New_Redirect", "Itemc")';
		window.Form_Catar.formSubmitURL = '@Url.Action("Catar_New", "Itemc")';
		window.Form_Catar.formRedirectURL_repeatInsertion = '@Url.Action("Catar_New_Redirect", "Itemc", new { repeatInsertion = true})';
		window.Form_Catar.formSubmitURL_repeatInsertion = '@Url.Action("Catar_New", "Itemc", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Catar.formAction = 'Catar_Edit';
		window.Form_Catar.formRedirectURL = '@Url.Action("Catar_Edit_Redirect", "Itemc")';
		window.Form_Catar.formSubmitURL = '@Url.Action("Catar_Edit", "Itemc")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Catar.formAction = 'Catar_Duplicate';
		window.Form_Catar.formRedirectURL = '@Url.Action("Catar_Duplicate_Redirect", "Itemc")';
		window.Form_Catar.formSubmitURL = '@Url.Action("Catar_Duplicate", "Itemc")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Catar.formAction = 'Catar_Delete';
		window.Form_Catar.formRedirectURL = '@Url.Action("Catar_Delete_Redirect", "Itemc")';
		window.Form_Catar.formSubmitURL = '@Url.Action("Catar_Delete", "Itemc")';
	</text>
}
		window.Form_Catar.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_CATAR___ITEM_ITEMDES_ = new QDbeditControl($('[data-identifier="IFF_CATAR___ITEM_ITEMDES_"]', this.element), window.Form_Catar);

			this.Controls.IFF_CATAR___CATTPTPCATEGO = new QDbeditControl($('[data-identifier="IFF_CATAR___CATTPTPCATEGO"]', this.element), window.Form_Catar);




	@*Hidden Required Foreign Keys for Formulas*@
			this.Controls.LED_CATAR___ITEM_CODITEM_ = new QHiddenControl($('[data-identifier="LED_CATAR___ITEM_CODITEM_"]', this.element), window.Form_Catar);
			this.Controls.LED_CATAR___CATTPCODTPCAT = new QHiddenControl($('[data-identifier="LED_CATAR___CATTPCODTPCAT"]', this.element), window.Form_Catar);
			this.Controls.LED_CATAR___ITEMCCODCATAR = new QHiddenControl($('[data-identifier="LED_CATAR___ITEMCCODCATAR"]', this.element), window.Form_Catar);

	@* Hidden Required Form Fields Controls for Formulas *@
			this.Controls.LED_CATAR___ITEMCTPCATEG_ = new QSpecialControl($('[data-identifier="LED_CATAR___ITEMCTPCATEG_"]', this.element), window.Form_Catar);
			this.Controls.LED_CATAR___ITEMCTPCATEG_.InitData(@JavaScriptConversion.ToString(@Model.ValTpcateg));

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Catar.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Catar.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI CATAR]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Catar.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS CATAR]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Catar.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS CATAR]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Catar.Data.RelationKeysSelector = {
				get item() { return $(':not([elem-identifier="Multiform"]) > #ValCoditem[pers-cs-area="Item"]', $('[data-form="Catar"]')); /* DBEdit */ },
				get cattp() { return $(':not([elem-identifier="Multiform"]) > #ValCodtpcat[pers-cs-area="Cattp"]', $('[data-form="Catar"]')); /* DBEdit */ },
		};

		window.Form_Catar.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Catar.Data.RelationKeys = {
			get formContext() { return window.Form_Catar; },
			get item() { return (this.formContext.Data.RelationKeysSelector.item.getQControl() || {}).Value; /* DBEdit */ },
			set item(newValue) { (this.formContext.Data.RelationKeysSelector.item.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get cattp() { return (this.formContext.Data.RelationKeysSelector.cattp.getQControl() || {}).Value; /* DBEdit */ },
			set cattp(newValue) { (this.formContext.Data.RelationKeysSelector.cattp.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Catar.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Catar.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitCatar(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Catar.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteCatar(event) {
			event.preventDefault();
			window.Form_Catar.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS CATAR]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Catar"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
