@model GenioMVC.ViewModels.Ldent.Ldent_IndocValDocumenr_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@{
    var searchStyle =  string.Empty;
}
<div class="search" @searchStyle>
    <div class="row">
        <div class="col">
            <h2 class="table-title c-table__title">@Resources.INPUT_DOCUMENTS05982</h2>
        </div>
        <div class="col-auto normal-view" id="Ldent_IndocValDocumenr_simple_filter">
            <div class="i-input-group" style="width:21rem; height: inherit;">
                @Html.TextBox("qLdent_IndocValDocumenr", this.Model.Menu.Query, new { @class = "i-input-group__field", @onkeydown = "ListSearchEnter(event)" })
                <div class="i-input-group--right">
                    <button type="button" elem-identifier="ListSearchButton" class="b-icon--secondary i-input-group__button--secondary" onclick="window.Ldent_IndocValDocumenr.Search()"><i class="glyphicons glyphicons-search i-input-group__tag-icon i-input-group__button-icon"></i></button>

                    <button id="Ldent_IndocValDocumenr_extra" class="b-icon--secondary i-input-group__button--secondary" type="button" onclick="changeFilters('#Ldent_IndocValDocumenr_complex_filter', 'Ldent_IndocValDocumenr_tableFilters')" title="@Resources.PESQUISA_AVANCADA06198"><span class="glyphicons glyphicons-zoom-in i-input-group__tag-icon i-input-group__button-icon"></span></button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="normal-view">
@{
    var data = Html.newTableFor<GenioMVC.Models.Indoc>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCoddentr, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN LDENT_DOCUMENR_INDOC_DOCUMENR]/
		column.Expression(p => p.ValDocumenr).Title(@Resources.NO_14817).Field("ValDocumenr").Size(10);
// USE /[MANUAL GQT CUSTOM_COLUMN LDENT_DOCUMENR_INDOC_DHDOCUME]/
		column.Expression(p => p.ValDhdocume).Title(@Resources.DATE18475).Field("ValDhdocume").Size(16);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
			//Hidden field used in JavaScript for return selected row text value
			column.Expression(p => p.ValDocumenr).Field("ValDocumenr").AddInlineStyle("display", "none", true).UsedForFilter(false);
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Indoc")
        .Id("Ldent_IndocValDocumenr")
        .AjaxRequest("Ldent_IndocValDocumenr")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: true)
        .RequestLink(@Url.Action("Ldent_IndocValDocumenr", new { id = Model.ValCodldent, partialView = "Ldent_IndocValDocumenr"}))
        .Filters(this.Model.Menu.Filters)
        .Permissions(canView: false, canInsert: false, canEdit: false, canDelete: false, canDuplicate: false)
		;
        
}

@tbl.ToHtml()
</div>
<script>
    try {
        if (jQuery) {
            try {
                $("#qLdent_IndocValDocumenr, #TableIndocDocumenr_complex_filter th input").change(function() {
                    var context = $("#TableIndocDocumenr").parent();
                    $("#table-inputs input", context).val("");
                });

                var row = $("#Ldent_IndocValDocumenr table tbody tr");
                row.mouseenter(function () {
                    $(this).addClass("highlighted");
                });
                row.mouseleave(function () {
                    $(this).removeClass("highlighted");
                });

                $('td:not([elem-identifier="RowActions"])', row).click(function () {
                    var row = $(this).parent();
                    var key = row.data("key");
                    var selectList = $('[data-identifier="IFF_LDENT___INDOCDOCUMENR"]');

                    selectList.trigger("DBEditSetValue", key);

                    $('#modal-dbedit').modal('hide');
                });

                $("td a").click(function (event) {
                    event.stopPropagation()
                });
                $('#Ldent_IndocValDocumenr_complex_filter input').keypress(function (event) {
                    var keycode = (event.keyCode ? event.keyCode : event.which);
                    if(keycode == '13' || keycode == '10'){
                        $('#Ldent_IndocValDocumenr_complex_filter button#applyComplexFilter').click();
                        event.preventDefault();
                    }
                });
@if(Model.Menu.Elements.Count() == 0 && !Model.Menu.Filters.ShowTableFilters)
{
<text>
                $('#Ldent_IndocValDocumenr_extra').prop("disabled", true);
</text>
}


// USE /[MANUAL GQT LISTING_JS LDENT_MENU]/
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
</script>