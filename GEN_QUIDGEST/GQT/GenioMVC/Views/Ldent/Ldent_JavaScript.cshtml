@model GenioMVC.ViewModels.Ldent.Ldent_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS LDENT]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Ldent !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Ldent = new QForm($('[data-form="Ldent"]'), "Form_Ldent");
		}
		window.Form_Ldent.baseArea = "ldent";
		window.Form_Ldent.FormMode = '@Model.GetFormMode';
		window.Form_Ldent.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Ldent.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Ldent")',
			GetDependants:		'@Url.Action("GetDependants", "Ldent")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Ldent", "Ldent")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Ldent", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Ldent", "Ldent")',
		};
		window.Form_Ldent.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Ldent.formSaveEdit = '@Url.Action("Ldent_SaveEdit", "Ldent")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Ldent.formAction = 'Ldent_New';
		window.Form_Ldent.formRedirectURL = '@Url.Action("Ldent_New_Redirect", "Ldent")';
		window.Form_Ldent.formSubmitURL = '@Url.Action("Ldent_New", "Ldent")';
		window.Form_Ldent.formRedirectURL_repeatInsertion = '@Url.Action("Ldent_New_Redirect", "Ldent", new { repeatInsertion = true})';
		window.Form_Ldent.formSubmitURL_repeatInsertion = '@Url.Action("Ldent_New", "Ldent", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Ldent.formAction = 'Ldent_Edit';
		window.Form_Ldent.formRedirectURL = '@Url.Action("Ldent_Edit_Redirect", "Ldent")';
		window.Form_Ldent.formSubmitURL = '@Url.Action("Ldent_Edit", "Ldent")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Ldent.formAction = 'Ldent_Duplicate';
		window.Form_Ldent.formRedirectURL = '@Url.Action("Ldent_Duplicate_Redirect", "Ldent")';
		window.Form_Ldent.formSubmitURL = '@Url.Action("Ldent_Duplicate", "Ldent")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Ldent.formAction = 'Ldent_Delete';
		window.Form_Ldent.formRedirectURL = '@Url.Action("Ldent_Delete_Redirect", "Ldent")';
		window.Form_Ldent.formSubmitURL = '@Url.Action("Ldent_Delete", "Ldent")';
	</text>
}
		window.Form_Ldent.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_LDENT___INDOCDOCUMENR = new QDbeditControl($('[data-identifier="IFF_LDENT___INDOCDOCUMENR"]', this.element), window.Form_Ldent);

			this.Controls.IFF_LDENT___WAREHWAREHDES = new QDbeditControl($('[data-identifier="IFF_LDENT___WAREHWAREHDES"]', this.element), window.Form_Ldent);

			this.Controls.LED_LDENT___LDENTLINE____ = new QNumericControl($('[data-identifier="LED_LDENT___LDENTLINE____"]', this.element), window.Form_Ldent);

			this.Controls.LED_LDENT___LDENTEMUSO___ = new QCheckBoxControl($('[data-identifier="LED_LDENT___LDENTEMUSO___"]', this.element), window.Form_Ldent);

			this.Controls.IFF_LDENT___ITEM_ITEMDES_ = new QDbeditControl($('[data-identifier="IFF_LDENT___ITEM_ITEMDES_"]', this.element), window.Form_Ldent);

			this.Controls.LED_LDENT___LDENTQTDENTRA = new QNumericControl($('[data-identifier="LED_LDENT___LDENTQTDENTRA"]', this.element), window.Form_Ldent);

			this.Controls.LED_LDENT___INDOCCODWAREH = new QTextControl($('[data-identifier="LED_LDENT___INDOCCODWAREH"]', this.element), window.Form_Ldent);




	@*Hidden Required Foreign Keys for Formulas*@
			this.Controls.LED_LDENT___INDOCCODDENTR = new QHiddenControl($('[data-identifier="LED_LDENT___INDOCCODDENTR"]', this.element), window.Form_Ldent);
			this.Controls.LED_LDENT___WAREHCODWAREH = new QHiddenControl($('[data-identifier="LED_LDENT___WAREHCODWAREH"]', this.element), window.Form_Ldent);
			this.Controls.LED_LDENT___ITEM_CODITEM_ = new QHiddenControl($('[data-identifier="LED_LDENT___ITEM_CODITEM_"]', this.element), window.Form_Ldent);
			this.Controls.LED_LDENT___LDENTCODLDENT = new QHiddenControl($('[data-identifier="LED_LDENT___LDENTCODLDENT"]', this.element), window.Form_Ldent);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	@* DBEdit Limits *@
			this.Controls.IFF_LDENT___ITEM_ITEMDES_.AddLimit(this.Controls.IFF_LDENT___WAREHWAREHDES, 'A');
	</text>
}

		window.Form_Ldent.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Ldent.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI LDENT]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Ldent.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS LDENT]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Ldent.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS LDENT]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Ldent.Data.RelationKeysSelector = {
				get indoc() { return $(':not([elem-identifier="Multiform"]) > #ValCoddentr[trelate="Indoc"]', $('[data-form="Ldent"]')); /* DBEdit (Hidden) */ },
				get wareh() { return $(':not([elem-identifier="Multiform"]) > #ValCodwareh[pers-cs-area="Wareh"]', $('[data-form="Ldent"]')); /* DBEdit */ },
				get item() { return $(':not([elem-identifier="Multiform"]) > #ValCoditem[pers-cs-area="Item"]', $('[data-form="Ldent"]')); /* DBEdit */ },
		};

		window.Form_Ldent.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Ldent.Data.RelationKeys = {
			get formContext() { return window.Form_Ldent; },
			get indoc() { return (this.formContext.Data.RelationKeysSelector.indoc.getQControl() || {}).Value; /* DBEdit */ },
			set indoc(newValue) { (this.formContext.Data.RelationKeysSelector.indoc.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get wareh() { return (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value; /* DBEdit */ },
			set wareh(newValue) { (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get item() { return (this.formContext.Data.RelationKeysSelector.item.getQControl() || {}).Value; /* DBEdit */ },
			set item(newValue) { (this.formContext.Data.RelationKeysSelector.item.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Ldent.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>

		@* Default of the 'ValCodwareh' field *@
		window.Form_Ldent.SetFormulaDefault('IFF_LDENT___WAREHWAREHDES', function() {
						return ( getFieldValue($(':not([elem-identifier="Multiform"]) > #IndocValCodwareh', $('[data-form="Ldent"]'))));
		}, 'INDOCVALCODWAREH_CHANGE VALCODDENTR_CHANGE');
		





	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Ldent.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitLdent(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Ldent.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteLdent(event) {
			event.preventDefault();
			window.Form_Ldent.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
    modal_QuidgestGlobals = {
        UrlAction: {
            newWindow: '@Url.Action("newWindow")',
            ReloadDBEdit: '@Url.Action("ReloadDBEdit")',
            formArea: '	@Url.Action("form", "area")',
            UpdateValues: '@Url.Action("UpdateValues")',
            LogJavaScriptError: '@Url.Action("LogJavaScriptError", "ApplicationErrors")',
            syncFormKeys: '@Url.Action("syncFormKeys")',
            GetDependants: 		'@Url.Action("GetDependants")',
            ImageHandlerPut:		'@Url.Action("ImageHandlerPut")',
            ImageHandlerGet:		'@Url.Action("ImageHandlerGet")'
        }
    };
$(document).ready(function () {
	loaded($("#form-modal"));
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
	var ajaxCallsRemaining = $("#form-modal .flash object embed").length;
	$('#form-modal form [elem-identifier="ButtonPrimary"]').click(function() {
		var flashObjects = $("#form-modal .flash object embed");
		ajaxCallsRemaining = flashObjects.length;
		if(flashObjects.length == 0)
			return true;
		else {
			flashObjects.each(function() {
			   var flashObj = GetFlash($(this).attr('id'))
			   flashObj.UsedFlash.TriggerSave(flashObj);
			});
			return false;
		}
	});
    </text>
}
});
// USE /[MANUAL GQT FORM_CODEJS LDENT]/



 

    $($('[elem-identifier="Actions"]'), $('[data-form="Ldent"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
