@model GenioMVC.ViewModels.Ldent.Ldentnor_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS LDENTNOR]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Ldentnor !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Ldentnor = new QForm($('[data-form="Ldentnor"]'), "Form_Ldentnor");
		}
		window.Form_Ldentnor.baseArea = "ldent";
		window.Form_Ldentnor.FormMode = '@Model.GetFormMode';
		window.Form_Ldentnor.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Ldentnor.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Ldent")',
			GetDependants:		'@Url.Action("GetDependants", "Ldent")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Ldentnor", "Ldent")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Ldentnor", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Ldent", "Ldent")',
		};
		window.Form_Ldentnor.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Ldentnor.formSaveEdit = '@Url.Action("Ldentnor_SaveEdit", "Ldent")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Ldentnor.formAction = 'Ldentnor_New';
		window.Form_Ldentnor.formRedirectURL = '@Url.Action("Ldentnor_New_Redirect", "Ldent")';
		window.Form_Ldentnor.formSubmitURL = '@Url.Action("Ldentnor_New", "Ldent")';
		window.Form_Ldentnor.formRedirectURL_repeatInsertion = '@Url.Action("Ldentnor_New_Redirect", "Ldent", new { repeatInsertion = true})';
		window.Form_Ldentnor.formSubmitURL_repeatInsertion = '@Url.Action("Ldentnor_New", "Ldent", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Ldentnor.formAction = 'Ldentnor_Edit';
		window.Form_Ldentnor.formRedirectURL = '@Url.Action("Ldentnor_Edit_Redirect", "Ldent")';
		window.Form_Ldentnor.formSubmitURL = '@Url.Action("Ldentnor_Edit", "Ldent")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Ldentnor.formAction = 'Ldentnor_Duplicate';
		window.Form_Ldentnor.formRedirectURL = '@Url.Action("Ldentnor_Duplicate_Redirect", "Ldent")';
		window.Form_Ldentnor.formSubmitURL = '@Url.Action("Ldentnor_Duplicate", "Ldent")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Ldentnor.formAction = 'Ldentnor_Delete';
		window.Form_Ldentnor.formRedirectURL = '@Url.Action("Ldentnor_Delete_Redirect", "Ldent")';
		window.Form_Ldentnor.formSubmitURL = '@Url.Action("Ldentnor_Delete", "Ldent")';
	</text>
}
		window.Form_Ldentnor.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_LDENTNORINDOCDOCUMENR = new QDbeditControl($('[data-identifier="IFF_LDENTNORINDOCDOCUMENR"]', this.element), window.Form_Ldentnor);

			this.Controls.IFF_LDENTNORWAREHWAREHDES = new QDbeditControl($('[data-identifier="IFF_LDENTNORWAREHWAREHDES"]', this.element), window.Form_Ldentnor);

			this.Controls.LED_LDENTNORLDENTLINE____ = new QNumericControl($('[data-identifier="LED_LDENTNORLDENTLINE____"]', this.element), window.Form_Ldentnor);

			this.Controls.IFF_LDENTNORITEM_ITEMDES_ = new QDbeditControl($('[data-identifier="IFF_LDENTNORITEM_ITEMDES_"]', this.element), window.Form_Ldentnor);

			this.Controls.LED_LDENTNORLDENTQTDENTRA = new QNumericControl($('[data-identifier="LED_LDENTNORLDENTQTDENTRA"]', this.element), window.Form_Ldentnor);

			this.Controls.LED_LDENTNORINDOCCODWAREH = new QTextControl($('[data-identifier="LED_LDENTNORINDOCCODWAREH"]', this.element), window.Form_Ldentnor);




	@*Hidden Required Foreign Keys for Formulas*@
			this.Controls.LED_LDENTNORINDOCCODDENTR = new QHiddenControl($('[data-identifier="LED_LDENTNORINDOCCODDENTR"]', this.element), window.Form_Ldentnor);
			this.Controls.LED_LDENTNORWAREHCODWAREH = new QHiddenControl($('[data-identifier="LED_LDENTNORWAREHCODWAREH"]', this.element), window.Form_Ldentnor);
			this.Controls.LED_LDENTNORITEM_CODITEM_ = new QHiddenControl($('[data-identifier="LED_LDENTNORITEM_CODITEM_"]', this.element), window.Form_Ldentnor);
			this.Controls.LED_LDENTNORLDENTCODLDENT = new QHiddenControl($('[data-identifier="LED_LDENTNORLDENTCODLDENT"]', this.element), window.Form_Ldentnor);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	@* DBEdit Limits *@
			this.Controls.IFF_LDENTNORITEM_ITEMDES_.AddLimit(this.Controls.IFF_LDENTNORWAREHWAREHDES, 'A');
	</text>
}

		window.Form_Ldentnor.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Ldentnor.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI LDENTNOR]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Ldentnor.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS LDENTNOR]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Ldentnor.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS LDENTNOR]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Ldentnor.Data.RelationKeysSelector = {
				get indoc() { return $(':not([elem-identifier="Multiform"]) > #ValCoddentr[trelate="Indoc"]', $('[data-form="Ldentnor"]')); /* DBEdit (Hidden) */ },
				get wareh() { return $(':not([elem-identifier="Multiform"]) > #ValCodwareh[pers-cs-area="Wareh"]', $('[data-form="Ldentnor"]')); /* DBEdit */ },
				get item() { return $(':not([elem-identifier="Multiform"]) > #ValCoditem[pers-cs-area="Item"]', $('[data-form="Ldentnor"]')); /* DBEdit */ },
		};

		window.Form_Ldentnor.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Ldentnor.Data.RelationKeys = {
			get formContext() { return window.Form_Ldentnor; },
			get indoc() { return (this.formContext.Data.RelationKeysSelector.indoc.getQControl() || {}).Value; /* DBEdit */ },
			set indoc(newValue) { (this.formContext.Data.RelationKeysSelector.indoc.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get wareh() { return (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value; /* DBEdit */ },
			set wareh(newValue) { (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get item() { return (this.formContext.Data.RelationKeysSelector.item.getQControl() || {}).Value; /* DBEdit */ },
			set item(newValue) { (this.formContext.Data.RelationKeysSelector.item.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Ldentnor.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>

		@* Default of the 'ValCodwareh' field *@
		window.Form_Ldentnor.SetFormulaDefault('IFF_LDENTNORWAREHWAREHDES', function() {
						return ( getFieldValue($(':not([elem-identifier="Multiform"]) > #IndocValCodwareh', $('[data-form="Ldentnor"]'))));
		}, 'INDOCVALCODWAREH_CHANGE VALCODDENTR_CHANGE');
		





	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Ldentnor.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitLdentnor(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Ldentnor.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteLdentnor(event) {
			event.preventDefault();
			window.Form_Ldentnor.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS LDENTNOR]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Ldentnor"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
