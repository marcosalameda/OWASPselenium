@model GenioMVC.ViewModels.Lnhpd.Lnhpd_ValDesagreg_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
<div class="row">
	<div class="col">
		<div elem-identifier="List"  help-id="IFF_LNHPD___PSEUDDESAGREG" style="padding-top: 0.5rem;"><label class = "i-text__label table-title c-table__title" > <h2>@Resources.BREAKDOWN_60448</h2> </label></div>
	</div>
</div>

@* USE /[MANUAL GQT TABLE_FOR LNHPD_DESAGREG]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Lnhde;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Lnhde>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodlnhde, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN LNHPD_DESAGREG_LNHDE_ORDEM]/
		column.Expression(p => p.ValOrdem).Title(@Resources.ORDER39632).Field("ValOrdem").Size(3);
// USE /[MANUAL GQT CUSTOM_COLUMN LNHPD_DESAGREG_TPEQ1_TIPOEQUI]/
		column.Expression(p => p.Tpeq1.ValTipoequi).Title(@Resources.TYPE_OF_EQUIPMENT18080).Field("Tpeq1.ValTipoequi").Size(50);
// USE /[MANUAL GQT CUSTOM_COLUMN LNHPD_DESAGREG_LNHDE_QUANTIDA]/
		column.Expression(p => p.ValQuantida).Title(@Resources.AMOUNT46885).Field("ValQuantida").Size(3);
// USE /[MANUAL GQT CUSTOM_COLUMN LNHPD_DESAGREG_LNHDE_DESCRIPT]/
		column.Expression(p => p.ValDescript).Title(@Resources.DESCRIPTION07383).Field("ValDescript").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN LNHPD_DESAGREG_LNHDE_CODE]/
		column.Expression(p => p.ValCode).Title(@Resources.CODE49225).Field("ValCode").Size(10);
// USE /[MANUAL GQT CUSTOM_COLUMN LNHPD_DESAGREG_LNHDE_URL]/
		column.Expression(p => p.ValUrl).Title(@Resources.SITE06486).Field("ValUrl").Size(30);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Lnhde")
        .Id("ValDesagreg")
        .AjaxRequest("Lnhpd_ValDesagreg")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: false)
        .RequestLink(@Url.Action("Lnhpd_ValDesagreg", new { id = Model.ValCodlnhpd, partialView = "Lnhpd_ValDesagreg"}))
        .Form("Lnhde", false, false)
        .SetFollowUp("Lnhde_Show", "Lnhde" , (Func<GenioMVC.Models.Lnhde, object>)(item => new { id = item.ValCodlnhde }), isRoutine: false, isSpecificPaths: false, isAjaxAction: false, htmlAttributes: new { }, openInPopup: false)
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@tbl.ToHtml()

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qValDesagreg, #ValDesagreg_complex_filter th input, #ValDesagreg_filters input").change(function() {
                    $("#pValDesagreg").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#ValDesagreg_tableFilters').val(false);")
                    @Html.Raw("$('#ValDesagreg_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#ValDesagreg_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $('#ValDesagreg_complex_filter button#applyComplexFilter').click();
                    @:      event.preventDefault();
                    @:  }
                    @:});
                }
                @Html.Raw("$('#ValDesagreg_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_ValDesagreg').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });


                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValDesagreg']"));
                    InitSpecialControls($('#ValDesagreg'));
                    $('#ValDesagreg').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS LNHPD_VALDESAGREG]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>