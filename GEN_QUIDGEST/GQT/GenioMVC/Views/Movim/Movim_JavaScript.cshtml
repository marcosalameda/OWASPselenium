@model GenioMVC.ViewModels.Movim.Movim_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS MOVIM]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Movim !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Movim = new QForm($('[data-form="Movim"]'), "Form_Movim");
		}
		window.Form_Movim.baseArea = "movim";
		window.Form_Movim.FormMode = '@Model.GetFormMode';
		window.Form_Movim.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Movim.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Movim")',
			GetDependants:		'@Url.Action("GetDependants", "Movim")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Movim", "Movim")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Movim", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Movim", "Movim")',
		};
		window.Form_Movim.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Movim.formSaveEdit = '@Url.Action("Movim_SaveEdit", "Movim")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Movim.formAction = 'Movim_New';
		window.Form_Movim.formRedirectURL = '@Url.Action("Movim_New_Redirect", "Movim")';
		window.Form_Movim.formSubmitURL = '@Url.Action("Movim_New", "Movim")';
		window.Form_Movim.formRedirectURL_repeatInsertion = '@Url.Action("Movim_New_Redirect", "Movim", new { repeatInsertion = true})';
		window.Form_Movim.formSubmitURL_repeatInsertion = '@Url.Action("Movim_New", "Movim", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Movim.formAction = 'Movim_Edit';
		window.Form_Movim.formRedirectURL = '@Url.Action("Movim_Edit_Redirect", "Movim")';
		window.Form_Movim.formSubmitURL = '@Url.Action("Movim_Edit", "Movim")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Movim.formAction = 'Movim_Duplicate';
		window.Form_Movim.formRedirectURL = '@Url.Action("Movim_Duplicate_Redirect", "Movim")';
		window.Form_Movim.formSubmitURL = '@Url.Action("Movim_Duplicate", "Movim")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Movim.formAction = 'Movim_Delete';
		window.Form_Movim.formRedirectURL = '@Url.Action("Movim_Delete_Redirect", "Movim")';
		window.Form_Movim.formSubmitURL = '@Url.Action("Movim_Delete", "Movim")';
	</text>
}
		window.Form_Movim.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_MOVIM___MOVIMDHMUDANC = new QDateControl($('[data-identifier="LED_MOVIM___MOVIMDHMUDANC"]', this.element), window.Form_Movim);

			this.Controls.IFF_MOVIM___EQUIPREGISTNR = new QDbeditControl($('[data-identifier="IFF_MOVIM___EQUIPREGISTNR"]', this.element), window.Form_Movim);

			this.Controls.IFF_MOVIM___ROOMSROOMNR__ = new QDbeditControl($('[data-identifier="IFF_MOVIM___ROOMSROOMNR__"]', this.element), window.Form_Movim);

			this.Controls.LED_MOVIM___MOVIMOBSERVAT = new QGenericControl($('[data-identifier="LED_MOVIM___MOVIMOBSERVAT"]', this.element), window.Form_Movim);




			this.Controls.LED_MOVIM___MOVIMCODMOVIM = new QHiddenControl($('[data-identifier="LED_MOVIM___MOVIMCODMOVIM"]', this.element), window.Form_Movim);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Movim.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Movim.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI MOVIM]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Movim.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS MOVIM]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Movim.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS MOVIM]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Movim.Data.RelationKeysSelector = {
				get equip() { return $(':not([elem-identifier="Multiform"]) > #ValCodequip[pers-cs-area="Equip"]', $('[data-form="Movim"]')); /* DBEdit */ },
				get rooms() { return $(':not([elem-identifier="Multiform"]) > #ValCodrooms[pers-cs-area="Rooms"]', $('[data-form="Movim"]')); /* DBEdit */ },
		};

		window.Form_Movim.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Movim.Data.RelationKeys = {
			get formContext() { return window.Form_Movim; },
			get equip() { return (this.formContext.Data.RelationKeysSelector.equip.getQControl() || {}).Value; /* DBEdit */ },
			set equip(newValue) { (this.formContext.Data.RelationKeysSelector.equip.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get rooms() { return (this.formContext.Data.RelationKeysSelector.rooms.getQControl() || {}).Value; /* DBEdit */ },
			set rooms(newValue) { (this.formContext.Data.RelationKeysSelector.rooms.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Movim.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Movim.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitMovim(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Movim.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteMovim(event) {
			event.preventDefault();
			window.Form_Movim.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS MOVIM]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Movim"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
