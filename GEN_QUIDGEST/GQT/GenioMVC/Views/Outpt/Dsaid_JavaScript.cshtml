@model GenioMVC.ViewModels.Outpt.Dsaid_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS DSAID]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Dsaid !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Dsaid = new QForm($('[data-form="Dsaid"]'), "Form_Dsaid");
		}
		window.Form_Dsaid.baseArea = "outpt";
		window.Form_Dsaid.FormMode = '@Model.GetFormMode';
		window.Form_Dsaid.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Dsaid.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Outpt")',
			GetDependants:		'@Url.Action("GetDependants", "Outpt")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Dsaid", "Outpt")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Dsaid", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Outpt", "Outpt")',
		};
		window.Form_Dsaid.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
			ApplyButton: true,
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Dsaid.formSaveEdit = '@Url.Action("Dsaid_SaveEdit", "Outpt")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Dsaid.formAction = 'Dsaid_New';
		window.Form_Dsaid.formRedirectURL = '@Url.Action("Dsaid_New_Redirect", "Outpt")';
		window.Form_Dsaid.formSubmitURL = '@Url.Action("Dsaid_New", "Outpt")';
		window.Form_Dsaid.formRedirectURL_repeatInsertion = '@Url.Action("Dsaid_New_Redirect", "Outpt", new { repeatInsertion = true})';
		window.Form_Dsaid.formSubmitURL_repeatInsertion = '@Url.Action("Dsaid_New", "Outpt", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Dsaid.formAction = 'Dsaid_Edit';
		window.Form_Dsaid.formRedirectURL = '@Url.Action("Dsaid_Edit_Redirect", "Outpt")';
		window.Form_Dsaid.formSubmitURL = '@Url.Action("Dsaid_Edit", "Outpt")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Dsaid.formAction = 'Dsaid_Duplicate';
		window.Form_Dsaid.formRedirectURL = '@Url.Action("Dsaid_Duplicate_Redirect", "Outpt")';
		window.Form_Dsaid.formSubmitURL = '@Url.Action("Dsaid_Duplicate", "Outpt")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Dsaid.formAction = 'Dsaid_Delete';
		window.Form_Dsaid.formRedirectURL = '@Url.Action("Dsaid_Delete_Redirect", "Outpt")';
		window.Form_Dsaid.formSubmitURL = '@Url.Action("Dsaid_Delete", "Outpt")';
	</text>
}
		window.Form_Dsaid.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_DSAID___WARE1WAREHDES = new QDbeditControl($('[data-identifier="IFF_DSAID___WARE1WAREHDES"]', this.element), window.Form_Dsaid);

			this.Controls.LED_DSAID___OUTPTDOCUMENR = new QNumericControl($('[data-identifier="LED_DSAID___OUTPTDOCUMENR"]', this.element), window.Form_Dsaid);

			this.Controls.LED_DSAID___OUTPTDHDOCUME = new QDateControl($('[data-identifier="LED_DSAID___OUTPTDHDOCUME"]', this.element), window.Form_Dsaid);

			this.Controls.IFF_DSAID___PSEUDSAIDAS__ = new QTableListControl($('[data-identifier="IFF_DSAID___PSEUDSAIDAS__"]', this.element), window.Form_Dsaid);

			this.Controls.IFF_DSAID___PSEUDSAIDA___ = new QButtonControl($('[data-identifier="IFF_DSAID___PSEUDSAIDA___"]', this.element), window.Form_Dsaid);




			this.Controls.LED_DSAID___OUTPTCODOUTPT = new QHiddenControl($('[data-identifier="LED_DSAID___OUTPTCODOUTPT"]', this.element), window.Form_Dsaid);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Dsaid.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Dsaid.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI DSAID]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
@if (Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
					if (rData.mode === "NEW" || rData.mode === "EDIT" || rData.mode === "DUPLICATE") {
						$.when(window.Form_Dsaid.Apply()).then(function (issaved) { rData.deferred.resolve(issaved); });
					}
					else
	</text>
}
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Dsaid.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS DSAID]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Dsaid.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS DSAID]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Dsaid.Data.RelationKeysSelector = {
				get ware1() { return $(':not([elem-identifier="Multiform"]) > #ValCodwareh[pers-cs-area="Ware1"]', $('[data-form="Dsaid"]')); /* DBEdit */ },
		};

		window.Form_Dsaid.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Dsaid.Data.RelationKeys = {
			get formContext() { return window.Form_Dsaid; },
			get ware1() { return (this.formContext.Data.RelationKeysSelector.ware1.getQControl() || {}).Value; /* DBEdit */ },
			set ware1(newValue) { (this.formContext.Data.RelationKeysSelector.ware1.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Dsaid.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Dsaid.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitDsaid(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Dsaid.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteDsaid(event) {
			event.preventDefault();
			window.Form_Dsaid.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS DSAID]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Dsaid"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
