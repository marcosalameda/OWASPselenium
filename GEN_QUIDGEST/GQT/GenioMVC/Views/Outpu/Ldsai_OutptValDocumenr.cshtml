@model GenioMVC.ViewModels.Outpu.Ldsai_OutptValDocumenr_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@{
    var searchStyle =  string.Empty;
}
<div class="search" @searchStyle>
    <div class="row">
        <div class="col">
            <h2 class="table-title c-table__title">@Resources.OUTPUT_DOCUMENTS44871</h2>
        </div>
        <div class="col-auto normal-view" id="Ldsai_OutptValDocumenr_simple_filter">
            <div class="i-input-group" style="width:21rem; height: inherit;">
                @Html.TextBox("qLdsai_OutptValDocumenr", this.Model.Menu.Query, new { @class = "i-input-group__field", @onkeydown = "ListSearchEnter(event)" })
                <div class="i-input-group--right">
                    <button type="button" elem-identifier="ListSearchButton" class="b-icon--secondary i-input-group__button--secondary" onclick="window.Ldsai_OutptValDocumenr.Search()"><i class="glyphicons glyphicons-search i-input-group__tag-icon i-input-group__button-icon"></i></button>

                    <button id="Ldsai_OutptValDocumenr_extra" class="b-icon--secondary i-input-group__button--secondary" type="button" onclick="changeFilters('#Ldsai_OutptValDocumenr_complex_filter', 'Ldsai_OutptValDocumenr_tableFilters')" title="@Resources.PESQUISA_AVANCADA06198"><span class="glyphicons glyphicons-zoom-in i-input-group__tag-icon i-input-group__button-icon"></span></button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="normal-view">
@{
    var data = Html.newTableFor<GenioMVC.Models.Outpt>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodoutpt, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN LDSAI_DOCUMENR_OUTPT_DOCUMENR]/
		column.Expression(p => p.ValDocumenr).Title(@Resources.NO_14817).Field("ValDocumenr").Size(10);
// USE /[MANUAL GQT CUSTOM_COLUMN LDSAI_DOCUMENR_OUTPT_DHDOCUME]/
		column.Expression(p => p.ValDhdocume).Title(@Resources.DATE18475).Field("ValDhdocume").Size(16);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
			//Hidden field used in JavaScript for return selected row text value
			column.Expression(p => p.ValDocumenr).Field("ValDocumenr").AddInlineStyle("display", "none", true).UsedForFilter(false);
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Outpt")
        .Id("Ldsai_OutptValDocumenr")
        .AjaxRequest("Ldsai_OutptValDocumenr")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: true)
        .RequestLink(@Url.Action("Ldsai_OutptValDocumenr", new { id = Model.ValCodoutpu, partialView = "Ldsai_OutptValDocumenr"}))
        .Filters(this.Model.Menu.Filters)
        .Permissions(canView: false, canInsert: false, canEdit: false, canDelete: false, canDuplicate: false)
		;
        
}

@tbl.ToHtml()
</div>
<script>
    try {
        if (jQuery) {
            try {
                $("#qLdsai_OutptValDocumenr, #TableOutptDocumenr_complex_filter th input").change(function() {
                    var context = $("#TableOutptDocumenr").parent();
                    $("#table-inputs input", context).val("");
                });

                var row = $("#Ldsai_OutptValDocumenr table tbody tr");
                row.mouseenter(function () {
                    $(this).addClass("highlighted");
                });
                row.mouseleave(function () {
                    $(this).removeClass("highlighted");
                });

                $('td:not([elem-identifier="RowActions"])', row).click(function () {
                    var row = $(this).parent();
                    var key = row.data("key");
                    var selectList = $('[data-identifier="IFF_LDSAI___OUTPTDOCUMENR"]');

                    selectList.trigger("DBEditSetValue", key);

                    $('#modal-dbedit').modal('hide');
                });

                $("td a").click(function (event) {
                    event.stopPropagation()
                });
                $('#Ldsai_OutptValDocumenr_complex_filter input').keypress(function (event) {
                    var keycode = (event.keyCode ? event.keyCode : event.which);
                    if(keycode == '13' || keycode == '10'){
                        $('#Ldsai_OutptValDocumenr_complex_filter button#applyComplexFilter').click();
                        event.preventDefault();
                    }
                });
@if(Model.Menu.Elements.Count() == 0 && !Model.Menu.Filters.ShowTableFilters)
{
<text>
                $('#Ldsai_OutptValDocumenr_extra').prop("disabled", true);
</text>
}


// USE /[MANUAL GQT LISTING_JS LDSAI_MENU]/
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
</script>