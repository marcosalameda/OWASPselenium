@model GenioMVC.ViewModels.Pedid.Pedid_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS PEDID]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Pedid !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Pedid = new QForm($('[data-form="Pedid"]'), "Form_Pedid");
		}
		window.Form_Pedid.baseArea = "pedid";
		window.Form_Pedid.FormMode = '@Model.GetFormMode';
		window.Form_Pedid.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Pedid.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Pedid")',
			GetDependants:		'@Url.Action("GetDependants", "Pedid")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Pedid", "Pedid")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Pedid", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Pedid", "Pedid")',
		};
		window.Form_Pedid.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pedid.formSaveEdit = '@Url.Action("Pedid_SaveEdit", "Pedid")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Pedid.formAction = 'Pedid_New';
		window.Form_Pedid.formRedirectURL = '@Url.Action("Pedid_New_Redirect", "Pedid")';
		window.Form_Pedid.formSubmitURL = '@Url.Action("Pedid_New", "Pedid")';
		window.Form_Pedid.formRedirectURL_repeatInsertion = '@Url.Action("Pedid_New_Redirect", "Pedid", new { repeatInsertion = true})';
		window.Form_Pedid.formSubmitURL_repeatInsertion = '@Url.Action("Pedid_New", "Pedid", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Pedid.formAction = 'Pedid_Edit';
		window.Form_Pedid.formRedirectURL = '@Url.Action("Pedid_Edit_Redirect", "Pedid")';
		window.Form_Pedid.formSubmitURL = '@Url.Action("Pedid_Edit", "Pedid")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pedid.formAction = 'Pedid_Duplicate';
		window.Form_Pedid.formRedirectURL = '@Url.Action("Pedid_Duplicate_Redirect", "Pedid")';
		window.Form_Pedid.formSubmitURL = '@Url.Action("Pedid_Duplicate", "Pedid")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Pedid.formAction = 'Pedid_Delete';
		window.Form_Pedid.formRedirectURL = '@Url.Action("Pedid_Delete_Redirect", "Pedid")';
		window.Form_Pedid.formSubmitURL = '@Url.Action("Pedid_Delete", "Pedid")';
	</text>
}
		window.Form_Pedid.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_PEDID___PEDIDDTPEDIDO = new QDateControl($('[data-identifier="LED_PEDID___PEDIDDTPEDIDO"]', this.element), window.Form_Pedid);

			this.Controls.LED_PEDID___PEDIDNRPEDIDO = new QNumericControl($('[data-identifier="LED_PEDID___PEDIDNRPEDIDO"]', this.element), window.Form_Pedid);

			this.Controls.LED_PEDID___PEDIDMOTIVO__ = new QGenericControl($('[data-identifier="LED_PEDID___PEDIDMOTIVO__"]', this.element), window.Form_Pedid);

			this.Controls.IFF_PEDID___PSEUDLINHAS__ = new QTableListControl($('[data-identifier="IFF_PEDID___PSEUDLINHAS__"]', this.element), window.Form_Pedid);

			this.Controls.IFF_PEDID___PSEUDDESAGREG = new QTableListControl($('[data-identifier="IFF_PEDID___PSEUDDESAGREG"]', this.element), window.Form_Pedid);

			this.Controls.IFF_PEDID___PSEUDAGRUPAME = new QTableListControl($('[data-identifier="IFF_PEDID___PSEUDAGRUPAME"]', this.element), window.Form_Pedid);




			this.Controls.LED_PEDID___PEDIDCODPEDID = new QHiddenControl($('[data-identifier="LED_PEDID___PEDIDCODPEDID"]', this.element), window.Form_Pedid);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Pedid.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pedid.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI PEDID]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Pedid.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS PEDID]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Pedid.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS PEDID]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Pedid.Data.RelationKeysSelector = {
		};

		window.Form_Pedid.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Pedid.Data.RelationKeys = {
			get formContext() { return window.Form_Pedid; },
		};
	</text>
}

	window.Form_Pedid.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Pedid.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitPedid(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Pedid.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeletePedid(event) {
			event.preventDefault();
			window.Form_Pedid.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS PEDID]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Pedid"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
