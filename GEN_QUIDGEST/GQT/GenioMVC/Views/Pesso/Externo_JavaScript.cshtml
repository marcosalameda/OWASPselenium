@model GenioMVC.ViewModels.Pesso.Externo_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS EXTERNO]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Externo !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Externo = new QForm($('[data-form="Externo"]'), "Form_Externo");
		}
		window.Form_Externo.baseArea = "pesso";
		window.Form_Externo.FormMode = '@Model.GetFormMode';
		window.Form_Externo.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Externo.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Pesso")',
			GetDependants:		'@Url.Action("GetDependants", "Pesso")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Externo", "Pesso")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Externo", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Pesso", "Pesso")',
		};
		window.Form_Externo.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Externo.formSaveEdit = '@Url.Action("Externo_SaveEdit", "Pesso")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Externo.formAction = 'Externo_New';
		window.Form_Externo.formRedirectURL = '@Url.Action("Externo_New_Redirect", "Pesso")';
		window.Form_Externo.formSubmitURL = '@Url.Action("Externo_New", "Pesso")';
		window.Form_Externo.formRedirectURL_repeatInsertion = '@Url.Action("Externo_New_Redirect", "Pesso", new { repeatInsertion = true})';
		window.Form_Externo.formSubmitURL_repeatInsertion = '@Url.Action("Externo_New", "Pesso", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Externo.formAction = 'Externo_Edit';
		window.Form_Externo.formRedirectURL = '@Url.Action("Externo_Edit_Redirect", "Pesso")';
		window.Form_Externo.formSubmitURL = '@Url.Action("Externo_Edit", "Pesso")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Externo.formAction = 'Externo_Duplicate';
		window.Form_Externo.formRedirectURL = '@Url.Action("Externo_Duplicate_Redirect", "Pesso")';
		window.Form_Externo.formSubmitURL = '@Url.Action("Externo_Duplicate", "Pesso")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Externo.formAction = 'Externo_Delete';
		window.Form_Externo.formRedirectURL = '@Url.Action("Externo_Delete_Redirect", "Pesso")';
		window.Form_Externo.formSubmitURL = '@Url.Action("Externo_Delete", "Pesso")';
	</text>
}
		window.Form_Externo.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_EXTERNO_PSEUDNOVOGR01 = new QGenericControl($('[data-identifier="IFF_EXTERNO_PSEUDNOVOGR01"]', this.element), window.Form_Externo);

			this.Controls.IFF_EXTERNO_CMPNYDESIGNAT = new QDbeditControl($('[data-identifier="IFF_EXTERNO_CMPNYDESIGNAT"]', this.element), window.Form_Externo);

			this.Controls.IFF_EXTERNO_PSEUDNOVOGR02 = new QGenericControl($('[data-identifier="IFF_EXTERNO_PSEUDNOVOGR02"]', this.element), window.Form_Externo);

			this.Controls.LED_EXTERNO_PESSONAME____ = new QTextControl($('[data-identifier="LED_EXTERNO_PESSONAME____"]', this.element), window.Form_Externo);

			this.Controls.LED_EXTERNO_PESSOGENDER__ = new QArrayControl($('[data-identifier="LED_EXTERNO_PESSOGENDER__"]', this.element), window.Form_Externo);

			this.Controls.IFF_EXTERNO_PSEUDNOVOGR06 = new QGenericControl($('[data-identifier="IFF_EXTERNO_PSEUDNOVOGR06"]', this.element), window.Form_Externo);

			this.Controls.IFF_EXTERNO_PSEUDNOVOGR03 = new QGenericControl($('[data-identifier="IFF_EXTERNO_PSEUDNOVOGR03"]', this.element), window.Form_Externo);

			this.Controls.LED_EXTERNO_PESSOTELEPHON = new QTextControl($('[data-identifier="LED_EXTERNO_PESSOTELEPHON"]', this.element), window.Form_Externo);

			this.Controls.LED_EXTERNO_PESSOEMAIL___ = new QTextControl($('[data-identifier="LED_EXTERNO_PESSOEMAIL___"]', this.element), window.Form_Externo);

			this.Controls.IFF_EXTERNO_PSEUDNOVOGR04 = new QGenericControl($('[data-identifier="IFF_EXTERNO_PSEUDNOVOGR04"]', this.element), window.Form_Externo);

			this.Controls.LED_EXTERNO_PESSOPHOTOGRA = new QImageControl($('[data-identifier="LED_EXTERNO_PESSOPHOTOGRA"]', this.element), window.Form_Externo, '@Html.Raw(Url.Action("ImageDelete", "Pesso", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Pesso", "ValPhotogra", "", Model.ValCodpesso, null) }))', '@Html.Raw(Url.Action("ImageCropper", "Pesso", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Pesso", "ValPhotogra", "", Model.ValCodpesso, null), identifier= "FEXTERNO", FormName= "Externo", FieldId= "LED_EXTERNO_PESSOPHOTOGRA" }))',@Html.IsImageEmpty(Model.ValPhotogra));

			this.Controls.IFF_EXTERNO_PSEUDOBRIGATO = new QTextControl($('[data-identifier="IFF_EXTERNO_PSEUDOBRIGATO"]', this.element), window.Form_Externo);




@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	@*Hidden Foreign Keys Controls*@
			this.Controls.LED_EXTERNO_REGI1CODREGIA = new QHiddenControl($('[data-identifier="LED_EXTERNO_REGI1CODREGIA"]', this.element), window.Form_Externo);
			this.Controls.LED_EXTERNO_PAIS1CODCNTRY = new QHiddenControl($('[data-identifier="LED_EXTERNO_PAIS1CODCNTRY"]', this.element), window.Form_Externo);
			this.Controls.LED_EXTERNO_CATEGCODCATEG = new QHiddenControl($('[data-identifier="LED_EXTERNO_CATEGCODCATEG"]', this.element), window.Form_Externo);
			this.Controls.LED_EXTERNO_CNTRYCODCNTRY = new QHiddenControl($('[data-identifier="LED_EXTERNO_CNTRYCODCNTRY"]', this.element), window.Form_Externo);
	</text>
}
	@*Hidden Required Foreign Keys for Formulas*@
			this.Controls.LED_EXTERNO_CMPNYCODEMPRE = new QHiddenControl($('[data-identifier="LED_EXTERNO_CMPNYCODEMPRE"]', this.element), window.Form_Externo);
			this.Controls.LED_EXTERNO_PESSOCODPESSO = new QHiddenControl($('[data-identifier="LED_EXTERNO_PESSOCODPESSO"]', this.element), window.Form_Externo);

	@* Hidden Required Form Fields Controls for Formulas *@
			this.Controls.LED_EXTERNO_PESSOEMAIL2__ = new QSpecialControl($('[data-identifier="LED_EXTERNO_PESSOEMAIL2__"]', this.element), window.Form_Externo);
			this.Controls.LED_EXTERNO_PESSOEMAIL2__.InitData(@JavaScriptConversion.ToString(@Model.ValEmail2));

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Externo.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Externo.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI EXTERNO]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Externo.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS EXTERNO]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Externo.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS EXTERNO]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Externo.Data.RelationKeysSelector = {
				get regi1() { return $(':not([elem-identifier="Multiform"]) > #ValCodregia', $('[data-form="Externo"]')); /* Hidden */ },
				get pais1() { return $(':not([elem-identifier="Multiform"]) > #ValCodcntry', $('[data-form="Externo"]')); /* Hidden */ },
				get categ() { return $(':not([elem-identifier="Multiform"]) > #ValCodcateg', $('[data-form="Externo"]')); /* Hidden */ },
				get cntry() { return $(':not([elem-identifier="Multiform"]) > #ValCodpaise', $('[data-form="Externo"]')); /* Hidden */ },
				get cmpny() { return $(':not([elem-identifier="Multiform"]) > #ValCodempre[pers-cs-area="Cmpny"]', $('[data-form="Externo"]')); /* DBEdit */ },
		};

		window.Form_Externo.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Externo.Data.RelationKeys = {
			get formContext() { return window.Form_Externo; },
			get regi1() { return (this.formContext.Data.RelationKeysSelector.regi1.getQControl() || {}).Value; /* Hidden FK */ },
			set regi1(newValue) { (this.formContext.Data.RelationKeysSelector.regi1.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get pais1() { return (this.formContext.Data.RelationKeysSelector.pais1.getQControl() || {}).Value; /* Hidden FK */ },
			set pais1(newValue) { (this.formContext.Data.RelationKeysSelector.pais1.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get categ() { return (this.formContext.Data.RelationKeysSelector.categ.getQControl() || {}).Value; /* Hidden FK */ },
			set categ(newValue) { (this.formContext.Data.RelationKeysSelector.categ.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get cntry() { return (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value; /* Hidden FK */ },
			set cntry(newValue) { (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get cmpny() { return (this.formContext.Data.RelationKeysSelector.cmpny.getQControl() || {}).Value; /* DBEdit */ },
			set cmpny(newValue) { (this.formContext.Data.RelationKeysSelector.cmpny.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Externo.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Externo.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitExterno(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Externo.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteExterno(event) {
			event.preventDefault();
			window.Form_Externo.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS EXTERNO]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Externo"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
