@model GenioMVC.ViewModels.Pesso.Pesso1_CmpnyValDesignat_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@{
    var searchStyle =  string.Empty;
}
<div class="search" @searchStyle>
    <div class="row">
        <div class="col">
            <h2 class="table-title c-table__title">@Resources.COMPANIES04875</h2>
        </div>
        <div class="col-auto normal-view" id="Pesso1_CmpnyValDesignat_simple_filter">
            <div class="i-input-group" style="width:21rem; height: inherit;">
                @Html.TextBox("qPesso1_CmpnyValDesignat", this.Model.Menu.Query, new { @class = "i-input-group__field", @onkeydown = "ListSearchEnter(event)" })
                <div class="i-input-group--right">
                    <button type="button" elem-identifier="ListSearchButton" class="b-icon--secondary i-input-group__button--secondary" onclick="window.Pesso1_CmpnyValDesignat.Search()"><i class="glyphicons glyphicons-search i-input-group__tag-icon i-input-group__button-icon"></i></button>

                    <button id="Pesso1_CmpnyValDesignat_extra" class="b-icon--secondary i-input-group__button--secondary" type="button" onclick="changeFilters('#Pesso1_CmpnyValDesignat_complex_filter', 'Pesso1_CmpnyValDesignat_tableFilters')" title="@Resources.PESQUISA_AVANCADA06198"><span class="glyphicons glyphicons-zoom-in i-input-group__tag-icon i-input-group__button-icon"></span></button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="normal-view">
@using GenioMVC.ViewModels.Cmpny;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Cmpny>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodempre, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN PESSO1_DESIGNAT_CMPNY_DESIGNAT]/
		column.Expression(p => p.ValDesignat).Title(@Resources.DESIGNATION35876).Field("ValDesignat").Size(85);
// USE /[MANUAL GQT CUSTOM_COLUMN PESSO1_DESIGNAT_CMPNY_ACRONYM]/
		column.Expression(p => p.ValAcronym).Title(@Resources.ACRONYM00872).Field("ValAcronym").Size(15);
// USE /[MANUAL GQT CUSTOM_COLUMN PESSO1_DESIGNAT_CMPNY_NIF]/
		column.Expression(p => p.ValNif).Title(@Resources.TAX_IDENTIFICATION51190).Field("ValNif").Size(15);
// USE /[MANUAL GQT CUSTOM_COLUMN PESSO1_DESIGNAT_CMPNY_TELEPHON]/
		column.Expression(p => p.ValTelephon).Title(@Resources.PHONE56703).Field("ValTelephon").Size(20);
// USE /[MANUAL GQT CUSTOM_COLUMN PESSO1_DESIGNAT_CMPNY_EMAIL]/
		column.Expression(p => p.ValEmail).Title(@Resources.EMAIL25170).Field("ValEmail").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN PESSO1_DESIGNAT_CMPNY_LOGO]/
		column.Expression(p => Html.ImageMagnifierZoom(p.ValLogo, new {alt=@Resources.LOGO62483,}, Url.Action("ImageHandlerGet", "Home", new { ticket = Helpers.GetFileTicket(UserContext.Current.User, "Cmpny", "ValLogo", "", p.ValCodempre, null) }))).Title(@Resources.LOGO62483).Field("ValLogo").Size(3);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
			//Hidden field used in JavaScript for return selected row text value
			column.Expression(p => p.ValDesignat).Field("ValDesignat").AddInlineStyle("display", "none", true).UsedForFilter(false);
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Cmpny")
        .Id("Pesso1_CmpnyValDesignat")
        .AjaxRequest("Pesso1_CmpnyValDesignat")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: true)
        .RequestLink(@Url.Action("Pesso1_CmpnyValDesignat", new { id = Model.ValCodpesso, partialView = "Pesso1_CmpnyValDesignat"}))
        .Filters(this.Model.Menu.Filters)
        .Form("Empre", true, false, new { data_skip_prevalida = "true" })
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@tbl.ToHtml()
</div>
<script>
    try {
        if (jQuery) {
            try {
                $("#qPesso1_CmpnyValDesignat, #TableCmpnyDesignat_complex_filter th input").change(function() {
                    var context = $("#TableCmpnyDesignat").parent();
                    $("#table-inputs input", context).val("");
                });

                var row = $("#Pesso1_CmpnyValDesignat table tbody tr");
                row.mouseenter(function () {
                    $(this).addClass("highlighted");
                });
                row.mouseleave(function () {
                    $(this).removeClass("highlighted");
                });

                $('td:not([elem-identifier="RowActions"])', row).click(function () {
                    var row = $(this).parent();
                    var key = row.data("key");
                    var selectList = $('[data-identifier="IFF_PESSO1__CMPNYDESIGNAT"]');

                    selectList.trigger("DBEditSetValue", key);

                    $('#modal-dbedit').modal('hide');
                });

                $("td a").click(function (event) {
                    event.stopPropagation()
                });
                $('#Pesso1_CmpnyValDesignat_complex_filter input').keypress(function (event) {
                    var keycode = (event.keyCode ? event.keyCode : event.which);
                    if(keycode == '13' || keycode == '10'){
                        $('#Pesso1_CmpnyValDesignat_complex_filter button#applyComplexFilter').click();
                        event.preventDefault();
                    }
                });
@if(Model.Menu.Elements.Count() == 0 && !Model.Menu.Filters.ShowTableFilters)
{
<text>
                $('#Pesso1_CmpnyValDesignat_extra').prop("disabled", true);
</text>
}


// USE /[MANUAL GQT LISTING_JS PESSO1_MENU]/
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
</script>
