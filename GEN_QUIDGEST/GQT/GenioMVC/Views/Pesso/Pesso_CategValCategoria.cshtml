@model GenioMVC.ViewModels.Pesso.Pesso_CategValCategoria_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@{
    var searchStyle =  string.Empty;
}
<div class="search" @searchStyle>
    <div class="row">
        <div class="col">
            <h2 class="table-title c-table__title">@Resources.PROFESSIONAL_CATEGOR49666</h2>
        </div>
        <div class="col-auto normal-view" id="Pesso_CategValCategoria_simple_filter">
            <div class="i-input-group" style="width:21rem; height: inherit;">
                @Html.TextBox("qPesso_CategValCategoria", this.Model.Menu.Query, new { @class = "i-input-group__field", @onkeydown = "ListSearchEnter(event)" })
                <div class="i-input-group--right">
                    <button type="button" elem-identifier="ListSearchButton" class="b-icon--secondary i-input-group__button--secondary" onclick="window.Pesso_CategValCategoria.Search()"><i class="glyphicons glyphicons-search i-input-group__tag-icon i-input-group__button-icon"></i></button>

                    <button id="Pesso_CategValCategoria_extra" class="b-icon--secondary i-input-group__button--secondary" type="button" onclick="changeFilters('#Pesso_CategValCategoria_complex_filter', 'Pesso_CategValCategoria_tableFilters')" title="@Resources.PESQUISA_AVANCADA06198"><span class="glyphicons glyphicons-zoom-in i-input-group__tag-icon i-input-group__button-icon"></span></button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="normal-view">
@using GenioMVC.ViewModels.Categ;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Categ>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodcateg, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN PESSO_CATEGORY_CATEG_CATEGORY]/
		column.Expression(p => p.ValCategoria).Title(@Resources.CATEGORY18978).Field("ValCategoria").Size(50);
// USE /[MANUAL GQT CUSTOM_COLUMN PESSO_CATEGORY_CATEG_ABBREVIA]/
		column.Expression(p => p.ValAbbreviation).Title(@Resources.ABBREVIATION31267).Field("ValAbbreviation").Size(10);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
			//Hidden field used in JavaScript for return selected row text value
			column.Expression(p => p.ValCategoria).Field("ValCategoria").AddInlineStyle("display", "none", true).UsedForFilter(false);
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Categ")
        .Id("Pesso_CategValCategoria")
        .AjaxRequest("Pesso_CategValCategoria")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: true)
        .RequestLink(@Url.Action("Pesso_CategValCategoria", new { id = Model.ValCodpesso, partialView = "Pesso_CategValCategoria"}))
        .Filters(this.Model.Menu.Filters)
        .Form("Categ", false, false, new { data_skip_prevalida = "true" })
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@tbl.ToHtml()
</div>
<script>
    try {
        if (jQuery) {
            try {
                $("#qPesso_CategValCategoria, #TableCategCategory_complex_filter th input").change(function() {
                    var context = $("#TableCategCategory").parent();
                    $("#table-inputs input", context).val("");
                });

                var row = $("#Pesso_CategValCategoria table tbody tr");
                row.mouseenter(function () {
                    $(this).addClass("highlighted");
                });
                row.mouseleave(function () {
                    $(this).removeClass("highlighted");
                });

                $('td:not([elem-identifier="RowActions"])', row).click(function () {
                    var row = $(this).parent();
                    var key = row.data("key");
                    var selectList = $('[data-identifier="IFF_PESSO___CATEGCATEGORY"]');

                    selectList.trigger("DBEditSetValue", key);

                    $('#modal-dbedit').modal('hide');
                });

                $("td a").click(function (event) {
                    event.stopPropagation()
                });
                $('#Pesso_CategValCategoria_complex_filter input').keypress(function (event) {
                    var keycode = (event.keyCode ? event.keyCode : event.which);
                    if(keycode == '13' || keycode == '10'){
                        $('#Pesso_CategValCategoria_complex_filter button#applyComplexFilter').click();
                        event.preventDefault();
                    }
                });
@if(Model.Menu.Elements.Count() == 0 && !Model.Menu.Filters.ShowTableFilters)
{
<text>
                $('#Pesso_CategValCategoria_extra').prop("disabled", true);
</text>
}


// USE /[MANUAL GQT LISTING_JS PESSO_MENU]/
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
</script>