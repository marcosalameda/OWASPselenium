@model GenioMVC.ViewModels.Pesso.Pessohis_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS PESSOHIS]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Pessohis !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Pessohis = new QForm($('[data-form="Pessohis"]'), "Form_Pessohis");
		}
		window.Form_Pessohis.baseArea = "pesso";
		window.Form_Pessohis.FormMode = '@Model.GetFormMode';
		window.Form_Pessohis.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Pessohis.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Pesso")',
			GetDependants:		'@Url.Action("GetDependants", "Pesso")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Pessohis", "Pesso")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Pessohis", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Pesso", "Pesso")',
		};
		window.Form_Pessohis.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pessohis.formSaveEdit = '@Url.Action("Pessohis_SaveEdit", "Pesso")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Pessohis.formAction = 'Pessohis_New';
		window.Form_Pessohis.formRedirectURL = '@Url.Action("Pessohis_New_Redirect", "Pesso")';
		window.Form_Pessohis.formSubmitURL = '@Url.Action("Pessohis_New", "Pesso")';
		window.Form_Pessohis.formRedirectURL_repeatInsertion = '@Url.Action("Pessohis_New_Redirect", "Pesso", new { repeatInsertion = true})';
		window.Form_Pessohis.formSubmitURL_repeatInsertion = '@Url.Action("Pessohis_New", "Pesso", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Pessohis.formAction = 'Pessohis_Edit';
		window.Form_Pessohis.formRedirectURL = '@Url.Action("Pessohis_Edit_Redirect", "Pesso")';
		window.Form_Pessohis.formSubmitURL = '@Url.Action("Pessohis_Edit", "Pesso")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pessohis.formAction = 'Pessohis_Duplicate';
		window.Form_Pessohis.formRedirectURL = '@Url.Action("Pessohis_Duplicate_Redirect", "Pesso")';
		window.Form_Pessohis.formSubmitURL = '@Url.Action("Pessohis_Duplicate", "Pesso")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Pessohis.formAction = 'Pessohis_Delete';
		window.Form_Pessohis.formRedirectURL = '@Url.Action("Pessohis_Delete_Redirect", "Pesso")';
		window.Form_Pessohis.formSubmitURL = '@Url.Action("Pessohis_Delete", "Pesso")';
	</text>
}
		window.Form_Pessohis.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_PESSOHISPSEUDFIELD002 = new QTextControl($('[data-identifier="IFF_PESSOHISPSEUDFIELD002"]', this.element), window.Form_Pessohis);

			this.Controls.LED_PESSOHISPESSOIDFUNCIO = new QNumericControl($('[data-identifier="LED_PESSOHISPESSOIDFUNCIO"]', this.element), window.Form_Pessohis);

			this.Controls.LED_PESSOHISPESSONAME____ = new QTextControl($('[data-identifier="LED_PESSOHISPESSONAME____"]', this.element), window.Form_Pessohis);

			this.Controls.IFF_PESSOHISPSEUDFIELD001 = new QTableListControl($('[data-identifier="IFF_PESSOHISPSEUDFIELD001"]', this.element), window.Form_Pessohis);




@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	@*Hidden Foreign Keys Controls*@
			this.Controls.LED_PESSOHISCATEGCODCATEG = new QHiddenControl($('[data-identifier="LED_PESSOHISCATEGCODCATEG"]', this.element), window.Form_Pessohis);
			this.Controls.LED_PESSOHISPAIS1CODCNTRY = new QHiddenControl($('[data-identifier="LED_PESSOHISPAIS1CODCNTRY"]', this.element), window.Form_Pessohis);
			this.Controls.LED_PESSOHISCNTRYCODCNTRY = new QHiddenControl($('[data-identifier="LED_PESSOHISCNTRYCODCNTRY"]', this.element), window.Form_Pessohis);
			this.Controls.LED_PESSOHISCMPNYCODEMPRE = new QHiddenControl($('[data-identifier="LED_PESSOHISCMPNYCODEMPRE"]', this.element), window.Form_Pessohis);
			this.Controls.LED_PESSOHISREGI1CODREGIA = new QHiddenControl($('[data-identifier="LED_PESSOHISREGI1CODREGIA"]', this.element), window.Form_Pessohis);
	</text>
}
			this.Controls.LED_PESSOHISPESSOCODPESSO = new QHiddenControl($('[data-identifier="LED_PESSOHISPESSOCODPESSO"]', this.element), window.Form_Pessohis);

	@* Hidden Required Form Fields Controls for Formulas *@
			this.Controls.LED_PESSOHISPESSOEMAIL___ = new QSpecialControl($('[data-identifier="LED_PESSOHISPESSOEMAIL___"]', this.element), window.Form_Pessohis);
			this.Controls.LED_PESSOHISPESSOEMAIL___.InitData(@JavaScriptConversion.ToString(@Model.ValEmail));
			this.Controls.LED_PESSOHISPESSOEMAIL2__ = new QSpecialControl($('[data-identifier="LED_PESSOHISPESSOEMAIL2__"]', this.element), window.Form_Pessohis);
			this.Controls.LED_PESSOHISPESSOEMAIL2__.InitData(@JavaScriptConversion.ToString(@Model.ValEmail2));

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Pessohis.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Pessohis.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI PESSOHIS]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Pessohis.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS PESSOHIS]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Pessohis.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS PESSOHIS]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Pessohis.Data.RelationKeysSelector = {
				get categ() { return $(':not([elem-identifier="Multiform"]) > #ValCodcateg', $('[data-form="Pessohis"]')); /* Hidden */ },
				get pais1() { return $(':not([elem-identifier="Multiform"]) > #ValCodcntry', $('[data-form="Pessohis"]')); /* Hidden */ },
				get cntry() { return $(':not([elem-identifier="Multiform"]) > #ValCodpaise', $('[data-form="Pessohis"]')); /* Hidden */ },
				get cmpny() { return $(':not([elem-identifier="Multiform"]) > #ValCodempre', $('[data-form="Pessohis"]')); /* Hidden */ },
				get regi1() { return $(':not([elem-identifier="Multiform"]) > #ValCodregia', $('[data-form="Pessohis"]')); /* Hidden */ },
		};

		window.Form_Pessohis.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Pessohis.Data.RelationKeys = {
			get formContext() { return window.Form_Pessohis; },
			get categ() { return (this.formContext.Data.RelationKeysSelector.categ.getQControl() || {}).Value; /* Hidden FK */ },
			set categ(newValue) { (this.formContext.Data.RelationKeysSelector.categ.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get pais1() { return (this.formContext.Data.RelationKeysSelector.pais1.getQControl() || {}).Value; /* Hidden FK */ },
			set pais1(newValue) { (this.formContext.Data.RelationKeysSelector.pais1.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get cntry() { return (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value; /* Hidden FK */ },
			set cntry(newValue) { (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get cmpny() { return (this.formContext.Data.RelationKeysSelector.cmpny.getQControl() || {}).Value; /* Hidden FK */ },
			set cmpny(newValue) { (this.formContext.Data.RelationKeysSelector.cmpny.getQControl() || {}).Value = newValue; /* Hidden FK */ },
			get regi1() { return (this.formContext.Data.RelationKeysSelector.regi1.getQControl() || {}).Value; /* Hidden FK */ },
			set regi1(newValue) { (this.formContext.Data.RelationKeysSelector.regi1.getQControl() || {}).Value = newValue; /* Hidden FK */ },
		};
	</text>
}

	window.Form_Pessohis.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Pessohis.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitPessohis(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Pessohis.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeletePessohis(event) {
			event.preventDefault();
			window.Form_Pessohis.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS PESSOHIS]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Pessohis"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
