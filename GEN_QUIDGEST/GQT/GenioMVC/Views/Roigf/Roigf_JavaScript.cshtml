@model GenioMVC.ViewModels.Roigf.Roigf_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ROIGF]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Roigf !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Roigf = new QForm($('[data-form="Roigf"]'), "Form_Roigf");
		}
		window.Form_Roigf.baseArea = "roigf";
		window.Form_Roigf.FormMode = '@Model.GetFormMode';
		window.Form_Roigf.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Roigf.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Roigf")',
			GetDependants:		'@Url.Action("GetDependants", "Roigf")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Roigf", "Roigf")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Roigf", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Roigf", "Roigf")',
		};
		window.Form_Roigf.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Roigf.formSaveEdit = '@Url.Action("Roigf_SaveEdit", "Roigf")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Roigf.formAction = 'Roigf_New';
		window.Form_Roigf.formRedirectURL = '@Url.Action("Roigf_New_Redirect", "Roigf")';
		window.Form_Roigf.formSubmitURL = '@Url.Action("Roigf_New", "Roigf")';
		window.Form_Roigf.formRedirectURL_repeatInsertion = '@Url.Action("Roigf_New_Redirect", "Roigf", new { repeatInsertion = true})';
		window.Form_Roigf.formSubmitURL_repeatInsertion = '@Url.Action("Roigf_New", "Roigf", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Roigf.formAction = 'Roigf_Edit';
		window.Form_Roigf.formRedirectURL = '@Url.Action("Roigf_Edit_Redirect", "Roigf")';
		window.Form_Roigf.formSubmitURL = '@Url.Action("Roigf_Edit", "Roigf")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Roigf.formAction = 'Roigf_Duplicate';
		window.Form_Roigf.formRedirectURL = '@Url.Action("Roigf_Duplicate_Redirect", "Roigf")';
		window.Form_Roigf.formSubmitURL = '@Url.Action("Roigf_Duplicate", "Roigf")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Roigf.formAction = 'Roigf_Delete';
		window.Form_Roigf.formRedirectURL = '@Url.Action("Roigf_Delete_Redirect", "Roigf")';
		window.Form_Roigf.formSubmitURL = '@Url.Action("Roigf_Delete", "Roigf")';
	</text>
}
		window.Form_Roigf.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_ROIGF___ROGL1TITLE___ = new QDbeditControl($('[data-identifier="IFF_ROIGF___ROGL1TITLE___"]', this.element), window.Form_Roigf);

			this.Controls.LED_ROIGF___ROIGFORDER___ = new QNumericControl($('[data-identifier="LED_ROIGF___ROIGFORDER___"]', this.element), window.Form_Roigf);

			this.Controls.LED_ROIGF___ROIGFTITLE___ = new QTextControl($('[data-identifier="LED_ROIGF___ROIGFTITLE___"]', this.element), window.Form_Roigf);




			this.Controls.LED_ROIGF___ROIGFCODROIGF = new QHiddenControl($('[data-identifier="LED_ROIGF___ROIGFCODROIGF"]', this.element), window.Form_Roigf);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Roigf.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Roigf.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ROIGF]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Roigf.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ROIGF]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Roigf.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ROIGF]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Roigf.Data.RelationKeysSelector = {
				get rogl1() { return $(':not([elem-identifier="Multiform"]) > #ValCodrogl1[pers-cs-area="Rogl1"]', $('[data-form="Roigf"]')); /* DBEdit */ },
		};

		window.Form_Roigf.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Roigf.Data.RelationKeys = {
			get formContext() { return window.Form_Roigf; },
			get rogl1() { return (this.formContext.Data.RelationKeysSelector.rogl1.getQControl() || {}).Value; /* DBEdit */ },
			set rogl1(newValue) { (this.formContext.Data.RelationKeysSelector.rogl1.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Roigf.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Roigf.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitRoigf(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Roigf.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteRoigf(event) {
			event.preventDefault();
			window.Form_Roigf.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS ROIGF]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Roigf"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
