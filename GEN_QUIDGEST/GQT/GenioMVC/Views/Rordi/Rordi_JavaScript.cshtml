@model GenioMVC.ViewModels.Rordi.Rordi_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS RORDI]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Rordi !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Rordi = new QForm($('[data-form="Rordi"]'), "Form_Rordi");
		}
		window.Form_Rordi.baseArea = "rordi";
		window.Form_Rordi.FormMode = '@Model.GetFormMode';
		window.Form_Rordi.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Rordi.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Rordi")',
			GetDependants:		'@Url.Action("GetDependants", "Rordi")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Rordi", "Rordi")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Rordi", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Rordi", "Rordi")',
		};
		window.Form_Rordi.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Rordi.formSaveEdit = '@Url.Action("Rordi_SaveEdit", "Rordi")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Rordi.formAction = 'Rordi_New';
		window.Form_Rordi.formRedirectURL = '@Url.Action("Rordi_New_Redirect", "Rordi")';
		window.Form_Rordi.formSubmitURL = '@Url.Action("Rordi_New", "Rordi")';
		window.Form_Rordi.formRedirectURL_repeatInsertion = '@Url.Action("Rordi_New_Redirect", "Rordi", new { repeatInsertion = true})';
		window.Form_Rordi.formSubmitURL_repeatInsertion = '@Url.Action("Rordi_New", "Rordi", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Rordi.formAction = 'Rordi_Edit';
		window.Form_Rordi.formRedirectURL = '@Url.Action("Rordi_Edit_Redirect", "Rordi")';
		window.Form_Rordi.formSubmitURL = '@Url.Action("Rordi_Edit", "Rordi")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Rordi.formAction = 'Rordi_Duplicate';
		window.Form_Rordi.formRedirectURL = '@Url.Action("Rordi_Duplicate_Redirect", "Rordi")';
		window.Form_Rordi.formSubmitURL = '@Url.Action("Rordi_Duplicate", "Rordi")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Rordi.formAction = 'Rordi_Delete';
		window.Form_Rordi.formRedirectURL = '@Url.Action("Rordi_Delete_Redirect", "Rordi")';
		window.Form_Rordi.formSubmitURL = '@Url.Action("Rordi_Delete", "Rordi")';
	</text>
}
		window.Form_Rordi.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_RORDI___RORDIORDER___ = new QNumericControl($('[data-identifier="LED_RORDI___RORDIORDER___"]', this.element), window.Form_Rordi);

			this.Controls.LED_RORDI___RORDITITLE___ = new QTextControl($('[data-identifier="LED_RORDI___RORDITITLE___"]', this.element), window.Form_Rordi);




			this.Controls.LED_RORDI___RORDICODRORDI = new QHiddenControl($('[data-identifier="LED_RORDI___RORDICODRORDI"]', this.element), window.Form_Rordi);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Rordi.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Rordi.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI RORDI]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Rordi.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS RORDI]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Rordi.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS RORDI]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Rordi.Data.RelationKeysSelector = {
		};

		window.Form_Rordi.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Rordi.Data.RelationKeys = {
			get formContext() { return window.Form_Rordi; },
		};
	</text>
}

	window.Form_Rordi.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Rordi.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitRordi(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Rordi.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteRordi(event) {
			event.preventDefault();
			window.Form_Rordi.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS RORDI]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Rordi"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
