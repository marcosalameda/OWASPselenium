@model GenioMVC.ViewModels.Sale.Vendaw03_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS VENDAW03]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Vendaw03 !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Vendaw03 = new QForm($('[data-form="Vendaw03"]'), "Form_Vendaw03");
		}
		window.Form_Vendaw03.baseArea = "sale";
		window.Form_Vendaw03.FormMode = '@Model.GetFormMode';
		window.Form_Vendaw03.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Vendaw03.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Sale")',
			GetDependants:		'@Url.Action("GetDependants", "Sale")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Vendaw03", "Sale")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Vendaw03", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Sale", "Sale")',
		};
		window.Form_Vendaw03.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Vendaw03.formSaveEdit = '@Url.Action("Vendaw03_SaveEdit", "Sale")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Vendaw03.formAction = 'Vendaw03_New';
		window.Form_Vendaw03.formRedirectURL = '@Url.Action("Vendaw03_New_Redirect", "Sale")';
		window.Form_Vendaw03.formSubmitURL = '@Url.Action("Vendaw03_New", "Sale")';
		window.Form_Vendaw03.formRedirectURL_repeatInsertion = '@Url.Action("Vendaw03_New_Redirect", "Sale", new { repeatInsertion = true})';
		window.Form_Vendaw03.formSubmitURL_repeatInsertion = '@Url.Action("Vendaw03_New", "Sale", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Vendaw03.formAction = 'Vendaw03_Edit';
		window.Form_Vendaw03.formRedirectURL = '@Url.Action("Vendaw03_Edit_Redirect", "Sale")';
		window.Form_Vendaw03.formSubmitURL = '@Url.Action("Vendaw03_Edit", "Sale")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Vendaw03.formAction = 'Vendaw03_Duplicate';
		window.Form_Vendaw03.formRedirectURL = '@Url.Action("Vendaw03_Duplicate_Redirect", "Sale")';
		window.Form_Vendaw03.formSubmitURL = '@Url.Action("Vendaw03_Duplicate", "Sale")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Vendaw03.formAction = 'Vendaw03_Delete';
		window.Form_Vendaw03.formRedirectURL = '@Url.Action("Vendaw03_Delete_Redirect", "Sale")';
		window.Form_Vendaw03.formSubmitURL = '@Url.Action("Vendaw03_Delete", "Sale")';
	</text>
}
		window.Form_Vendaw03.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_VENDAW03PSEUDNOVOGR03 = new QGenericControl($('[data-identifier="IFF_VENDAW03PSEUDNOVOGR03"]', this.element), window.Form_Vendaw03);

			this.Controls.LED_VENDAW03SALE_PREABORD = new QDateControl($('[data-identifier="LED_VENDAW03SALE_PREABORD"]', this.element), window.Form_Vendaw03);

			this.Controls.LED_VENDAW03SALE_HOMEWORK = new QCheckBoxControl($('[data-identifier="LED_VENDAW03SALE_HOMEWORK"]', this.element), window.Form_Vendaw03);




@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	@*Hidden Foreign Keys Controls*@
			this.Controls.LED_VENDAW03ORGANCODORGAN = new QHiddenControl($('[data-identifier="LED_VENDAW03ORGANCODORGAN"]', this.element), window.Form_Vendaw03);
	</text>
}
			this.Controls.LED_VENDAW03SALE_CODVENDA = new QHiddenControl($('[data-identifier="LED_VENDAW03SALE_CODVENDA"]', this.element), window.Form_Vendaw03);

	@* Hidden Required Form Fields Controls for Formulas *@
			this.Controls.LED_VENDAW03SALE_IDENTIFI = new QSpecialControl($('[data-identifier="LED_VENDAW03SALE_IDENTIFI"]', this.element), window.Form_Vendaw03);
			this.Controls.LED_VENDAW03SALE_IDENTIFI.InitData(@JavaScriptConversion.ToString(@Model.ValIdentifi));

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Vendaw03.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Vendaw03.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI VENDAW03]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Vendaw03.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS VENDAW03]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Vendaw03.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS VENDAW03]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Vendaw03.Data.RelationKeysSelector = {
				get organ() { return $(':not([elem-identifier="Multiform"]) > #ValCodorgan', $('[data-form="Vendaw03"]')); /* Hidden */ },
		};

		window.Form_Vendaw03.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Vendaw03.Data.RelationKeys = {
			get formContext() { return window.Form_Vendaw03; },
			get organ() { return (this.formContext.Data.RelationKeysSelector.organ.getQControl() || {}).Value; /* Hidden FK */ },
			set organ(newValue) { (this.formContext.Data.RelationKeysSelector.organ.getQControl() || {}).Value = newValue; /* Hidden FK */ },
		};
	</text>
}

	window.Form_Vendaw03.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Vendaw03.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitVendaw03(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Vendaw03.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteVendaw03(event) {
			event.preventDefault();
			window.Form_Vendaw03.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS VENDAW03]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Vendaw03"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
