@model GenioMVC.ViewModels.Tabpr.Tabpr_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS TABPR]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Tabpr !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Tabpr = new QForm($('[data-form="Tabpr"]'), "Form_Tabpr");
		}
		window.Form_Tabpr.baseArea = "tabpr";
		window.Form_Tabpr.FormMode = '@Model.GetFormMode';
		window.Form_Tabpr.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Tabpr.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Tabpr")',
			GetDependants:		'@Url.Action("GetDependants", "Tabpr")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Tabpr", "Tabpr")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Tabpr", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Tabpr", "Tabpr")',
		};
		window.Form_Tabpr.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tabpr.formSaveEdit = '@Url.Action("Tabpr_SaveEdit", "Tabpr")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Tabpr.formAction = 'Tabpr_New';
		window.Form_Tabpr.formRedirectURL = '@Url.Action("Tabpr_New_Redirect", "Tabpr")';
		window.Form_Tabpr.formSubmitURL = '@Url.Action("Tabpr_New", "Tabpr")';
		window.Form_Tabpr.formRedirectURL_repeatInsertion = '@Url.Action("Tabpr_New_Redirect", "Tabpr", new { repeatInsertion = true})';
		window.Form_Tabpr.formSubmitURL_repeatInsertion = '@Url.Action("Tabpr_New", "Tabpr", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Tabpr.formAction = 'Tabpr_Edit';
		window.Form_Tabpr.formRedirectURL = '@Url.Action("Tabpr_Edit_Redirect", "Tabpr")';
		window.Form_Tabpr.formSubmitURL = '@Url.Action("Tabpr_Edit", "Tabpr")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tabpr.formAction = 'Tabpr_Duplicate';
		window.Form_Tabpr.formRedirectURL = '@Url.Action("Tabpr_Duplicate_Redirect", "Tabpr")';
		window.Form_Tabpr.formSubmitURL = '@Url.Action("Tabpr_Duplicate", "Tabpr")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Tabpr.formAction = 'Tabpr_Delete';
		window.Form_Tabpr.formRedirectURL = '@Url.Action("Tabpr_Delete_Redirect", "Tabpr")';
		window.Form_Tabpr.formSubmitURL = '@Url.Action("Tabpr_Delete", "Tabpr")';
	</text>
}
		window.Form_Tabpr.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_TABPR___PSEUDNOVOGR01 = new QGenericControl($('[data-identifier="IFF_TABPR___PSEUDNOVOGR01"]', this.element), window.Form_Tabpr);

			this.Controls.IFF_TABPR___TPEQUTIPOEQUI = new QDbeditControl($('[data-identifier="IFF_TABPR___TPEQUTIPOEQUI"]', this.element), window.Form_Tabpr);

			this.Controls.LED_TABPR___TABPRSINCE___ = new QDateControl($('[data-identifier="LED_TABPR___TABPRSINCE___"]', this.element), window.Form_Tabpr);

			this.Controls.LED_TABPR___TABPRPRECOHOR = new QNumericControl($('[data-identifier="LED_TABPR___TABPRPRECOHOR"]', this.element), window.Form_Tabpr);




	@*Hidden Required Foreign Keys for Formulas*@
			this.Controls.LED_TABPR___TPEQUCODTPEQU = new QHiddenControl($('[data-identifier="LED_TABPR___TPEQUCODTPEQU"]', this.element), window.Form_Tabpr);
			this.Controls.LED_TABPR___TABPRCODTABPR = new QHiddenControl($('[data-identifier="LED_TABPR___TABPRCODTABPR"]', this.element), window.Form_Tabpr);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Tabpr.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tabpr.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI TABPR]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Tabpr.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS TABPR]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Tabpr.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS TABPR]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Tabpr.Data.RelationKeysSelector = {
				get tpequ() { return $(':not([elem-identifier="Multiform"]) > #ValCodtpeq1[pers-cs-area="Tpequ"]', $('[data-form="Tabpr"]')); /* DBEdit */ },
		};

		window.Form_Tabpr.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Tabpr.Data.RelationKeys = {
			get formContext() { return window.Form_Tabpr; },
			get tpequ() { return (this.formContext.Data.RelationKeysSelector.tpequ.getQControl() || {}).Value; /* DBEdit */ },
			set tpequ(newValue) { (this.formContext.Data.RelationKeysSelector.tpequ.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Tabpr.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Tabpr.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitTabpr(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Tabpr.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteTabpr(event) {
			event.preventDefault();
			window.Form_Tabpr.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS TABPR]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Tabpr"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
