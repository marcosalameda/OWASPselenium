@model GenioMVC.ViewModels.Tblk.Tblk_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS TBLK]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Tblk !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Tblk = new QForm($('[data-form="Tblk"]'), "Form_Tblk");
		}
		window.Form_Tblk.baseArea = "tblk";
		window.Form_Tblk.FormMode = '@Model.GetFormMode';
		window.Form_Tblk.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Tblk.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Tblk")',
			GetDependants:		'@Url.Action("GetDependants", "Tblk")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Tblk", "Tblk")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Tblk", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Tblk", "Tblk")',
		};
		window.Form_Tblk.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tblk.formSaveEdit = '@Url.Action("Tblk_SaveEdit", "Tblk")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Tblk.formAction = 'Tblk_New';
		window.Form_Tblk.formRedirectURL = '@Url.Action("Tblk_New_Redirect", "Tblk")';
		window.Form_Tblk.formSubmitURL = '@Url.Action("Tblk_New", "Tblk")';
		window.Form_Tblk.formRedirectURL_repeatInsertion = '@Url.Action("Tblk_New_Redirect", "Tblk", new { repeatInsertion = true})';
		window.Form_Tblk.formSubmitURL_repeatInsertion = '@Url.Action("Tblk_New", "Tblk", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Tblk.formAction = 'Tblk_Edit';
		window.Form_Tblk.formRedirectURL = '@Url.Action("Tblk_Edit_Redirect", "Tblk")';
		window.Form_Tblk.formSubmitURL = '@Url.Action("Tblk_Edit", "Tblk")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tblk.formAction = 'Tblk_Duplicate';
		window.Form_Tblk.formRedirectURL = '@Url.Action("Tblk_Duplicate_Redirect", "Tblk")';
		window.Form_Tblk.formSubmitURL = '@Url.Action("Tblk_Duplicate", "Tblk")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Tblk.formAction = 'Tblk_Delete';
		window.Form_Tblk.formRedirectURL = '@Url.Action("Tblk_Delete_Redirect", "Tblk")';
		window.Form_Tblk.formSubmitURL = '@Url.Action("Tblk_Delete", "Tblk")';
	</text>
}
		window.Form_Tblk.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_TBLK____TBLK_NAME____ = new QTextControl($('[data-identifier="LED_TBLK____TBLK_NAME____"]', this.element), window.Form_Tblk);

			this.Controls.IFF_TBLK____GRPB_NAME____ = new QDbeditControl($('[data-identifier="IFF_TBLK____GRPB_NAME____"]', this.element), window.Form_Tblk);

			this.Controls.IFF_TBLK____TRSB_NAME____ = new QDbeditControl($('[data-identifier="IFF_TBLK____TRSB_NAME____"]', this.element), window.Form_Tblk);




			this.Controls.LED_TBLK____TBLK_CODTBLK_ = new QHiddenControl($('[data-identifier="LED_TBLK____TBLK_CODTBLK_"]', this.element), window.Form_Tblk);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Tblk.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tblk.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI TBLK]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Tblk.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS TBLK]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Tblk.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS TBLK]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Tblk.Data.RelationKeysSelector = {
				get grpb() { return $(':not([elem-identifier="Multiform"]) > #ValFkey1[pers-cs-area="Grpb"]', $('[data-form="Tblk"]')); /* DBEdit */ },
				get trsb() { return $(':not([elem-identifier="Multiform"]) > #ValFkey2[pers-cs-area="Trsb"]', $('[data-form="Tblk"]')); /* DBEdit */ },
		};

		window.Form_Tblk.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Tblk.Data.RelationKeys = {
			get formContext() { return window.Form_Tblk; },
			get grpb() { return (this.formContext.Data.RelationKeysSelector.grpb.getQControl() || {}).Value; /* DBEdit */ },
			set grpb(newValue) { (this.formContext.Data.RelationKeysSelector.grpb.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get trsb() { return (this.formContext.Data.RelationKeysSelector.trsb.getQControl() || {}).Value; /* DBEdit */ },
			set trsb(newValue) { (this.formContext.Data.RelationKeysSelector.trsb.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Tblk.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Tblk.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitTblk(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Tblk.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteTblk(event) {
			event.preventDefault();
			window.Form_Tblk.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS TBLK]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Tblk"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
