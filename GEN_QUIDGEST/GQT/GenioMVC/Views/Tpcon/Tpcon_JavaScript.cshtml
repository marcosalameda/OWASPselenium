@model GenioMVC.ViewModels.Tpcon.Tpcon_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS TPCON]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Tpcon !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Tpcon = new QForm($('[data-form="Tpcon"]'), "Form_Tpcon");
		}
		window.Form_Tpcon.baseArea = "tpcon";
		window.Form_Tpcon.FormMode = '@Model.GetFormMode';
		window.Form_Tpcon.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Tpcon.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Tpcon")',
			GetDependants:		'@Url.Action("GetDependants", "Tpcon")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Tpcon", "Tpcon")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Tpcon", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Tpcon", "Tpcon")',
		};
		window.Form_Tpcon.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tpcon.formSaveEdit = '@Url.Action("Tpcon_SaveEdit", "Tpcon")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Tpcon.formAction = 'Tpcon_New';
		window.Form_Tpcon.formRedirectURL = '@Url.Action("Tpcon_New_Redirect", "Tpcon")';
		window.Form_Tpcon.formSubmitURL = '@Url.Action("Tpcon_New", "Tpcon")';
		window.Form_Tpcon.formRedirectURL_repeatInsertion = '@Url.Action("Tpcon_New_Redirect", "Tpcon", new { repeatInsertion = true})';
		window.Form_Tpcon.formSubmitURL_repeatInsertion = '@Url.Action("Tpcon_New", "Tpcon", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Tpcon.formAction = 'Tpcon_Edit';
		window.Form_Tpcon.formRedirectURL = '@Url.Action("Tpcon_Edit_Redirect", "Tpcon")';
		window.Form_Tpcon.formSubmitURL = '@Url.Action("Tpcon_Edit", "Tpcon")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tpcon.formAction = 'Tpcon_Duplicate';
		window.Form_Tpcon.formRedirectURL = '@Url.Action("Tpcon_Duplicate_Redirect", "Tpcon")';
		window.Form_Tpcon.formSubmitURL = '@Url.Action("Tpcon_Duplicate", "Tpcon")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Tpcon.formAction = 'Tpcon_Delete';
		window.Form_Tpcon.formRedirectURL = '@Url.Action("Tpcon_Delete_Redirect", "Tpcon")';
		window.Form_Tpcon.formSubmitURL = '@Url.Action("Tpcon_Delete", "Tpcon")';
	</text>
}
		window.Form_Tpcon.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_TPCON___PSEUDNOVOGR01 = new QGenericControl($('[data-identifier="IFF_TPCON___PSEUDNOVOGR01"]', this.element), window.Form_Tpcon);

			this.Controls.IFF_TPCON___GENREGENDER__ = new QDbeditControl($('[data-identifier="IFF_TPCON___GENREGENDER__"]', this.element), window.Form_Tpcon);

			this.Controls.LED_TPCON___TPCONTIPOCONT = new QTextControl($('[data-identifier="LED_TPCON___TPCONTIPOCONT"]', this.element), window.Form_Tpcon);




	@*Hidden Required Foreign Keys for Formulas*@
			this.Controls.LED_TPCON___GENRECODGENRE = new QHiddenControl($('[data-identifier="LED_TPCON___GENRECODGENRE"]', this.element), window.Form_Tpcon);
			this.Controls.LED_TPCON___TPCONCODTPCON = new QHiddenControl($('[data-identifier="LED_TPCON___TPCONCODTPCON"]', this.element), window.Form_Tpcon);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Tpcon.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tpcon.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI TPCON]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Tpcon.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS TPCON]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Tpcon.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS TPCON]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Tpcon.Data.RelationKeysSelector = {
				get genre() { return $(':not([elem-identifier="Multiform"]) > #ValCodgenre[pers-cs-area="Genre"]', $('[data-form="Tpcon"]')); /* DBEdit */ },
		};

		window.Form_Tpcon.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Tpcon.Data.RelationKeys = {
			get formContext() { return window.Form_Tpcon; },
			get genre() { return (this.formContext.Data.RelationKeysSelector.genre.getQControl() || {}).Value; /* DBEdit */ },
			set genre(newValue) { (this.formContext.Data.RelationKeysSelector.genre.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Tpcon.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Tpcon.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitTpcon(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Tpcon.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteTpcon(event) {
			event.preventDefault();
			window.Form_Tpcon.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS TPCON]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Tpcon"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
