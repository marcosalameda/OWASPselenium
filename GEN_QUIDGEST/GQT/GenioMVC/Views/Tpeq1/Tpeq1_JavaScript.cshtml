@model GenioMVC.ViewModels.Tpeq1.Tpeq1_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS TPEQ1]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Tpeq1 !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Tpeq1 = new QForm($('[data-form="Tpeq1"]'), "Form_Tpeq1");
		}
		window.Form_Tpeq1.baseArea = "tpeq1";
		window.Form_Tpeq1.FormMode = '@Model.GetFormMode';
		window.Form_Tpeq1.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Tpeq1.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Tpeq1")',
			GetDependants:		'@Url.Action("GetDependants", "Tpeq1")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Tpeq1", "Tpeq1")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Tpeq1", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Tpeq1", "Tpeq1")',
		};
		window.Form_Tpeq1.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tpeq1.formSaveEdit = '@Url.Action("Tpeq1_SaveEdit", "Tpeq1")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Tpeq1.formAction = 'Tpeq1_New';
		window.Form_Tpeq1.formRedirectURL = '@Url.Action("Tpeq1_New_Redirect", "Tpeq1")';
		window.Form_Tpeq1.formSubmitURL = '@Url.Action("Tpeq1_New", "Tpeq1")';
		window.Form_Tpeq1.formRedirectURL_repeatInsertion = '@Url.Action("Tpeq1_New_Redirect", "Tpeq1", new { repeatInsertion = true})';
		window.Form_Tpeq1.formSubmitURL_repeatInsertion = '@Url.Action("Tpeq1_New", "Tpeq1", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Tpeq1.formAction = 'Tpeq1_Edit';
		window.Form_Tpeq1.formRedirectURL = '@Url.Action("Tpeq1_Edit_Redirect", "Tpeq1")';
		window.Form_Tpeq1.formSubmitURL = '@Url.Action("Tpeq1_Edit", "Tpeq1")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tpeq1.formAction = 'Tpeq1_Duplicate';
		window.Form_Tpeq1.formRedirectURL = '@Url.Action("Tpeq1_Duplicate_Redirect", "Tpeq1")';
		window.Form_Tpeq1.formSubmitURL = '@Url.Action("Tpeq1_Duplicate", "Tpeq1")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Tpeq1.formAction = 'Tpeq1_Delete';
		window.Form_Tpeq1.formRedirectURL = '@Url.Action("Tpeq1_Delete_Redirect", "Tpeq1")';
		window.Form_Tpeq1.formSubmitURL = '@Url.Action("Tpeq1_Delete", "Tpeq1")';
	</text>
}
		window.Form_Tpeq1.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_TPEQ1___FAMI1FAMILY__ = new QDbeditControl($('[data-identifier="IFF_TPEQ1___FAMI1FAMILY__"]', this.element), window.Form_Tpeq1);

			this.Controls.LED_TPEQ1___TPEQ1TPEQUCOD = new QTextControl($('[data-identifier="LED_TPEQ1___TPEQ1TPEQUCOD"]', this.element), window.Form_Tpeq1);

			this.Controls.LED_TPEQ1___TPEQ1NIVEL___ = new QNumericControl($('[data-identifier="LED_TPEQ1___TPEQ1NIVEL___"]', this.element), window.Form_Tpeq1);

			this.Controls.LED_TPEQ1___TPEQ1TIPOEQUI = new QTextControl($('[data-identifier="LED_TPEQ1___TPEQ1TIPOEQUI"]', this.element), window.Form_Tpeq1);

			this.Controls.LED_TPEQ1___TPEQ1TPEQUPAI = new QTextControl($('[data-identifier="LED_TPEQ1___TPEQ1TPEQUPAI"]', this.element), window.Form_Tpeq1);

			this.Controls.LED_TPEQ1___TPEQ1BACKCOLO = new QTextControl($('[data-identifier="LED_TPEQ1___TPEQ1BACKCOLO"]', this.element), window.Form_Tpeq1);

			this.Controls.LED_TPEQ1___TPEQ1CORLETRA = new QTextControl($('[data-identifier="LED_TPEQ1___TPEQ1CORLETRA"]', this.element), window.Form_Tpeq1);

			this.Controls.LED_TPEQ1___TPEQ1PRECOMAX = new QNumericControl($('[data-identifier="LED_TPEQ1___TPEQ1PRECOMAX"]', this.element), window.Form_Tpeq1);

			this.Controls.LED_TPEQ1___TPEQ1PRECOULT = new QNumericControl($('[data-identifier="LED_TPEQ1___TPEQ1PRECOULT"]', this.element), window.Form_Tpeq1);

			this.Controls.LED_TPEQ1___TPEQ1SINCE___ = new QDateControl($('[data-identifier="LED_TPEQ1___TPEQ1SINCE___"]', this.element), window.Form_Tpeq1);

			this.Controls.LED_TPEQ1___TPEQ1QTDEQUIP = new QNumericControl($('[data-identifier="LED_TPEQ1___TPEQ1QTDEQUIP"]', this.element), window.Form_Tpeq1);

			this.Controls.LED_TPEQ1___TPEQ1KIT_____ = new QCheckBoxControl($('[data-identifier="LED_TPEQ1___TPEQ1KIT_____"]', this.element), window.Form_Tpeq1);




			this.Controls.LED_TPEQ1___TPEQ1CODTPEQU = new QHiddenControl($('[data-identifier="LED_TPEQ1___TPEQ1CODTPEQU"]', this.element), window.Form_Tpeq1);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Tpeq1.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tpeq1.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI TPEQ1]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Tpeq1.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS TPEQ1]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Tpeq1.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS TPEQ1]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Tpeq1.Data.RelationKeysSelector = {
				get fami1() { return $(':not([elem-identifier="Multiform"]) > #ValCodfamil[pers-cs-area="Fami1"]', $('[data-form="Tpeq1"]')); /* DBEdit */ },
		};

		window.Form_Tpeq1.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Tpeq1.Data.RelationKeys = {
			get formContext() { return window.Form_Tpeq1; },
			get fami1() { return (this.formContext.Data.RelationKeysSelector.fami1.getQControl() || {}).Value; /* DBEdit */ },
			set fami1(newValue) { (this.formContext.Data.RelationKeysSelector.fami1.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Tpeq1.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Tpeq1.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitTpeq1(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Tpeq1.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteTpeq1(event) {
			event.preventDefault();
			window.Form_Tpeq1.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS TPEQ1]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Tpeq1"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
