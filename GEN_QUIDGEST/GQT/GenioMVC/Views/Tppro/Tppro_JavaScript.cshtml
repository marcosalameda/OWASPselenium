@model GenioMVC.ViewModels.Tppro.Tppro_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS TPPRO]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Tppro !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Tppro = new QForm($('[data-form="Tppro"]'), "Form_Tppro");
		}
		window.Form_Tppro.baseArea = "tppro";
		window.Form_Tppro.FormMode = '@Model.GetFormMode';
		window.Form_Tppro.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Tppro.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Tppro")',
			GetDependants:		'@Url.Action("GetDependants", "Tppro")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Tppro", "Tppro")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Tppro", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Tppro", "Tppro")',
		};
		window.Form_Tppro.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tppro.formSaveEdit = '@Url.Action("Tppro_SaveEdit", "Tppro")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Tppro.formAction = 'Tppro_New';
		window.Form_Tppro.formRedirectURL = '@Url.Action("Tppro_New_Redirect", "Tppro")';
		window.Form_Tppro.formSubmitURL = '@Url.Action("Tppro_New", "Tppro")';
		window.Form_Tppro.formRedirectURL_repeatInsertion = '@Url.Action("Tppro_New_Redirect", "Tppro", new { repeatInsertion = true})';
		window.Form_Tppro.formSubmitURL_repeatInsertion = '@Url.Action("Tppro_New", "Tppro", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Tppro.formAction = 'Tppro_Edit';
		window.Form_Tppro.formRedirectURL = '@Url.Action("Tppro_Edit_Redirect", "Tppro")';
		window.Form_Tppro.formSubmitURL = '@Url.Action("Tppro_Edit", "Tppro")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tppro.formAction = 'Tppro_Duplicate';
		window.Form_Tppro.formRedirectURL = '@Url.Action("Tppro_Duplicate_Redirect", "Tppro")';
		window.Form_Tppro.formSubmitURL = '@Url.Action("Tppro_Duplicate", "Tppro")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Tppro.formAction = 'Tppro_Delete';
		window.Form_Tppro.formRedirectURL = '@Url.Action("Tppro_Delete_Redirect", "Tppro")';
		window.Form_Tppro.formSubmitURL = '@Url.Action("Tppro_Delete", "Tppro")';
	</text>
}
		window.Form_Tppro.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_TPPRO___TPPROTPPROPRI = new QTextControl($('[data-identifier="LED_TPPRO___TPPROTPPROPRI"]', this.element), window.Form_Tppro);




			this.Controls.LED_TPPRO___TPPROCODTPPRO = new QHiddenControl($('[data-identifier="LED_TPPRO___TPPROCODTPPRO"]', this.element), window.Form_Tppro);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Tppro.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tppro.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI TPPRO]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Tppro.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS TPPRO]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Tppro.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS TPPRO]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Tppro.Data.RelationKeysSelector = {
		};

		window.Form_Tppro.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Tppro.Data.RelationKeys = {
			get formContext() { return window.Form_Tppro; },
		};
	</text>
}

	window.Form_Tppro.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Tppro.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitTppro(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Tppro.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteTppro(event) {
			event.preventDefault();
			window.Form_Tppro.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS TPPRO]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Tppro"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
