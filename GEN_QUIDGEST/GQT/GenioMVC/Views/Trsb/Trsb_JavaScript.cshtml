@model GenioMVC.ViewModels.Trsb.Trsb_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS TRSB]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Trsb !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Trsb = new QForm($('[data-form="Trsb"]'), "Form_Trsb");
		}
		window.Form_Trsb.baseArea = "trsb";
		window.Form_Trsb.FormMode = '@Model.GetFormMode';
		window.Form_Trsb.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Trsb.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Trsb")',
			GetDependants:		'@Url.Action("GetDependants", "Trsb")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Trsb", "Trsb")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Trsb", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Trsb", "Trsb")',
		};
		window.Form_Trsb.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Trsb.formSaveEdit = '@Url.Action("Trsb_SaveEdit", "Trsb")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Trsb.formAction = 'Trsb_New';
		window.Form_Trsb.formRedirectURL = '@Url.Action("Trsb_New_Redirect", "Trsb")';
		window.Form_Trsb.formSubmitURL = '@Url.Action("Trsb_New", "Trsb")';
		window.Form_Trsb.formRedirectURL_repeatInsertion = '@Url.Action("Trsb_New_Redirect", "Trsb", new { repeatInsertion = true})';
		window.Form_Trsb.formSubmitURL_repeatInsertion = '@Url.Action("Trsb_New", "Trsb", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Trsb.formAction = 'Trsb_Edit';
		window.Form_Trsb.formRedirectURL = '@Url.Action("Trsb_Edit_Redirect", "Trsb")';
		window.Form_Trsb.formSubmitURL = '@Url.Action("Trsb_Edit", "Trsb")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Trsb.formAction = 'Trsb_Duplicate';
		window.Form_Trsb.formRedirectURL = '@Url.Action("Trsb_Duplicate_Redirect", "Trsb")';
		window.Form_Trsb.formSubmitURL = '@Url.Action("Trsb_Duplicate", "Trsb")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Trsb.formAction = 'Trsb_Delete';
		window.Form_Trsb.formRedirectURL = '@Url.Action("Trsb_Delete_Redirect", "Trsb")';
		window.Form_Trsb.formSubmitURL = '@Url.Action("Trsb_Delete", "Trsb")';
	</text>
}
		window.Form_Trsb.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_TRSB____TRSB_NAME____ = new QTextControl($('[data-identifier="LED_TRSB____TRSB_NAME____"]', this.element), window.Form_Trsb);




			this.Controls.LED_TRSB____TRSB_CODTRSB_ = new QHiddenControl($('[data-identifier="LED_TRSB____TRSB_CODTRSB_"]', this.element), window.Form_Trsb);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Trsb.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Trsb.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI TRSB]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Trsb.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS TRSB]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Trsb.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS TRSB]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Trsb.Data.RelationKeysSelector = {
		};

		window.Form_Trsb.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Trsb.Data.RelationKeys = {
			get formContext() { return window.Form_Trsb; },
		};
	</text>
}

	window.Form_Trsb.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Trsb.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitTrsb(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Trsb.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteTrsb(event) {
			event.preventDefault();
			window.Form_Trsb.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS TRSB]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Trsb"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
