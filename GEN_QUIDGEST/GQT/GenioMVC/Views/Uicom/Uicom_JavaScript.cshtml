@model GenioMVC.ViewModels.Uicom.Uicom_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS UICOM]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Uicom !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Uicom = new QForm($('[data-form="Uicom"]'), "Form_Uicom");
		}
		window.Form_Uicom.baseArea = "uicom";
		window.Form_Uicom.FormMode = '@Model.GetFormMode';
		window.Form_Uicom.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Uicom.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Uicom")',
			GetDependants:		'@Url.Action("GetDependants", "Uicom")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Uicom", "Uicom")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Uicom", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Uicom", "Uicom")',
		};
		window.Form_Uicom.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Uicom.formSaveEdit = '@Url.Action("Uicom_SaveEdit", "Uicom")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Uicom.formAction = 'Uicom_New';
		window.Form_Uicom.formRedirectURL = '@Url.Action("Uicom_New_Redirect", "Uicom")';
		window.Form_Uicom.formSubmitURL = '@Url.Action("Uicom_New", "Uicom")';
		window.Form_Uicom.formRedirectURL_repeatInsertion = '@Url.Action("Uicom_New_Redirect", "Uicom", new { repeatInsertion = true})';
		window.Form_Uicom.formSubmitURL_repeatInsertion = '@Url.Action("Uicom_New", "Uicom", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Uicom.formAction = 'Uicom_Edit';
		window.Form_Uicom.formRedirectURL = '@Url.Action("Uicom_Edit_Redirect", "Uicom")';
		window.Form_Uicom.formSubmitURL = '@Url.Action("Uicom_Edit", "Uicom")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Uicom.formAction = 'Uicom_Duplicate';
		window.Form_Uicom.formRedirectURL = '@Url.Action("Uicom_Duplicate_Redirect", "Uicom")';
		window.Form_Uicom.formSubmitURL = '@Url.Action("Uicom_Duplicate", "Uicom")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Uicom.formAction = 'Uicom_Delete';
		window.Form_Uicom.formRedirectURL = '@Url.Action("Uicom_Delete_Redirect", "Uicom")';
		window.Form_Uicom.formSubmitURL = '@Url.Action("Uicom_Delete", "Uicom")';
	</text>
}
		window.Form_Uicom.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_UICOM___UICOMTHUMBNAI = new QImageControl($('[data-identifier="LED_UICOM___UICOMTHUMBNAI"]', this.element), window.Form_Uicom, '@Html.Raw(Url.Action("ImageDelete", "Uicom", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Uicom", "ValThumbnai", "", Model.ValCoduicom, null) }))', '@Html.Raw(Url.Action("ImageCropper", "Uicom", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Uicom", "ValThumbnai", "", Model.ValCoduicom, null), identifier= "FUICOM", FormName= "Uicom", FieldId= "LED_UICOM___UICOMTHUMBNAI" }))',@Html.IsImageEmpty(Model.ValThumbnai));

			this.Controls.LED_UICOM___UICOMNAME____ = new QTextControl($('[data-identifier="LED_UICOM___UICOMNAME____"]', this.element), window.Form_Uicom);

			this.Controls.LED_UICOM___UICOMCATEGORY = new QTextControl($('[data-identifier="LED_UICOM___UICOMCATEGORY"]', this.element), window.Form_Uicom);

			this.Controls.LED_UICOM___UICOMMENUID__ = new QTextControl($('[data-identifier="LED_UICOM___UICOMMENUID__"]', this.element), window.Form_Uicom);




			this.Controls.LED_UICOM___UICOMCODUICOM = new QHiddenControl($('[data-identifier="LED_UICOM___UICOMCODUICOM"]', this.element), window.Form_Uicom);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Uicom.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Uicom.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI UICOM]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Uicom.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS UICOM]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Uicom.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS UICOM]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Uicom.Data.RelationKeysSelector = {
		};

		window.Form_Uicom.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Uicom.Data.RelationKeys = {
			get formContext() { return window.Form_Uicom; },
		};
	</text>
}

	window.Form_Uicom.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Uicom.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitUicom(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Uicom.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteUicom(event) {
			event.preventDefault();
			window.Form_Uicom.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS UICOM]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Uicom"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
