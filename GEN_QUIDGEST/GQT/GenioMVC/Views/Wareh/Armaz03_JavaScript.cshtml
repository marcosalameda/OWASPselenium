@model GenioMVC.ViewModels.Wareh.Armaz03_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ARMAZ03]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Armaz03 !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Armaz03 = new QForm($('[data-form="Armaz03"]'), "Form_Armaz03");
		}
		window.Form_Armaz03.baseArea = "wareh";
		window.Form_Armaz03.FormMode = '@Model.GetFormMode';
		window.Form_Armaz03.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Armaz03.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Wareh")',
			GetDependants:		'@Url.Action("GetDependants", "Wareh")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Armaz03", "Wareh")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Armaz03", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Wareh", "Wareh")',
		};
		window.Form_Armaz03.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Armaz03.formSaveEdit = '@Url.Action("Armaz03_SaveEdit", "Wareh")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Armaz03.formAction = 'Armaz03_New';
		window.Form_Armaz03.formRedirectURL = '@Url.Action("Armaz03_New_Redirect", "Wareh")';
		window.Form_Armaz03.formSubmitURL = '@Url.Action("Armaz03_New", "Wareh")';
		window.Form_Armaz03.formRedirectURL_repeatInsertion = '@Url.Action("Armaz03_New_Redirect", "Wareh", new { repeatInsertion = true})';
		window.Form_Armaz03.formSubmitURL_repeatInsertion = '@Url.Action("Armaz03_New", "Wareh", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Armaz03.formAction = 'Armaz03_Edit';
		window.Form_Armaz03.formRedirectURL = '@Url.Action("Armaz03_Edit_Redirect", "Wareh")';
		window.Form_Armaz03.formSubmitURL = '@Url.Action("Armaz03_Edit", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Armaz03.formAction = 'Armaz03_Duplicate';
		window.Form_Armaz03.formRedirectURL = '@Url.Action("Armaz03_Duplicate_Redirect", "Wareh")';
		window.Form_Armaz03.formSubmitURL = '@Url.Action("Armaz03_Duplicate", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Armaz03.formAction = 'Armaz03_Delete';
		window.Form_Armaz03.formRedirectURL = '@Url.Action("Armaz03_Delete_Redirect", "Wareh")';
		window.Form_Armaz03.formSubmitURL = '@Url.Action("Armaz03_Delete", "Wareh")';
	</text>
}
		window.Form_Armaz03.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_ARMAZ03_PSEUDARTIGAPO = new QDateControl($('[data-identifier="IFF_ARMAZ03_PSEUDARTIGAPO"]', this.element), window.Form_Armaz03);

			this.Controls.IFF_ARMAZ03_PSEUDARTIGOS_ = new QTableListControl($('[data-identifier="IFF_ARMAZ03_PSEUDARTIGOS_"]', this.element), window.Form_Armaz03);




			this.Controls.LED_ARMAZ03_WAREHCODWAREH = new QHiddenControl($('[data-identifier="LED_ARMAZ03_WAREHCODWAREH"]', this.element), window.Form_Armaz03);

	@* Hidden Required Form Fields Controls for Formulas *@
			this.Controls.LED_ARMAZ03_WAREHWAREHDES = new QSpecialControl($('[data-identifier="LED_ARMAZ03_WAREHWAREHDES"]', this.element), window.Form_Armaz03);
			this.Controls.LED_ARMAZ03_WAREHWAREHDES.InitData(@JavaScriptConversion.ToString(@Model.ValWarehdes));

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Armaz03.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Armaz03.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ARMAZ03]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Armaz03.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ARMAZ03]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Armaz03.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ARMAZ03]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Armaz03.Data.RelationKeysSelector = {
		};

		window.Form_Armaz03.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Armaz03.Data.RelationKeys = {
			get formContext() { return window.Form_Armaz03; },
		};
	</text>
}

	window.Form_Armaz03.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Armaz03.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitArmaz03(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Armaz03.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteArmaz03(event) {
			event.preventDefault();
			window.Form_Armaz03.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
    modal_QuidgestGlobals = {
        UrlAction: {
            newWindow: '@Url.Action("newWindow")',
            ReloadDBEdit: '@Url.Action("ReloadDBEdit")',
            formArea: '	@Url.Action("form", "area")',
            UpdateValues: '@Url.Action("UpdateValues")',
            LogJavaScriptError: '@Url.Action("LogJavaScriptError", "ApplicationErrors")',
            syncFormKeys: '@Url.Action("syncFormKeys")',
            GetDependants: 		'@Url.Action("GetDependants")',
            ImageHandlerPut:		'@Url.Action("ImageHandlerPut")',
            ImageHandlerGet:		'@Url.Action("ImageHandlerGet")'
        }
    };
$(document).ready(function () {
	loaded($("#form-modal"));
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
	var ajaxCallsRemaining = $("#form-modal .flash object embed").length;
	$('#form-modal form [elem-identifier="ButtonPrimary"]').click(function() {
		var flashObjects = $("#form-modal .flash object embed");
		ajaxCallsRemaining = flashObjects.length;
		if(flashObjects.length == 0)
			return true;
		else {
			flashObjects.each(function() {
			   var flashObj = GetFlash($(this).attr('id'))
			   flashObj.UsedFlash.TriggerSave(flashObj);
			});
			return false;
		}
	});
    </text>
}
});
// USE /[MANUAL GQT FORM_CODEJS ARMAZ03]/



 

    $($('[elem-identifier="Actions"]'), $('[data-form="Armaz03"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
