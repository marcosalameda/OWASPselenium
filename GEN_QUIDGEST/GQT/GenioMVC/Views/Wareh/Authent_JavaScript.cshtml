@model GenioMVC.ViewModels.Wareh.Authent_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS AUTHENT]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Authent !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Authent = new QForm($('[data-form="Authent"]'), "Form_Authent");
		}
		window.Form_Authent.baseArea = "wareh";
		window.Form_Authent.FormMode = '@Model.GetFormMode';
		window.Form_Authent.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Authent.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Wareh")',
			GetDependants:		'@Url.Action("GetDependants", "Wareh")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Authent", "Wareh")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Authent", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Wareh", "Wareh")',
		};
		window.Form_Authent.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Authent.formSaveEdit = '@Url.Action("Authent_SaveEdit", "Wareh")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Authent.formAction = 'Authent_New';
		window.Form_Authent.formRedirectURL = '@Url.Action("Authent_New_Redirect", "Wareh")';
		window.Form_Authent.formSubmitURL = '@Url.Action("Authent_New", "Wareh")';
		window.Form_Authent.formRedirectURL_repeatInsertion = '@Url.Action("Authent_New_Redirect", "Wareh", new { repeatInsertion = true})';
		window.Form_Authent.formSubmitURL_repeatInsertion = '@Url.Action("Authent_New", "Wareh", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Authent.formAction = 'Authent_Edit';
		window.Form_Authent.formRedirectURL = '@Url.Action("Authent_Edit_Redirect", "Wareh")';
		window.Form_Authent.formSubmitURL = '@Url.Action("Authent_Edit", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Authent.formAction = 'Authent_Duplicate';
		window.Form_Authent.formRedirectURL = '@Url.Action("Authent_Duplicate_Redirect", "Wareh")';
		window.Form_Authent.formSubmitURL = '@Url.Action("Authent_Duplicate", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Authent.formAction = 'Authent_Delete';
		window.Form_Authent.formRedirectURL = '@Url.Action("Authent_Delete_Redirect", "Wareh")';
		window.Form_Authent.formSubmitURL = '@Url.Action("Authent_Delete", "Wareh")';
	</text>
}
		window.Form_Authent.DeclareControls = function () {
			@*Form controls*@



			this.Controls.LED_AUTHENT_WAREHCODWAREH = new QHiddenControl($('[data-identifier="LED_AUTHENT_WAREHCODWAREH"]', this.element), window.Form_Authent);

	@* Hidden Required Form Fields Controls for Formulas *@
			this.Controls.LED_AUTHENT_WAREHWAREHDES = new QSpecialControl($('[data-identifier="LED_AUTHENT_WAREHWAREHDES"]', this.element), window.Form_Authent);
			this.Controls.LED_AUTHENT_WAREHWAREHDES.InitData(@JavaScriptConversion.ToString(@Model.ValWarehdes));

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Authent.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Authent.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI AUTHENT]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Authent.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS AUTHENT]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Authent.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS AUTHENT]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Authent.Data.RelationKeysSelector = {
		};

		window.Form_Authent.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Authent.Data.RelationKeys = {
			get formContext() { return window.Form_Authent; },
		};
	</text>
}

	window.Form_Authent.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Authent.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitAuthent(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Authent.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteAuthent(event) {
			event.preventDefault();
			window.Form_Authent.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS AUTHENT]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Authent"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
