@model GenioMVC.ViewModels.Wareh.Btnsform_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS BTNSFORM]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Btnsform !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Btnsform = new QForm($('[data-form="Btnsform"]'), "Form_Btnsform");
		}
		window.Form_Btnsform.baseArea = "wareh";
		window.Form_Btnsform.FormMode = '@Model.GetFormMode';
		window.Form_Btnsform.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Btnsform.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Wareh")',
			GetDependants:		'@Url.Action("GetDependants", "Wareh")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Btnsform", "Wareh")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Btnsform", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Wareh", "Wareh")',
		};
		window.Form_Btnsform.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Btnsform.formSaveEdit = '@Url.Action("Btnsform_SaveEdit", "Wareh")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Btnsform.formAction = 'Btnsform_New';
		window.Form_Btnsform.formRedirectURL = '@Url.Action("Btnsform_New_Redirect", "Wareh")';
		window.Form_Btnsform.formSubmitURL = '@Url.Action("Btnsform_New", "Wareh")';
		window.Form_Btnsform.formRedirectURL_repeatInsertion = '@Url.Action("Btnsform_New_Redirect", "Wareh", new { repeatInsertion = true})';
		window.Form_Btnsform.formSubmitURL_repeatInsertion = '@Url.Action("Btnsform_New", "Wareh", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Btnsform.formAction = 'Btnsform_Edit';
		window.Form_Btnsform.formRedirectURL = '@Url.Action("Btnsform_Edit_Redirect", "Wareh")';
		window.Form_Btnsform.formSubmitURL = '@Url.Action("Btnsform_Edit", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Btnsform.formAction = 'Btnsform_Duplicate';
		window.Form_Btnsform.formRedirectURL = '@Url.Action("Btnsform_Duplicate_Redirect", "Wareh")';
		window.Form_Btnsform.formSubmitURL = '@Url.Action("Btnsform_Duplicate", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Btnsform.formAction = 'Btnsform_Delete';
		window.Form_Btnsform.formRedirectURL = '@Url.Action("Btnsform_Delete_Redirect", "Wareh")';
		window.Form_Btnsform.formSubmitURL = '@Url.Action("Btnsform_Delete", "Wareh")';
	</text>
}
		window.Form_Btnsform.DeclareControls = function () {
			@*Form controls*@



			this.Controls.LED_BTNSFORMWAREHCODWAREH = new QHiddenControl($('[data-identifier="LED_BTNSFORMWAREHCODWAREH"]', this.element), window.Form_Btnsform);

	@* Hidden Required Form Fields Controls for Formulas *@
			this.Controls.LED_BTNSFORMWAREHWAREHDES = new QSpecialControl($('[data-identifier="LED_BTNSFORMWAREHWAREHDES"]', this.element), window.Form_Btnsform);
			this.Controls.LED_BTNSFORMWAREHWAREHDES.InitData(@JavaScriptConversion.ToString(@Model.ValWarehdes));

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Btnsform.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Btnsform.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI BTNSFORM]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Btnsform.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS BTNSFORM]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Btnsform.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS BTNSFORM]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Btnsform.Data.RelationKeysSelector = {
		};

		window.Form_Btnsform.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Btnsform.Data.RelationKeys = {
			get formContext() { return window.Form_Btnsform; },
		};
	</text>
}

	window.Form_Btnsform.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Btnsform.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitBtnsform(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Btnsform.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteBtnsform(event) {
			event.preventDefault();
			window.Form_Btnsform.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS BTNSFORM]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Btnsform"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
