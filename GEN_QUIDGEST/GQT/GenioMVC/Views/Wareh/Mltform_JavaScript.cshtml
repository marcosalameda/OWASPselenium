@model GenioMVC.ViewModels.Wareh.Mltform_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS MLTFORM]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Mltform !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Mltform = new QForm($('[data-form="Mltform"]'), "Form_Mltform");
		}
		window.Form_Mltform.baseArea = "wareh";
		window.Form_Mltform.FormMode = '@Model.GetFormMode';
		window.Form_Mltform.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Mltform.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Wareh")',
			GetDependants:		'@Url.Action("GetDependants", "Wareh")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Mltform", "Wareh")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Mltform", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Wareh", "Wareh")',
		};
		window.Form_Mltform.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Mltform.formSaveEdit = '@Url.Action("Mltform_SaveEdit", "Wareh")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Mltform.formAction = 'Mltform_New';
		window.Form_Mltform.formRedirectURL = '@Url.Action("Mltform_New_Redirect", "Wareh")';
		window.Form_Mltform.formSubmitURL = '@Url.Action("Mltform_New", "Wareh")';
		window.Form_Mltform.formRedirectURL_repeatInsertion = '@Url.Action("Mltform_New_Redirect", "Wareh", new { repeatInsertion = true})';
		window.Form_Mltform.formSubmitURL_repeatInsertion = '@Url.Action("Mltform_New", "Wareh", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Mltform.formAction = 'Mltform_Edit';
		window.Form_Mltform.formRedirectURL = '@Url.Action("Mltform_Edit_Redirect", "Wareh")';
		window.Form_Mltform.formSubmitURL = '@Url.Action("Mltform_Edit", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Mltform.formAction = 'Mltform_Duplicate';
		window.Form_Mltform.formRedirectURL = '@Url.Action("Mltform_Duplicate_Redirect", "Wareh")';
		window.Form_Mltform.formSubmitURL = '@Url.Action("Mltform_Duplicate", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Mltform.formAction = 'Mltform_Delete';
		window.Form_Mltform.formRedirectURL = '@Url.Action("Mltform_Delete_Redirect", "Wareh")';
		window.Form_Mltform.formSubmitURL = '@Url.Action("Mltform_Delete", "Wareh")';
	</text>
}
		window.Form_Mltform.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_MLTFORM_WAREHWAREHDES = new QTextControl($('[data-identifier="LED_MLTFORM_WAREHWAREHDES"]', this.element), window.Form_Mltform);

			this.Controls.LED_MLTFORM_WAREHWAREHCOD = new QTextControl($('[data-identifier="LED_MLTFORM_WAREHWAREHCOD"]', this.element), window.Form_Mltform);

			this.Controls.IFF_MLTFORM_PSEUDMLTFORM1 = new QGenericControl($('[data-identifier="IFF_MLTFORM_PSEUDMLTFORM1"]', this.element), window.Form_Mltform);




			this.Controls.LED_MLTFORM_WAREHCODWAREH = new QHiddenControl($('[data-identifier="LED_MLTFORM_WAREHCODWAREH"]', this.element), window.Form_Mltform);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Mltform.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Mltform.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI MLTFORM]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Mltform.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS MLTFORM]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Mltform.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS MLTFORM]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Mltform.Data.RelationKeysSelector = {
		};

		window.Form_Mltform.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Mltform.Data.RelationKeys = {
			get formContext() { return window.Form_Mltform; },
		};
	</text>
}

	window.Form_Mltform.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Mltform.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitMltform(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Mltform.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteMltform(event) {
			event.preventDefault();
			window.Form_Mltform.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS MLTFORM]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Mltform"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
