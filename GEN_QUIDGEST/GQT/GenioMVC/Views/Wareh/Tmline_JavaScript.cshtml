@model GenioMVC.ViewModels.Wareh.Tmline_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS TMLINE]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Tmline !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Tmline = new QForm($('[data-form="Tmline"]'), "Form_Tmline");
		}
		window.Form_Tmline.baseArea = "wareh";
		window.Form_Tmline.FormMode = '@Model.GetFormMode';
		window.Form_Tmline.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Tmline.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Wareh")',
			GetDependants:		'@Url.Action("GetDependants", "Wareh")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Tmline", "Wareh")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Tmline", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Wareh", "Wareh")',
		};
		window.Form_Tmline.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tmline.formSaveEdit = '@Url.Action("Tmline_SaveEdit", "Wareh")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Tmline.formAction = 'Tmline_New';
		window.Form_Tmline.formRedirectURL = '@Url.Action("Tmline_New_Redirect", "Wareh")';
		window.Form_Tmline.formSubmitURL = '@Url.Action("Tmline_New", "Wareh")';
		window.Form_Tmline.formRedirectURL_repeatInsertion = '@Url.Action("Tmline_New_Redirect", "Wareh", new { repeatInsertion = true})';
		window.Form_Tmline.formSubmitURL_repeatInsertion = '@Url.Action("Tmline_New", "Wareh", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Tmline.formAction = 'Tmline_Edit';
		window.Form_Tmline.formRedirectURL = '@Url.Action("Tmline_Edit_Redirect", "Wareh")';
		window.Form_Tmline.formSubmitURL = '@Url.Action("Tmline_Edit", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tmline.formAction = 'Tmline_Duplicate';
		window.Form_Tmline.formRedirectURL = '@Url.Action("Tmline_Duplicate_Redirect", "Wareh")';
		window.Form_Tmline.formSubmitURL = '@Url.Action("Tmline_Duplicate", "Wareh")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Tmline.formAction = 'Tmline_Delete';
		window.Form_Tmline.formRedirectURL = '@Url.Action("Tmline_Delete_Redirect", "Wareh")';
		window.Form_Tmline.formSubmitURL = '@Url.Action("Tmline_Delete", "Wareh")';
	</text>
}
		window.Form_Tmline.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_TMLINE__PSEUDTMDSAID_ = new QTimeLineControl($('[data-identifier="IFF_TMLINE__PSEUDTMDSAID_"]', this.element), window.Form_Tmline);




			this.Controls.LED_TMLINE__WAREHCODWAREH = new QHiddenControl($('[data-identifier="LED_TMLINE__WAREHCODWAREH"]', this.element), window.Form_Tmline);

	@* Hidden Required Form Fields Controls for Formulas *@
			this.Controls.LED_TMLINE__WAREHWAREHDES = new QSpecialControl($('[data-identifier="LED_TMLINE__WAREHWAREHDES"]', this.element), window.Form_Tmline);
			this.Controls.LED_TMLINE__WAREHWAREHDES.InitData(@JavaScriptConversion.ToString(@Model.ValWarehdes));

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Tmline.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Tmline.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI TMLINE]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Tmline.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS TMLINE]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Tmline.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS TMLINE]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Tmline.Data.RelationKeysSelector = {
		};

		window.Form_Tmline.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Tmline.Data.RelationKeys = {
			get formContext() { return window.Form_Tmline; },
		};
	</text>
}

	window.Form_Tmline.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Tmline.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitTmline(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Tmline.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteTmline(event) {
			event.preventDefault();
			window.Form_Tmline.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS TMLINE]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Tmline"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
