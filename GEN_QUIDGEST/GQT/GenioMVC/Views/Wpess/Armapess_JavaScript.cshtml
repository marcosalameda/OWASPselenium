@model GenioMVC.ViewModels.Wpess.Armapess_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ARMAPESS]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Armapess !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Armapess = new QForm($('[data-form="Armapess"]'), "Form_Armapess");
		}
		window.Form_Armapess.baseArea = "wpess";
		window.Form_Armapess.FormMode = '@Model.GetFormMode';
		window.Form_Armapess.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Armapess.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Wpess")',
			GetDependants:		'@Url.Action("GetDependants", "Wpess")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Armapess", "Wpess")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Armapess", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Wpess", "Wpess")',
		};
		window.Form_Armapess.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Armapess.formSaveEdit = '@Url.Action("Armapess_SaveEdit", "Wpess")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Armapess.formAction = 'Armapess_New';
		window.Form_Armapess.formRedirectURL = '@Url.Action("Armapess_New_Redirect", "Wpess")';
		window.Form_Armapess.formSubmitURL = '@Url.Action("Armapess_New", "Wpess")';
		window.Form_Armapess.formRedirectURL_repeatInsertion = '@Url.Action("Armapess_New_Redirect", "Wpess", new { repeatInsertion = true})';
		window.Form_Armapess.formSubmitURL_repeatInsertion = '@Url.Action("Armapess_New", "Wpess", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Armapess.formAction = 'Armapess_Edit';
		window.Form_Armapess.formRedirectURL = '@Url.Action("Armapess_Edit_Redirect", "Wpess")';
		window.Form_Armapess.formSubmitURL = '@Url.Action("Armapess_Edit", "Wpess")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Armapess.formAction = 'Armapess_Duplicate';
		window.Form_Armapess.formRedirectURL = '@Url.Action("Armapess_Duplicate_Redirect", "Wpess")';
		window.Form_Armapess.formSubmitURL = '@Url.Action("Armapess_Duplicate", "Wpess")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Armapess.formAction = 'Armapess_Delete';
		window.Form_Armapess.formRedirectURL = '@Url.Action("Armapess_Delete_Redirect", "Wpess")';
		window.Form_Armapess.formSubmitURL = '@Url.Action("Armapess_Delete", "Wpess")';
	</text>
}
		window.Form_Armapess.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_ARMAPESSWPESSNFUNC___ = new QNumericControl($('[data-identifier="LED_ARMAPESSWPESSNFUNC___"]', this.element), window.Form_Armapess);

			this.Controls.LED_ARMAPESSWPESSPFOTO___ = new QImageControl($('[data-identifier="LED_ARMAPESSWPESSPFOTO___"]', this.element), window.Form_Armapess, '@Html.Raw(Url.Action("ImageDelete", "Wpess", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Wpess", "ValPfoto", "", Model.ValCodpess, null) }))', '@Html.Raw(Url.Action("ImageCropper", "Wpess", new { ticket = Helpers.GetFileTicket(GenioMVC.Models.Navigation.UserContext.Current.User, "Wpess", "ValPfoto", "", Model.ValCodpess, null), identifier= "FARMAPESS", FormName= "Armapess", FieldId= "LED_ARMAPESSWPESSPFOTO___" }))',@Html.IsImageEmpty(Model.ValPfoto));

			this.Controls.LED_ARMAPESSWPESSNAME____ = new QTextControl($('[data-identifier="LED_ARMAPESSWPESSNAME____"]', this.element), window.Form_Armapess);

			this.Controls.LED_ARMAPESSWPESSDATE____ = new QDateControl($('[data-identifier="LED_ARMAPESSWPESSDATE____"]', this.element), window.Form_Armapess);

			this.Controls.LED_ARMAPESSWPESSSEX_____ = new QArrayControl($('[data-identifier="LED_ARMAPESSWPESSSEX_____"]', this.element), window.Form_Armapess);

			this.Controls.LED_ARMAPESSWPESSNATURALI = new QTextControl($('[data-identifier="LED_ARMAPESSWPESSNATURALI"]', this.element), window.Form_Armapess);

			this.Controls.LED_ARMAPESSWPESSNACIONAL = new QTextControl($('[data-identifier="LED_ARMAPESSWPESSNACIONAL"]', this.element), window.Form_Armapess);

			this.Controls.LED_ARMAPESSWPESSADRESS__ = new QTextControl($('[data-identifier="LED_ARMAPESSWPESSADRESS__"]', this.element), window.Form_Armapess);

			this.Controls.LED_ARMAPESSWPESSZIPCODE_ = new QTextControl($('[data-identifier="LED_ARMAPESSWPESSZIPCODE_"]', this.element), window.Form_Armapess);

			this.Controls.LED_ARMAPESSWPESSCOUNTRY_ = new QTextControl($('[data-identifier="LED_ARMAPESSWPESSCOUNTRY_"]', this.element), window.Form_Armapess);

			this.Controls.LED_ARMAPESSWPESSEMAIL___ = new QTextControl($('[data-identifier="LED_ARMAPESSWPESSEMAIL___"]', this.element), window.Form_Armapess);

			this.Controls.LED_ARMAPESSWPESSCELLPHON = new QNumericControl($('[data-identifier="LED_ARMAPESSWPESSCELLPHON"]', this.element), window.Form_Armapess);

			this.Controls.IFF_ARMAPESSWAREHWAREHDES = new QDbeditControl($('[data-identifier="IFF_ARMAPESSWAREHWAREHDES"]', this.element), window.Form_Armapess);




			this.Controls.LED_ARMAPESSWPESSCODPESS_ = new QHiddenControl($('[data-identifier="LED_ARMAPESSWPESSCODPESS_"]', this.element), window.Form_Armapess);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Armapess.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Armapess.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ARMAPESS]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Armapess.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ARMAPESS]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Armapess.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ARMAPESS]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Armapess.Data.RelationKeysSelector = {
				get wareh() { return $(':not([elem-identifier="Multiform"]) > #ValCodwareh[pers-cs-area="Wareh"]', $('[data-form="Armapess"]')); /* DBEdit */ },
		};

		window.Form_Armapess.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Armapess.Data.RelationKeys = {
			get formContext() { return window.Form_Armapess; },
			get wareh() { return (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value; /* DBEdit */ },
			set wareh(newValue) { (this.formContext.Data.RelationKeysSelector.wareh.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Armapess.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Armapess.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitArmapess(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Armapess.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteArmapess(event) {
			event.preventDefault();
			window.Form_Armapess.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS ARMAPESS]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Armapess"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
