@model GenioMVC.ViewModels.Anexd.Anexd_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ANEXD]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Anexd !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Anexd = new QForm($('[data-form="Anexd"]'), "Form_Anexd");
		}
		window.Form_Anexd.baseArea = "anexd";
		window.Form_Anexd.FormMode = '@Model.GetFormMode';
		window.Form_Anexd.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Anexd.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Anexd")',
			GetDependants:		'@Url.Action("GetDependants", "Anexd")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Anexd", "Anexd")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Anexd", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Anexd", "Anexd")',
		};
		window.Form_Anexd.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Anexd.formSaveEdit = '@Url.Action("Anexd_SaveEdit", "Anexd")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Anexd.formAction = 'Anexd_New';
		window.Form_Anexd.formRedirectURL = '@Url.Action("Anexd_New_Redirect", "Anexd")';
		window.Form_Anexd.formSubmitURL = '@Url.Action("Anexd_New", "Anexd")';
		window.Form_Anexd.formRedirectURL_repeatInsertion = '@Url.Action("Anexd_New_Redirect", "Anexd", new { repeatInsertion = true})';
		window.Form_Anexd.formSubmitURL_repeatInsertion = '@Url.Action("Anexd_New", "Anexd", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Anexd.formAction = 'Anexd_Edit';
		window.Form_Anexd.formRedirectURL = '@Url.Action("Anexd_Edit_Redirect", "Anexd")';
		window.Form_Anexd.formSubmitURL = '@Url.Action("Anexd_Edit", "Anexd")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Anexd.formAction = 'Anexd_Duplicate';
		window.Form_Anexd.formRedirectURL = '@Url.Action("Anexd_Duplicate_Redirect", "Anexd")';
		window.Form_Anexd.formSubmitURL = '@Url.Action("Anexd_Duplicate", "Anexd")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Anexd.formAction = 'Anexd_Delete';
		window.Form_Anexd.formRedirectURL = '@Url.Action("Anexd_Delete_Redirect", "Anexd")';
		window.Form_Anexd.formSubmitURL = '@Url.Action("Anexd_Delete", "Anexd")';
	</text>
}
		window.Form_Anexd.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_ANEXD___EQUIPREGISTNR = new QDbeditControl($('[data-identifier="IFF_ANEXD___EQUIPREGISTNR"]', this.element), window.Form_Anexd);

			this.Controls.LED_ANEXD___ANEXDDTHRANEX = new QDateControl($('[data-identifier="LED_ANEXD___ANEXDDTHRANEX"]', this.element), window.Form_Anexd);

			this.Controls.LED_ANEXD___ANEXDREFERENC = new QTextControl($('[data-identifier="LED_ANEXD___ANEXDREFERENC"]', this.element), window.Form_Anexd);

			this.Controls.LED_ANEXD___ANEXDTITLE___ = new QTextControl($('[data-identifier="LED_ANEXD___ANEXDTITLE___"]', this.element), window.Form_Anexd);

			this.Controls.IFF_ANEXD___LANGULANGUA__ = new QDbeditControl($('[data-identifier="IFF_ANEXD___LANGULANGUA__"]', this.element), window.Form_Anexd);

			this.Controls.LED_ANEXD___ANEXDTITTRADU = new QTextControl($('[data-identifier="LED_ANEXD___ANEXDTITTRADU"]', this.element), window.Form_Anexd);

			this.Controls.LED_ANEXD___ANEXDDOCUMENT = new QTextControl($('[data-identifier="LED_ANEXD___ANEXDDOCUMENT"]', this.element), window.Form_Anexd);




			this.Controls.LED_ANEXD___ANEXDCODANEXD = new QHiddenControl($('[data-identifier="LED_ANEXD___ANEXDCODANEXD"]', this.element), window.Form_Anexd);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Anexd.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Anexd.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ANEXD]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Anexd.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ANEXD]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Anexd.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ANEXD]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Anexd.Data.RelationKeysSelector = {
				get equip() { return $(':not([elem-identifier="Multiform"]) > #ValCodequip[pers-cs-area="Equip"]', $('[data-form="Anexd"]')); /* DBEdit */ },
				get langu() { return $(':not([elem-identifier="Multiform"]) > #ValCodlang[pers-cs-area="Langu"]', $('[data-form="Anexd"]')); /* DBEdit */ },
		};

		window.Form_Anexd.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Anexd.Data.RelationKeys = {
			get formContext() { return window.Form_Anexd; },
			get equip() { return (this.formContext.Data.RelationKeysSelector.equip.getQControl() || {}).Value; /* DBEdit */ },
			set equip(newValue) { (this.formContext.Data.RelationKeysSelector.equip.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get langu() { return (this.formContext.Data.RelationKeysSelector.langu.getQControl() || {}).Value; /* DBEdit */ },
			set langu(newValue) { (this.formContext.Data.RelationKeysSelector.langu.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Anexd.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>

		@* Recalculate formulas (server side) *@
		$(qForm).bind('VALTITLE_CHANGE VALCODLANG_CHANGE', function(event, eTarget) {
				window.Form_Anexd.RecalculateFormulas();
		});
		





	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Anexd.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitAnexd(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Anexd.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteAnexd(event) {
			event.preventDefault();
			window.Form_Anexd.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS ANEXD]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Anexd"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
