@model GenioMVC.ViewModels.Cmpny.Empre_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS EMPRE]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Empre !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Empre = new QForm($('[data-form="Empre"]'), "Form_Empre");
		}
		window.Form_Empre.baseArea = "cmpny";
		window.Form_Empre.FormMode = '@Model.GetFormMode';
		window.Form_Empre.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Empre.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Cmpny")',
			GetDependants:		'@Url.Action("GetDependants", "Cmpny")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Empre", "Cmpny")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Empre", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Cmpny", "Cmpny")',
		};
		window.Form_Empre.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Empre.formSaveEdit = '@Url.Action("Empre_SaveEdit", "Cmpny")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Empre.formAction = 'Empre_New';
		window.Form_Empre.formRedirectURL = '@Url.Action("Empre_New_Redirect", "Cmpny")';
		window.Form_Empre.formSubmitURL = '@Url.Action("Empre_New", "Cmpny")';
		window.Form_Empre.formRedirectURL_repeatInsertion = '@Url.Action("Empre_New_Redirect", "Cmpny", new { repeatInsertion = true})';
		window.Form_Empre.formSubmitURL_repeatInsertion = '@Url.Action("Empre_New", "Cmpny", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Empre.formAction = 'Empre_Edit';
		window.Form_Empre.formRedirectURL = '@Url.Action("Empre_Edit_Redirect", "Cmpny")';
		window.Form_Empre.formSubmitURL = '@Url.Action("Empre_Edit", "Cmpny")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Empre.formAction = 'Empre_Duplicate';
		window.Form_Empre.formRedirectURL = '@Url.Action("Empre_Duplicate_Redirect", "Cmpny")';
		window.Form_Empre.formSubmitURL = '@Url.Action("Empre_Duplicate", "Cmpny")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Empre.formAction = 'Empre_Delete';
		window.Form_Empre.formRedirectURL = '@Url.Action("Empre_Delete_Redirect", "Cmpny")';
		window.Form_Empre.formSubmitURL = '@Url.Action("Empre_Delete", "Cmpny")';
	</text>
}
		window.Form_Empre.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_EMPRE___PSEUDNOVOGR02 = new QGenericControl($('[data-identifier="IFF_EMPRE___PSEUDNOVOGR02"]', this.element), window.Form_Empre);

			this.Controls.LED_EMPRE___CMPNYLOGO____ = new QImageControl($('[data-identifier="LED_EMPRE___CMPNYLOGO____"]', this.element), window.Form_Empre, '@Html.Raw(Url.Action("ImageDelete", "Cmpny", new { id=Model.ValCodempre, modelname="Cmpny", fldname="ValLogo" }))', '@Html.Raw(Url.Action("ImageCropper", "Cmpny", new { key=Model.ValCodempre, modelname= "Cmpny", fldname= "ValLogo", identifier= "FEMPRE", FormName= "Empre", FieldId= "LED_EMPRE___CMPNYLOGO____" }))',@Html.IsImageEmpty(Model.ValLogo));

			this.Controls.IFF_EMPRE___PSEUDNOVOGR01 = new QGenericControl($('[data-identifier="IFF_EMPRE___PSEUDNOVOGR01"]', this.element), window.Form_Empre);

			this.Controls.LED_EMPRE___CMPNYDESIGNAT = new QTextControl($('[data-identifier="LED_EMPRE___CMPNYDESIGNAT"]', this.element), window.Form_Empre);

			this.Controls.LED_EMPRE___CMPNYACRONYM_ = new QTextControl($('[data-identifier="LED_EMPRE___CMPNYACRONYM_"]', this.element), window.Form_Empre);

			this.Controls.LED_EMPRE___CMPNYNIF_____ = new QTextControl($('[data-identifier="LED_EMPRE___CMPNYNIF_____"]', this.element), window.Form_Empre);

			this.Controls.LED_EMPRE___CMPNYTELEPHON = new QTextControl($('[data-identifier="LED_EMPRE___CMPNYTELEPHON"]', this.element), window.Form_Empre);

			this.Controls.LED_EMPRE___CMPNYEMAIL___ = new QTextControl($('[data-identifier="LED_EMPRE___CMPNYEMAIL___"]', this.element), window.Form_Empre);

			this.Controls.IFF_EMPRE___PSEUDNOVOGR03 = new QGenericControl($('[data-identifier="IFF_EMPRE___PSEUDNOVOGR03"]', this.element), window.Form_Empre);

			this.Controls.IFF_EMPRE___CNTRYCOUNTRY_ = new QDbeditControl($('[data-identifier="IFF_EMPRE___CNTRYCOUNTRY_"]', this.element), window.Form_Empre);

			this.Controls.LED_EMPRE___CMPNYQTDPESSO = new QNumericControl($('[data-identifier="LED_EMPRE___CMPNYQTDPESSO"]', this.element), window.Form_Empre);

			this.Controls.LED_EMPRE___CMPNYHEADLOC_ = new QTextControl($('[data-identifier="LED_EMPRE___CMPNYHEADLOC_"]', this.element), window.Form_Empre);




			this.Controls.LED_EMPRE___CMPNYCODEMPRE = new QHiddenControl($('[data-identifier="LED_EMPRE___CMPNYCODEMPRE"]', this.element), window.Form_Empre);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Empre.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Empre.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI EMPRE]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Empre.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS EMPRE]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Empre.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS EMPRE]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Empre.Data.RelationKeysSelector = {
				get cntry() { return $(':not([elem-identifier="Multiform"]) > #ValCodcntry[pers-cs-area="Cntry"]', $('[data-form="Empre"]')); /* DBEdit */ },
		};

		window.Form_Empre.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Empre.Data.RelationKeys = {
			get formContext() { return window.Form_Empre; },
			get cntry() { return (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value; /* DBEdit */ },
			set cntry(newValue) { (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Empre.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Empre.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitEmpre(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Empre.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteEmpre(event) {
			event.preventDefault();
			window.Form_Empre.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
    modal_QuidgestGlobals = {
        UrlAction: {
            newWindow: '@Url.Action("newWindow")',
            ReloadDBEdit: '@Url.Action("ReloadDBEdit")',
            formArea: '	@Url.Action("form", "area")',
            UpdateValues: '@Url.Action("UpdateValues")',
            LogJavaScriptError: '@Url.Action("LogJavaScriptError", "ApplicationErrors")',
            syncFormKeys: '@Url.Action("syncFormKeys")',
            GetDependants: 		'@Url.Action("GetDependants")',
            ImageHandlerPut:		'@Url.Action("ImageHandlerPut")',
            ImageHandlerGet:		'@Url.Action("ImageHandlerGet")'
        }
    };
$(document).ready(function () {
	loaded($("#form-modal"));
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
	var ajaxCallsRemaining = $("#form-modal .flash object embed").length;
	$('#form-modal form [elem-identifier="ButtonPrimary"]').click(function() {
		var flashObjects = $("#form-modal .flash object embed");
		ajaxCallsRemaining = flashObjects.length;
		if(flashObjects.length == 0)
			return true;
		else {
			flashObjects.each(function() {
			   var flashObj = GetFlash($(this).attr('id'))
			   flashObj.UsedFlash.TriggerSave(flashObj);
			});
			return false;
		}
	});
    </text>
}
});
// USE /[MANUAL GQT FORM_CODEJS EMPRE]/



 

    $($('[elem-identifier="Actions"]'), $('[data-form="Empre"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
