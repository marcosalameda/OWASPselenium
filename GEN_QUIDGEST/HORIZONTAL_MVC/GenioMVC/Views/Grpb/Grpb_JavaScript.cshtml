@model GenioMVC.ViewModels.Grpb.Grpb_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS GRPB]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Grpb !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Grpb = new QForm($('[data-form="Grpb"]'), "Form_Grpb");
		}
		window.Form_Grpb.baseArea = "grpb";
		window.Form_Grpb.FormMode = '@Model.GetFormMode';
		window.Form_Grpb.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Grpb.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Grpb")',
			GetDependants:		'@Url.Action("GetDependants", "Grpb")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Grpb", "Grpb")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Grpb", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Grpb", "Grpb")',
		};
		window.Form_Grpb.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Grpb.formSaveEdit = '@Url.Action("Grpb_SaveEdit", "Grpb")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Grpb.formAction = 'Grpb_New';
		window.Form_Grpb.formRedirectURL = '@Url.Action("Grpb_New_Redirect", "Grpb")';
		window.Form_Grpb.formSubmitURL = '@Url.Action("Grpb_New", "Grpb")';
		window.Form_Grpb.formRedirectURL_repeatInsertion = '@Url.Action("Grpb_New_Redirect", "Grpb", new { repeatInsertion = true})';
		window.Form_Grpb.formSubmitURL_repeatInsertion = '@Url.Action("Grpb_New", "Grpb", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Grpb.formAction = 'Grpb_Edit';
		window.Form_Grpb.formRedirectURL = '@Url.Action("Grpb_Edit_Redirect", "Grpb")';
		window.Form_Grpb.formSubmitURL = '@Url.Action("Grpb_Edit", "Grpb")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Grpb.formAction = 'Grpb_Duplicate';
		window.Form_Grpb.formRedirectURL = '@Url.Action("Grpb_Duplicate_Redirect", "Grpb")';
		window.Form_Grpb.formSubmitURL = '@Url.Action("Grpb_Duplicate", "Grpb")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Grpb.formAction = 'Grpb_Delete';
		window.Form_Grpb.formRedirectURL = '@Url.Action("Grpb_Delete_Redirect", "Grpb")';
		window.Form_Grpb.formSubmitURL = '@Url.Action("Grpb_Delete", "Grpb")';
	</text>
}
		window.Form_Grpb.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_GRPB____GRPB_NAME____ = new QTextControl($('[data-identifier="LED_GRPB____GRPB_NAME____"]', this.element), window.Form_Grpb);

			this.Controls.IFF_GRPB____PSEUDTBLB____ = new QTableListControl($('[data-identifier="IFF_GRPB____PSEUDTBLB____"]', this.element), window.Form_Grpb);




			this.Controls.LED_GRPB____GRPB_CODGRPB_ = new QHiddenControl($('[data-identifier="LED_GRPB____GRPB_CODGRPB_"]', this.element), window.Form_Grpb);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Grpb.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Grpb.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI GRPB]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Grpb.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS GRPB]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Grpb.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS GRPB]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Grpb.Data.RelationKeysSelector = {
		};

		window.Form_Grpb.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Grpb.Data.RelationKeys = {
			get formContext() { return window.Form_Grpb; },
		};
	</text>
}

	window.Form_Grpb.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Grpb.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitGrpb(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Grpb.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteGrpb(event) {
			event.preventDefault();
			window.Form_Grpb.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS GRPB]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Grpb"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
