@model GenioMVC.ViewModels.Lendi.PTN_Menu_1211_ViewModel
@using Newtonsoft.Json;
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Lendi;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Lendi>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodlendi, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_PESS1_NAME]/
		column.Expression(p => p.Pess1.ValName).Title(@Resources.NAME31974).Field("Pess1.ValName").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_EQUIP_REGISTNR]/
		column.Expression(p => p.Equip.ValRegistnr).Title(@Resources.NO__REGISTER04207).Field("Equip.ValRegistnr").Size(6);
// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_PESS2_NAME]/
		column.Expression(p => p.Pess2.ValName).Title(@Resources.NAME31974).Field("Pess2.ValName").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_LENDI_LENDINNR]/
		column.Expression(p => p.ValLendinnr).Title(@Resources.NO__OF_THE_DADATO35934).Field("ValLendinnr").Size(6);
// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_LENDI_START]/
		column.Expression(p => p.ValStart).Title(@Resources.BEGINNING18124).Field("ValStart").Size(16);
// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_LENDI_WARNDT]/
		column.Expression(p => p.ValWarndt).Title(@Resources.WARNING52043).Field("ValWarndt").Size(16);
// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_LENDI_END]/
		column.Expression(p => p.ValEnd).Title(@Resources.END47577).Field("ValEnd").Size(16);
// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_LENDI_OBSERVAT]/
		column.Expression(p => p.ValObservat).Title(@Resources.OBSERVATIONS03729).Field("ValObservat").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_LENDI_RETURNDT]/
		column.Expression(p => p.ValReturndt).Title(@Resources.RETURN32222).Field("ValReturndt").Size(8).Format("{0:d}");
// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_LENDI_RETURNED]/
		column.Expression(p => p.ValReturned).Title(@Resources.RETURNED01606).Field("ValReturned").Size(1);
// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_LENDI_DAYSLIMI]/
		column.Expression(p => p.ValDayslimi).Title(@Resources.DAYS_FOR_RETURN_PERI04559).Field("ValDayslimi").Size(10);
// USE /[MANUAL GQT CUSTOM_COLUMN PTN_MENU_1211_LENDI_IFOUTDT]/
		column.Expression(p => p.ValIfoutdt).Title(@Resources.IF_OUT_OF_DATE49042).Field("ValIfoutdt").Size(1);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Lendi")
        .Id("PTN_Menu_1211")
        .AjaxRequest("Menu")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .DBEdit()
        .MultipleSelection()
        .Filters(this.Model.Menu.Filters)
        .Form("Comod", false, false)
        .AddTableAction("PTN_MenuR_DELETEONEROW", "Lendi" , (Func<GenioMVC.Models.Lendi, object>)(item => new { id = item.ValCodlendi }), "delete.svg", @Resources.DELETE_SINGLE_RECORD05929, isRoutine: true, isAjaxAction: false, htmlAttributes: new { }, accesslevel: GenioMVC.Models.Navigation.UserContext.Current.User.VerifyAccess(CSGenio.framework.Role.ROLE_1), openInPopup: false, isBootStrapIcon: false)
        .AddTableAction("PTN_MenuR_DELETEROWS", "Lendi" , (Func<GenioMVC.Models.Lendi, object>)(item => new { id = item.ValCodlendi }), "delete.svg", @Resources.DELETE_MULTIPLE_RECO17551, isRoutine: true, multipleSelection: true, isAjaxAction: false, htmlAttributes: new { }, accesslevel: GenioMVC.Models.Navigation.UserContext.Current.User.VerifyAccess(CSGenio.framework.Role.ROLE_1), openInPopup: false, isBootStrapIcon: false)
        .AddTableAction("PTN_MenuR_DELETEROWS", "Lendi" , (Func<GenioMVC.Models.Lendi, object>)(item => new { id = item.ValCodlendi }), "delete.svg", @Resources.DELETE_OPTIONAL_RECO37994, isRoutine: true, isAjaxAction: false, htmlAttributes: new { }, accesslevel: GenioMVC.Models.Navigation.UserContext.Current.User.VerifyAccess(CSGenio.framework.Role.ROLE_1), openInPopup: false, isBootStrapIcon: false)
        .Permissions(canView: false, canInsert: false, canEdit: false, canDelete: false, canDuplicate: false)
		;
        
}

@tbl.ToHtml()


<script>
 

	function PTN_MenuR_DELETEONEROW (jsonRouteValues) {

		displayMessage('@Resources.DO_YOU_WANT_TO_DELET18100', MessageDefs.StatusEnum.Q, undefined,
			[
				{
					label: '@Resources.CONTINUAR44831',
					style: MessageDefs.ButtonTypes.Primary,
					callback: function (confirmed) {
						if (confirmed) {
							PTN_MenuR_DELETEONEROW_Success (jsonRouteValues)
						}
					},
					icon: "check"
				},
				{
					label: '@Resources.CANCELAR49513',
					style: MessageDefs.ButtonTypes.Secondary,
					icon: "ban-circle"
				}
			]
		);

	}
	function PTN_MenuR_DELETEONEROW_Success (jsonRouteValues) {

		var customParameters;
// USE /[MANUAL GQT VIEW_MANUAL_ROUTINE DELETEONEROW]/

		return PTN_MenuR_DELETEONEROW_AjaxCall(jsonRouteValues, customParameters);
	}
	function PTN_MenuR_DELETEONEROW_AjaxCall (jsonRouteValues, customParameters) {
		//Check for all selected
		/*
		* This parameter will not cause harm to MB routines without multipleSelection,
		* since the controller method's signature doesn't receive this parameter,
		* thus being ignored.
		*/
		let tblId = "PTN_Menu_1211";
		let criteriaSetId = "ML1211";
		jsonRouteValues.criteriaSetId = criteriaSetId;
		jsonRouteValues.allSelected = QLocalStorage.getTableAllSelected(tblId + QUtils.NavigationId);

		jsonRouteValues.queryParams = window[tblId].GetTableFilters();

		var urlAction = '@Url.Action("PTN_MenuR_DELETEONEROW", "Lendi")';
		var jqxhr = $.ajax({
			url : urlAction,
			cache: false,
			type: "POST",
			contentType: 'application/json',
			// USE /[MANUAL GQT PARAMETERS_ROUTINE DELETEONEROW]/
			data: JSON.stringify(jsonRouteValues),
			beforeSend : function(xhr, opts) {
 			QAnimation.showPleaseWait('@Resources.PROCESSING44327');
// USE /[MANUAL GQT BEFORESEND_ROUTINE DELETEONEROW]/

			}
		})
		.done(function(data, textStatus, jqXHR) {
			QAnimation.hidePleaseWait();
// USE /[MANUAL GQT DONE_ROUTINE DELETEONEROW]/
			try {
				if (data.success == undefined || data.message == undefined)
					throw "Invalid data structure.";
				switch (data.success) {
					case 'E':
						if (typeof console != "undefined") {
							console.log("Erro na Rotina Manual 'DELETEONEROW' (" + data.message + ") ");
						}
						displayMessage(data.message, MessageDefs.StatusEnum.E, undefined, [{label: "OK", callback: () => PTN_MenuR_DELETEONEROW_AfterDone(data), icon: "check"}], {onEscapeCallback: 0});
						break;
					case 'W':
						if (typeof console != "undefined") {
							console.log("Rotina Manual 'DELETEONEROW' executada com sucesso!");
						}
						displayMessage(data.message, MessageDefs.StatusEnum.W, undefined, [{label: "OK", callback: () => PTN_MenuR_DELETEONEROW_AfterDone(data), icon: "check"}], {onEscapeCallback: 0});
						break;
					case 'OK':
						if (typeof console != "undefined") {
							console.log("Rotina Manual 'DELETEONEROW' executada com sucesso!");
						}


						if(data.message != ""){
							displayMessage(data.message, MessageDefs.StatusEnum.OK, undefined, [{label: "OK", callback: () => PTN_MenuR_DELETEONEROW_AfterDone(data), icon: "check"}], {onEscapeCallback: 0});
						}else{
							PTN_MenuR_DELETEONEROW_AfterDone(data)
						}

						break;
				}
			}
			catch (e) {
				displayMessage(quidgestGlobals.Resources.NAO_FOI_POSSIVEL_CONCLUIR, MessageDefs.StatusEnum.E);
				if (typeof QError !== "undefined") {
					var qErrorMsg = 'AFTER DONE ROUTINE "DELETEONEROW"';
					QError.AppendError(qErrorMsg, e, '@Url.Action("PTN_MenuR_DELETEONEROW", "Lendi")', 0, 0);
				}
				if (typeof console !== "undefined") {
					console.error(e.toString());
				}
			}
		})
		.fail(function(jqXHR, textStatus, errorThrown) {
			if (errorThrown != "canceled")
				displayMessage(quidgestGlobals.Resources.NAO_FOI_POSSIVEL_CONCLUIR, MessageDefs.StatusEnum.E);
		});
		return jqxhr;
	}

	function PTN_MenuR_DELETEONEROW_AfterDone (data) {
// USE /[MANUAL GQT AFTER_DONE_ROUTINE DELETEONEROW]/
	}


	@*// END ROTINA DELETEONEROW *@


	function PTN_MenuR_DELETEROWS (jsonRouteValues) {

		displayMessage('@Resources.DO_YOU_WANT_TO_DELET37828', MessageDefs.StatusEnum.Q, undefined,
			[
				{
					label: '@Resources.CONTINUAR44831',
					style: MessageDefs.ButtonTypes.Primary,
					callback: function (confirmed) {
						if (confirmed) {
							PTN_MenuR_DELETEROWS_Success (jsonRouteValues)
						}
					},
					icon: "check"
				},
				{
					label: '@Resources.CANCELAR49513',
					style: MessageDefs.ButtonTypes.Secondary,
					icon: "ban-circle"
				}
			]
		);

	}
	function PTN_MenuR_DELETEROWS_Success (jsonRouteValues) {

		var customParameters;
// USE /[MANUAL GQT VIEW_MANUAL_ROUTINE DELETEROWS]/

		return PTN_MenuR_DELETEROWS_AjaxCall(jsonRouteValues, customParameters);
	}
	function PTN_MenuR_DELETEROWS_AjaxCall (jsonRouteValues, customParameters) {
		//Check for all selected
		/*
		* This parameter will not cause harm to MB routines without multipleSelection,
		* since the controller method's signature doesn't receive this parameter,
		* thus being ignored.
		*/
		let tblId = "PTN_Menu_1211";
		let criteriaSetId = "ML1211";
		jsonRouteValues.criteriaSetId = criteriaSetId;
		jsonRouteValues.allSelected = QLocalStorage.getTableAllSelected(tblId + QUtils.NavigationId);

		jsonRouteValues.queryParams = window[tblId].GetTableFilters();

		var urlAction = '@Url.Action("PTN_MenuR_DELETEROWS", "Lendi")';
		var jqxhr = $.ajax({
			url : urlAction,
			cache: false,
			type: "POST",
			contentType: 'application/json',
			// USE /[MANUAL GQT PARAMETERS_ROUTINE DELETEROWS]/
			data: JSON.stringify(jsonRouteValues),
			beforeSend : function(xhr, opts) {
 			QAnimation.showPleaseWait('@Resources.PROCESSING44327');
// USE /[MANUAL GQT BEFORESEND_ROUTINE DELETEROWS]/

			}
		})
		.done(function(data, textStatus, jqXHR) {
			QAnimation.hidePleaseWait();
// USE /[MANUAL GQT DONE_ROUTINE DELETEROWS]/
			try {
				if (data.success == undefined || data.message == undefined)
					throw "Invalid data structure.";
				switch (data.success) {
					case 'E':
						if (typeof console != "undefined") {
							console.log("Erro na Rotina Manual 'DELETEROWS' (" + data.message + ") ");
						}
						displayMessage(data.message, MessageDefs.StatusEnum.E, undefined, [{label: "OK", callback: () => PTN_MenuR_DELETEROWS_AfterDone(data), icon: "check"}], {onEscapeCallback: 0});
						break;
					case 'W':
						if (typeof console != "undefined") {
							console.log("Rotina Manual 'DELETEROWS' executada com sucesso!");
						}
						displayMessage(data.message, MessageDefs.StatusEnum.W, undefined, [{label: "OK", callback: () => PTN_MenuR_DELETEROWS_AfterDone(data), icon: "check"}], {onEscapeCallback: 0});
						break;
					case 'OK':
						if (typeof console != "undefined") {
							console.log("Rotina Manual 'DELETEROWS' executada com sucesso!");
						}


						if(data.message != ""){
							displayMessage(data.message, MessageDefs.StatusEnum.OK, undefined, [{label: "OK", callback: () => PTN_MenuR_DELETEROWS_AfterDone(data), icon: "check"}], {onEscapeCallback: 0});
						}else{
							PTN_MenuR_DELETEROWS_AfterDone(data)
						}

						break;
				}
			}
			catch (e) {
				displayMessage(quidgestGlobals.Resources.NAO_FOI_POSSIVEL_CONCLUIR, MessageDefs.StatusEnum.E);
				if (typeof QError !== "undefined") {
					var qErrorMsg = 'AFTER DONE ROUTINE "DELETEROWS"';
					QError.AppendError(qErrorMsg, e, '@Url.Action("PTN_MenuR_DELETEROWS", "Lendi")', 0, 0);
				}
				if (typeof console !== "undefined") {
					console.error(e.toString());
				}
			}
		})
		.fail(function(jqXHR, textStatus, errorThrown) {
			if (errorThrown != "canceled")
				displayMessage(quidgestGlobals.Resources.NAO_FOI_POSSIVEL_CONCLUIR, MessageDefs.StatusEnum.E);
		});
		return jqxhr;
	}

	function PTN_MenuR_DELETEROWS_AfterDone (data) {
// USE /[MANUAL GQT AFTER_DONE_ROUTINE DELETEROWS]/
	}


	@*// END ROTINA DELETEROWS *@


	function PTN_MenuR_DELETEROWS (jsonRouteValues) {

		displayMessage('@Resources.DO_YOU_WANT_TO_EXECU27028', MessageDefs.StatusEnum.Q, undefined,
			[
				{
					label: '@Resources.CONTINUAR44831',
					style: MessageDefs.ButtonTypes.Primary,
					callback: function (confirmed) {
						if (confirmed) {
							PTN_MenuR_DELETEROWS_Success (jsonRouteValues)
						}
					},
					icon: "check"
				},
				{
					label: '@Resources.CANCELAR49513',
					style: MessageDefs.ButtonTypes.Secondary,
					icon: "ban-circle"
				}
			]
		);

	}
	function PTN_MenuR_DELETEROWS_Success (jsonRouteValues) {

		var customParameters;
// USE /[MANUAL GQT VIEW_MANUAL_ROUTINE DELETEROWS]/

		return PTN_MenuR_DELETEROWS_AjaxCall(jsonRouteValues, customParameters);
	}
	function PTN_MenuR_DELETEROWS_AjaxCall (jsonRouteValues, customParameters) {
		//Check for all selected
		/*
		* This parameter will not cause harm to MB routines without multipleSelection,
		* since the controller method's signature doesn't receive this parameter,
		* thus being ignored.
		*/
		let tblId = "PTN_Menu_1211";
		let criteriaSetId = "ML1211";
		jsonRouteValues.criteriaSetId = criteriaSetId;
		jsonRouteValues.allSelected = QLocalStorage.getTableAllSelected(tblId + QUtils.NavigationId);

		jsonRouteValues.queryParams = window[tblId].GetTableFilters();

		var urlAction = '@Url.Action("PTN_MenuR_DELETEROWS", "Lendi")';
		var jqxhr = $.ajax({
			url : urlAction,
			cache: false,
			type: "POST",
			contentType: 'application/json',
			// USE /[MANUAL GQT PARAMETERS_ROUTINE DELETEROWS]/
			data: JSON.stringify(jsonRouteValues),
			beforeSend : function(xhr, opts) {
 			QAnimation.showPleaseWait('@Resources.PROCESSING44327');
// USE /[MANUAL GQT BEFORESEND_ROUTINE DELETEROWS]/

			}
		})
		.done(function(data, textStatus, jqXHR) {
			QAnimation.hidePleaseWait();
// USE /[MANUAL GQT DONE_ROUTINE DELETEROWS]/
			try {
				if (data.success == undefined || data.message == undefined)
					throw "Invalid data structure.";
				switch (data.success) {
					case 'E':
						if (typeof console != "undefined") {
							console.log("Erro na Rotina Manual 'DELETEROWS' (" + data.message + ") ");
						}
						displayMessage(data.message, MessageDefs.StatusEnum.E, undefined, [{label: "OK", callback: () => PTN_MenuR_DELETEROWS_AfterDone(data), icon: "check"}], {onEscapeCallback: 0});
						break;
					case 'W':
						if (typeof console != "undefined") {
							console.log("Rotina Manual 'DELETEROWS' executada com sucesso!");
						}
						displayMessage(data.message, MessageDefs.StatusEnum.W, undefined, [{label: "OK", callback: () => PTN_MenuR_DELETEROWS_AfterDone(data), icon: "check"}], {onEscapeCallback: 0});
						break;
					case 'OK':
						if (typeof console != "undefined") {
							console.log("Rotina Manual 'DELETEROWS' executada com sucesso!");
						}


						if(data.message != ""){
							displayMessage(data.message, MessageDefs.StatusEnum.OK, undefined, [{label: "OK", callback: () => PTN_MenuR_DELETEROWS_AfterDone(data), icon: "check"}], {onEscapeCallback: 0});
						}else{
							PTN_MenuR_DELETEROWS_AfterDone(data)
						}

						break;
				}
			}
			catch (e) {
				displayMessage(quidgestGlobals.Resources.NAO_FOI_POSSIVEL_CONCLUIR, MessageDefs.StatusEnum.E);
				if (typeof QError !== "undefined") {
					var qErrorMsg = 'AFTER DONE ROUTINE "DELETEROWS"';
					QError.AppendError(qErrorMsg, e, '@Url.Action("PTN_MenuR_DELETEROWS", "Lendi")', 0, 0);
				}
				if (typeof console !== "undefined") {
					console.error(e.toString());
				}
			}
		})
		.fail(function(jqXHR, textStatus, errorThrown) {
			if (errorThrown != "canceled")
				displayMessage(quidgestGlobals.Resources.NAO_FOI_POSSIVEL_CONCLUIR, MessageDefs.StatusEnum.E);
		});
		return jqxhr;
	}

	function PTN_MenuR_DELETEROWS_AfterDone (data) {
// USE /[MANUAL GQT AFTER_DONE_ROUTINE DELETEROWS]/
	}


	@*// END ROTINA DELETEROWS *@

 

$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qPTN_Menu_1211, #PTN_Menu_1211_complex_filter th input, #PTN_Menu_1211_filters input").change(function() {
                    $("#pPTN_Menu_1211").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#PTN_Menu_1211_tableFilters').val(false);")
                    @Html.Raw("$('#PTN_Menu_1211_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#PTN_Menu_1211_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $("[data-form='PTN_Menu_1211']").submit();
                    @:      event.preventDefault();
                    @:      ClearSearchBox('qPTN_Menu_1211');
                    @:  }
                    @:});
                }
                @Html.Raw("$('#PTN_Menu_1211_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_PTN_Menu_1211').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });
                 @if (this.Model.Menu.Filters.FiltersValues.Count >= 1) {
                    var activeFields = Newtonsoft.Json.JsonConvert.SerializeObject(this.Model.Menu.Filters.FiltersValues);
                    @: SetActiveFilters('PTN_Menu_1211', 'LENDI', @Html.Raw(activeFields))
                  }


                setupHistoryClicks()
                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='Menu']"));
                    InitSpecialControls($('#Menu'));
                    $('#Menu').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS PTN_MENU_1211]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
// USE /[MANUAL GQT LISTING_CODEJS PTN_MENU_1211]/
</script>