@model GenioMVC.ViewModels.Sbcat.Sbcat_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS SBCAT]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Sbcat !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Sbcat = new QForm($('[data-form="Sbcat"]'), "Form_Sbcat");
		}
		window.Form_Sbcat.baseArea = "sbcat";
		window.Form_Sbcat.FormMode = '@Model.GetFormMode';
		window.Form_Sbcat.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Sbcat.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Sbcat")',
			GetDependants:		'@Url.Action("GetDependants", "Sbcat")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Sbcat", "Sbcat")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Sbcat", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Sbcat", "Sbcat")',
		};
		window.Form_Sbcat.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Sbcat.formSaveEdit = '@Url.Action("Sbcat_SaveEdit", "Sbcat")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Sbcat.formAction = 'Sbcat_New';
		window.Form_Sbcat.formRedirectURL = '@Url.Action("Sbcat_New_Redirect", "Sbcat")';
		window.Form_Sbcat.formSubmitURL = '@Url.Action("Sbcat_New", "Sbcat")';
		window.Form_Sbcat.formRedirectURL_repeatInsertion = '@Url.Action("Sbcat_New_Redirect", "Sbcat", new { repeatInsertion = true})';
		window.Form_Sbcat.formSubmitURL_repeatInsertion = '@Url.Action("Sbcat_New", "Sbcat", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Sbcat.formAction = 'Sbcat_Edit';
		window.Form_Sbcat.formRedirectURL = '@Url.Action("Sbcat_Edit_Redirect", "Sbcat")';
		window.Form_Sbcat.formSubmitURL = '@Url.Action("Sbcat_Edit", "Sbcat")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Sbcat.formAction = 'Sbcat_Duplicate';
		window.Form_Sbcat.formRedirectURL = '@Url.Action("Sbcat_Duplicate_Redirect", "Sbcat")';
		window.Form_Sbcat.formSubmitURL = '@Url.Action("Sbcat_Duplicate", "Sbcat")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Sbcat.formAction = 'Sbcat_Delete';
		window.Form_Sbcat.formRedirectURL = '@Url.Action("Sbcat_Delete_Redirect", "Sbcat")';
		window.Form_Sbcat.formSubmitURL = '@Url.Action("Sbcat_Delete", "Sbcat")';
	</text>
}
		window.Form_Sbcat.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_SBCAT___SBCATSUBCATEG = new QTextControl($('[data-identifier="LED_SBCAT___SBCATSUBCATEG"]', this.element), window.Form_Sbcat);




			this.Controls.LED_SBCAT___SBCATCODSBCAT = new QHiddenControl($('[data-identifier="LED_SBCAT___SBCATCODSBCAT"]', this.element), window.Form_Sbcat);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Sbcat.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Sbcat.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI SBCAT]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Sbcat.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS SBCAT]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Sbcat.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS SBCAT]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Sbcat.Data.RelationKeysSelector = {
		};

		window.Form_Sbcat.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Sbcat.Data.RelationKeys = {
			get formContext() { return window.Form_Sbcat; },
		};
	</text>
}

	window.Form_Sbcat.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Sbcat.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitSbcat(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Sbcat.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteSbcat(event) {
			event.preventDefault();
			window.Form_Sbcat.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS SBCAT]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Sbcat"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
