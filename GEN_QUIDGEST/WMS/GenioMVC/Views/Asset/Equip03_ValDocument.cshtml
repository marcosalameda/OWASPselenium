@model GenioMVC.ViewModels.Asset.Equip03_ValDocument_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
<div class="row">
	<div class="col">
		<div elem-identifier="List"  help-id="IFF_EQUIP03_PSEUDDOCUMENT" style="padding-top: 0.5rem;"><label class = "i-text__label table-title c-table__title" > <h2 list-header>@Resources.DOCUMENTS14470</h2 list-header> </label></div>
	</div>
</div>

@* USE /[MANUAL GQT TABLE_FOR EQUIP03_DOCUMENT]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Assma;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Assma>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodassma, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP03_DOCUMENT_ASSMA_NAME]/
		column.Expression(p => p.ValName).Title(@Resources.MANUAL_NAME60077).Field("ValName").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP03_DOCUMENT_ASSMA_DIGDOCUM]/
		column.Expression(p => p.ValDigdocum).Title(@Resources.DIGITAL_DOCUMENT59580).Field("ValDigdocum").Size(30).Document(p => Url.Action("GetFile", "Assma", new { ticket = p.GetTicketForDocField("ValDigdocum"), viewType = (int)CSGenio.framework.DocumentViewTypeMode.Print}));
// USE /[MANUAL GQT CUSTOM_COLUMN EQUIP03_DOCUMENT_ASSMA_NOTES]/
		column.Expression(p => p.ValNotes).Title(@Resources.NOTES05274).Field("ValNotes").Size(30);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Assma")
        .Id("ValDocument")
        .AjaxRequest("Equip03_ValDocument")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: false)
        .RequestLink(@Url.Action("Equip03_ValDocument", new { id = Model.ValCodasset, partialView = "Equip03_ValDocument"}))
        .Form("Assma", false, false)
        .SetFollowUp("Assma_Show", "Assma" , (Func<GenioMVC.Models.Assma, object>)(item => new { id = item.ValCodassma }), isRoutine: false, isSpecificPaths: false, isAjaxAction: false, htmlAttributes: new { }, openInPopup: false)
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@tbl.ToHtml()

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qValDocument, #ValDocument_complex_filter th input, #ValDocument_filters input").change(function() {
                    $("#pValDocument").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#ValDocument_tableFilters').val(false);")
                    @Html.Raw("$('#ValDocument_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#ValDocument_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $('#ValDocument_complex_filter button#applyComplexFilter').click();
                    @:      event.preventDefault();
                    @:  }
                    @:});
                }
                @Html.Raw("$('#ValDocument_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_ValDocument').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });


                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValDocument']"));
                    InitSpecialControls($('#ValDocument'));
                    $('#ValDocument').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS EQUIPM_VALDOCUMENT]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>