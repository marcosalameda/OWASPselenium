@model GenioMVC.ViewModels.Assma.Assma_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ASSMA]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Assma !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Assma = new QForm($('[data-form="Assma"]'), "Form_Assma");
		}
		window.Form_Assma.baseArea = "assma";
		window.Form_Assma.FormMode = '@Model.GetFormMode';
		window.Form_Assma.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Assma.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Assma")',
			GetDependants:		'@Url.Action("GetDependants", "Assma")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Assma", "Assma")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Assma", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Assma", "Assma")',
		};
		window.Form_Assma.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Assma.formSaveEdit = '@Url.Action("Assma_SaveEdit", "Assma")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Assma.formAction = 'Assma_New';
		window.Form_Assma.formRedirectURL = '@Url.Action("Assma_New_Redirect", "Assma")';
		window.Form_Assma.formSubmitURL = '@Url.Action("Assma_New", "Assma")';
		window.Form_Assma.formRedirectURL_repeatInsertion = '@Url.Action("Assma_New_Redirect", "Assma", new { repeatInsertion = true})';
		window.Form_Assma.formSubmitURL_repeatInsertion = '@Url.Action("Assma_New", "Assma", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Assma.formAction = 'Assma_Edit';
		window.Form_Assma.formRedirectURL = '@Url.Action("Assma_Edit_Redirect", "Assma")';
		window.Form_Assma.formSubmitURL = '@Url.Action("Assma_Edit", "Assma")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Assma.formAction = 'Assma_Duplicate';
		window.Form_Assma.formRedirectURL = '@Url.Action("Assma_Duplicate_Redirect", "Assma")';
		window.Form_Assma.formSubmitURL = '@Url.Action("Assma_Duplicate", "Assma")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Assma.formAction = 'Assma_Delete';
		window.Form_Assma.formRedirectURL = '@Url.Action("Assma_Delete_Redirect", "Assma")';
		window.Form_Assma.formSubmitURL = '@Url.Action("Assma_Delete", "Assma")';
	</text>
}
		window.Form_Assma.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_ASSMA___ASSETNAME____ = new QDbeditControl($('[data-identifier="IFF_ASSMA___ASSETNAME____"]', this.element), window.Form_Assma);

			this.Controls.LED_ASSMA___ASSMANAME____ = new QTextControl($('[data-identifier="LED_ASSMA___ASSMANAME____"]', this.element), window.Form_Assma);

			this.Controls.LED_ASSMA___ASSMADIGDOCUM = new QTextControl($('[data-identifier="LED_ASSMA___ASSMADIGDOCUM"]', this.element), window.Form_Assma);

			this.Controls.LED_ASSMA___ASSMANOTES___ = new QGenericControl($('[data-identifier="LED_ASSMA___ASSMANOTES___"]', this.element), window.Form_Assma);




			this.Controls.LED_ASSMA___ASSMACODASSMA = new QHiddenControl($('[data-identifier="LED_ASSMA___ASSMACODASSMA"]', this.element), window.Form_Assma);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Assma.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Assma.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ASSMA]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Assma.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ASSMA]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Assma.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ASSMA]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Assma.Data.RelationKeysSelector = {
				get asset() { return $(':not([elem-identifier="Multiform"]) > #ValCodasset[pers-cs-area="Asset"]', $('[data-form="Assma"]')); /* DBEdit */ },
		};

		window.Form_Assma.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Assma.Data.RelationKeys = {
			get formContext() { return window.Form_Assma; },
			get asset() { return (this.formContext.Data.RelationKeysSelector.asset.getQControl() || {}).Value; /* DBEdit */ },
			set asset(newValue) { (this.formContext.Data.RelationKeysSelector.asset.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Assma.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Assma.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitAssma(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Assma.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteAssma(event) {
			event.preventDefault();
			window.Form_Assma.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS ASSMA]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Assma"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
