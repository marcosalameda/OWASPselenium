@model GenioMVC.ViewModels.Attac.Attac_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ATTAC]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Attac !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Attac = new QForm($('[data-form="Attac"]'), "Form_Attac");
		}
		window.Form_Attac.baseArea = "attac";
		window.Form_Attac.FormMode = '@Model.GetFormMode';
		window.Form_Attac.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Attac.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Attac")',
			GetDependants:		'@Url.Action("GetDependants", "Attac")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Attac", "Attac")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Attac", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Attac", "Attac")',
		};
		window.Form_Attac.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Attac.formSaveEdit = '@Url.Action("Attac_SaveEdit", "Attac")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Attac.formAction = 'Attac_New';
		window.Form_Attac.formRedirectURL = '@Url.Action("Attac_New_Redirect", "Attac")';
		window.Form_Attac.formSubmitURL = '@Url.Action("Attac_New", "Attac")';
		window.Form_Attac.formRedirectURL_repeatInsertion = '@Url.Action("Attac_New_Redirect", "Attac", new { repeatInsertion = true})';
		window.Form_Attac.formSubmitURL_repeatInsertion = '@Url.Action("Attac_New", "Attac", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Attac.formAction = 'Attac_Edit';
		window.Form_Attac.formRedirectURL = '@Url.Action("Attac_Edit_Redirect", "Attac")';
		window.Form_Attac.formSubmitURL = '@Url.Action("Attac_Edit", "Attac")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Attac.formAction = 'Attac_Duplicate';
		window.Form_Attac.formRedirectURL = '@Url.Action("Attac_Duplicate_Redirect", "Attac")';
		window.Form_Attac.formSubmitURL = '@Url.Action("Attac_Duplicate", "Attac")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Attac.formAction = 'Attac_Delete';
		window.Form_Attac.formRedirectURL = '@Url.Action("Attac_Delete_Redirect", "Attac")';
		window.Form_Attac.formSubmitURL = '@Url.Action("Attac_Delete", "Attac")';
	</text>
}
		window.Form_Attac.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_ATTAC___ASSETNAME____ = new QDbeditControl($('[data-identifier="IFF_ATTAC___ASSETNAME____"]', this.element), window.Form_Attac);

			this.Controls.LED_ATTAC___ATTACATTACHED = new QDateControl($('[data-identifier="LED_ATTAC___ATTACATTACHED"]', this.element), window.Form_Attac);

			this.Controls.LED_ATTAC___ATTACNOTE____ = new QGenericControl($('[data-identifier="LED_ATTAC___ATTACNOTE____"]', this.element), window.Form_Attac);

			this.Controls.LED_ATTAC___ATTACDOCUMENT = new QTextControl($('[data-identifier="LED_ATTAC___ATTACDOCUMENT"]', this.element), window.Form_Attac);




			this.Controls.LED_ATTAC___ATTACCODATTAC = new QHiddenControl($('[data-identifier="LED_ATTAC___ATTACCODATTAC"]', this.element), window.Form_Attac);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Attac.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Attac.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ATTAC]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Attac.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ATTAC]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Attac.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ATTAC]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Attac.Data.RelationKeysSelector = {
				get asset() { return $(':not([elem-identifier="Multiform"]) > #ValCodasset[pers-cs-area="Asset"]', $('[data-form="Attac"]')); /* DBEdit */ },
		};

		window.Form_Attac.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Attac.Data.RelationKeys = {
			get formContext() { return window.Form_Attac; },
			get asset() { return (this.formContext.Data.RelationKeysSelector.asset.getQControl() || {}).Value; /* DBEdit */ },
			set asset(newValue) { (this.formContext.Data.RelationKeysSelector.asset.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Attac.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Attac.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitAttac(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Attac.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteAttac(event) {
			event.preventDefault();
			window.Form_Attac.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS ATTAC]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Attac"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
