@model GenioMVC.ViewModels.Cntry.Proppais_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS PROPPAIS]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Proppais !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Proppais = new QForm($('[data-form="Proppais"]'), "Form_Proppais");
		}
		window.Form_Proppais.baseArea = "cntry";
		window.Form_Proppais.FormMode = '@Model.GetFormMode';
		window.Form_Proppais.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Proppais.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Cntry")',
			GetDependants:		'@Url.Action("GetDependants", "Cntry")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Proppais", "Cntry")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Proppais", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Cntry", "Cntry")',
		};
		window.Form_Proppais.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Proppais.formSaveEdit = '@Url.Action("Proppais_SaveEdit", "Cntry")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Proppais.formAction = 'Proppais_New';
		window.Form_Proppais.formRedirectURL = '@Url.Action("Proppais_New_Redirect", "Cntry")';
		window.Form_Proppais.formSubmitURL = '@Url.Action("Proppais_New", "Cntry")';
		window.Form_Proppais.formRedirectURL_repeatInsertion = '@Url.Action("Proppais_New_Redirect", "Cntry", new { repeatInsertion = true})';
		window.Form_Proppais.formSubmitURL_repeatInsertion = '@Url.Action("Proppais_New", "Cntry", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Proppais.formAction = 'Proppais_Edit';
		window.Form_Proppais.formRedirectURL = '@Url.Action("Proppais_Edit_Redirect", "Cntry")';
		window.Form_Proppais.formSubmitURL = '@Url.Action("Proppais_Edit", "Cntry")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Proppais.formAction = 'Proppais_Duplicate';
		window.Form_Proppais.formRedirectURL = '@Url.Action("Proppais_Duplicate_Redirect", "Cntry")';
		window.Form_Proppais.formSubmitURL = '@Url.Action("Proppais_Duplicate", "Cntry")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Proppais.formAction = 'Proppais_Delete';
		window.Form_Proppais.formRedirectURL = '@Url.Action("Proppais_Delete_Redirect", "Cntry")';
		window.Form_Proppais.formSubmitURL = '@Url.Action("Proppais_Delete", "Cntry")';
	</text>
}
		window.Form_Proppais.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_PROPPAISPSEUDNOVOGR02 = new QGenericControl($('[data-identifier="IFF_PROPPAISPSEUDNOVOGR02"]', this.element), window.Form_Proppais);

			this.Controls.LED_PROPPAISCNTRYCOUNTRY_ = new QTextControl($('[data-identifier="LED_PROPPAISCNTRYCOUNTRY_"]', this.element), window.Form_Proppais);

			this.Controls.LED_PROPPAISCNTRYACTIVE__ = new QCheckBoxControl($('[data-identifier="LED_PROPPAISCNTRYACTIVE__"]', this.element), window.Form_Proppais);

			this.Controls.IFF_PROPPAISPSEUDNOVOGR01 = new QGenericControl($('[data-identifier="IFF_PROPPAISPSEUDNOVOGR01"]', this.element), window.Form_Proppais);

			this.Controls.LED_PROPPAISCNTRYCODIGONR = new QTextControl($('[data-identifier="LED_PROPPAISCNTRYCODIGONR"]', this.element), window.Form_Proppais);

			this.Controls.LED_PROPPAISCNTRYALFA2___ = new QTextControl($('[data-identifier="LED_PROPPAISCNTRYALFA2___"]', this.element), window.Form_Proppais);

			this.Controls.LED_PROPPAISCNTRYALFA3___ = new QTextControl($('[data-identifier="LED_PROPPAISCNTRYALFA3___"]', this.element), window.Form_Proppais);

			this.Controls.IFF_PROPPAISPSEUDPROPRIED = new QGenericControl($('[data-identifier="IFF_PROPPAISPSEUDPROPRIED"]', this.element), window.Form_Proppais);




			this.Controls.LED_PROPPAISCNTRYCODCNTRY = new QHiddenControl($('[data-identifier="LED_PROPPAISCNTRYCODCNTRY"]', this.element), window.Form_Proppais);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Proppais.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Proppais.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI PROPPAIS]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Proppais.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS PROPPAIS]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Proppais.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS PROPPAIS]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Proppais.Data.RelationKeysSelector = {
		};

		window.Form_Proppais.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Proppais.Data.RelationKeys = {
			get formContext() { return window.Form_Proppais; },
		};
	</text>
}

	window.Form_Proppais.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Proppais.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitProppais(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Proppais.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteProppais(event) {
			event.preventDefault();
			window.Form_Proppais.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS PROPPAIS]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Proppais"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
