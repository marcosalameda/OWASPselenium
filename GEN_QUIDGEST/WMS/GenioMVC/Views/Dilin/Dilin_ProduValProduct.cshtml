@model GenioMVC.ViewModels.Dilin.Dilin_ProduValProduct_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@{
    var searchStyle =  string.Empty;
}
<div class="search" @searchStyle>
    <div class="row">
        <div class="col">
            <h2 class="table-title c-table__title">@Resources.PRODUCTS34689</h2>
        </div>
        <div class="col-auto normal-view" id="Dilin_ProduValProduct_simple_filter">
            <div class="i-input-group" style="width:21rem; height: inherit;">
                @Html.TextBox("qDilin_ProduValProduct", this.Model.Menu.Query, new { @class = "i-input-group__field", @onkeydown = "ListSearchEnter(event)" })
                <div class="i-input-group--right">
                    <button type="button" elem-identifier="ListSearchButton" class="b-icon--secondary i-input-group__button--secondary" onclick="window.Dilin_ProduValProduct.Search()"><i class="glyphicons glyphicons-search i-input-group__tag-icon i-input-group__button-icon"></i></button>

                    <button id="Dilin_ProduValProduct_extra" class="b-icon--secondary i-input-group__button--secondary" type="button" onclick="changeFilters('#Dilin_ProduValProduct_complex_filter', 'Dilin_ProduValProduct_tableFilters')" title="@Resources.PESQUISA_AVANCADA06198"><span class="glyphicons glyphicons-zoom-in i-input-group__tag-icon i-input-group__button-icon"></span></button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="normal-view">
@{
    var data = Html.newTableFor<GenioMVC.Models.Produ>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCodprodu, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN DILIN_PRODUCT_PRODU_PRODUCT]/
		column.Expression(p => p.ValProduct).Title(@Resources.PRODUCT12880).Field("ValProduct").Size(85);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
			//Hidden field used in JavaScript for return selected row text value
			column.Expression(p => p.ValProduct).Field("ValProduct").AddInlineStyle("display", "none", true).UsedForFilter(false);
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Produ")
        .Id("Dilin_ProduValProduct")
        .AjaxRequest("Dilin_ProduValProduct")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: true)
        .RequestLink(@Url.Action("Dilin_ProduValProduct", new { id = Model.ValCoddilin, partialView = "Dilin_ProduValProduct"}))
        .Filters(this.Model.Menu.Filters)
        .Permissions(canView: false, canInsert: false, canEdit: false, canDelete: false, canDuplicate: false)
		;
        
}

@tbl.ToHtml()
</div>
<script>
    try {
        if (jQuery) {
            try {
                $("#qDilin_ProduValProduct, #TableProduProduct_complex_filter th input").change(function() {
                    var context = $("#TableProduProduct").parent();
                    $("#table-inputs input", context).val("");
                });

                var row = $("#Dilin_ProduValProduct table tbody tr");
                row.mouseenter(function () {
                    $(this).addClass("highlighted");
                });
                row.mouseleave(function () {
                    $(this).removeClass("highlighted");
                });

                $('td:not([elem-identifier="RowActions"])', row).click(function () {
                    var row = $(this).parent();
                    var key = row.data("key");
                    var selectList = $('[data-identifier="IFF_DILIN___PRODUPRODUCT_"]');

                    selectList.trigger("DBEditSetValue", key);

                    $('#modal-dbedit').modal('hide');
                });

                $("td a").click(function (event) {
                    event.stopPropagation()
                });
                $('#Dilin_ProduValProduct_complex_filter input').keypress(function (event) {
                    var keycode = (event.keyCode ? event.keyCode : event.which);
                    if(keycode == '13' || keycode == '10'){
                        $('#Dilin_ProduValProduct_complex_filter button#applyComplexFilter').click();
                        event.preventDefault();
                    }
                });
@if(Model.Menu.Elements.Count() == 0 && !Model.Menu.Filters.ShowTableFilters)
{
<text>
                $('#Dilin_ProduValProduct_extra').prop("disabled", true);
</text>
}


// USE /[MANUAL GQT LISTING_JS DILIN_MENU]/
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
</script>