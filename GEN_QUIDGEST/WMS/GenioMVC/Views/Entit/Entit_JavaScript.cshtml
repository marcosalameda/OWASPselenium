@model GenioMVC.ViewModels.Entit.Entit_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ENTIT]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Entit !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Entit = new QForm($('[data-form="Entit"]'), "Form_Entit");
		}
		window.Form_Entit.baseArea = "entit";
		window.Form_Entit.FormMode = '@Model.GetFormMode';
		window.Form_Entit.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Entit.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Entit")',
			GetDependants:		'@Url.Action("GetDependants", "Entit")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Entit", "Entit")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Entit", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Entit", "Entit")',
		};
		window.Form_Entit.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Entit.formSaveEdit = '@Url.Action("Entit_SaveEdit", "Entit")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Entit.formAction = 'Entit_New';
		window.Form_Entit.formRedirectURL = '@Url.Action("Entit_New_Redirect", "Entit")';
		window.Form_Entit.formSubmitURL = '@Url.Action("Entit_New", "Entit")';
		window.Form_Entit.formRedirectURL_repeatInsertion = '@Url.Action("Entit_New_Redirect", "Entit", new { repeatInsertion = true})';
		window.Form_Entit.formSubmitURL_repeatInsertion = '@Url.Action("Entit_New", "Entit", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Entit.formAction = 'Entit_Edit';
		window.Form_Entit.formRedirectURL = '@Url.Action("Entit_Edit_Redirect", "Entit")';
		window.Form_Entit.formSubmitURL = '@Url.Action("Entit_Edit", "Entit")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Entit.formAction = 'Entit_Duplicate';
		window.Form_Entit.formRedirectURL = '@Url.Action("Entit_Duplicate_Redirect", "Entit")';
		window.Form_Entit.formSubmitURL = '@Url.Action("Entit_Duplicate", "Entit")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Entit.formAction = 'Entit_Delete';
		window.Form_Entit.formRedirectURL = '@Url.Action("Entit_Delete_Redirect", "Entit")';
		window.Form_Entit.formSubmitURL = '@Url.Action("Entit_Delete", "Entit")';
	</text>
}
		window.Form_Entit.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_ENTIT___ENTITNAME____ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITNAME____"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITINITIALS = new QTextControl($('[data-identifier="LED_ENTIT___ENTITINITIALS"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITREGISTRA = new QTextControl($('[data-identifier="LED_ENTIT___ENTITREGISTRA"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITTAXNUMBE = new QTextControl($('[data-identifier="LED_ENTIT___ENTITTAXNUMBE"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITEMAIL___ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITEMAIL___"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITPHONENUM = new QTextControl($('[data-identifier="LED_ENTIT___ENTITPHONENUM"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITIBAN____ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITIBAN____"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITBUILDING = new QTextControl($('[data-identifier="LED_ENTIT___ENTITBUILDING"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITSTREET__ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITSTREET__"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITTOWN____ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITTOWN____"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITCOUNTY__ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITCOUNTY__"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITSTATE___ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITSTATE___"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITPOBOX___ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITPOBOX___"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITPOSTALCO = new QTextControl($('[data-identifier="LED_ENTIT___ENTITPOSTALCO"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITTELEPHON = new QTextControl($('[data-identifier="LED_ENTIT___ENTITTELEPHON"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITFAX_____ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITFAX_____"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITWEBSITE_ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITWEBSITE_"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITPERSON__ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITPERSON__"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITCONTACT_ = new QTextControl($('[data-identifier="LED_ENTIT___ENTITCONTACT_"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITOWNER___ = new QCheckBoxControl($('[data-identifier="LED_ENTIT___ENTITOWNER___"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITCARRIER_ = new QCheckBoxControl($('[data-identifier="LED_ENTIT___ENTITCARRIER_"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITSUPPLIER = new QCheckBoxControl($('[data-identifier="LED_ENTIT___ENTITSUPPLIER"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITMANUFACT = new QCheckBoxControl($('[data-identifier="LED_ENTIT___ENTITMANUFACT"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITFOUNDED_ = new QDateControl($('[data-identifier="LED_ENTIT___ENTITFOUNDED_"]', this.element), window.Form_Entit);

			this.Controls.IFF_ENTIT___FACI1NAME____ = new QDbeditControl($('[data-identifier="IFF_ENTIT___FACI1NAME____"]', this.element), window.Form_Entit);

			this.Controls.IFF_ENTIT___FACI2NAME____ = new QDbeditControl($('[data-identifier="IFF_ENTIT___FACI2NAME____"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITLANGUAGE = new QTextControl($('[data-identifier="LED_ENTIT___ENTITLANGUAGE"]', this.element), window.Form_Entit);

			this.Controls.LED_ENTIT___ENTITCURRENCY = new QTextControl($('[data-identifier="LED_ENTIT___ENTITCURRENCY"]', this.element), window.Form_Entit);




			this.Controls.LED_ENTIT___ENTITCODENTIT = new QHiddenControl($('[data-identifier="LED_ENTIT___ENTITCODENTIT"]', this.element), window.Form_Entit);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Entit.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Entit.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ENTIT]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Entit.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ENTIT]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Entit.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ENTIT]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Entit.Data.RelationKeysSelector = {
				get faci1() { return $(':not([elem-identifier="Multiform"]) > #ValFirstfacilitie[pers-cs-area="Faci1"]', $('[data-form="Entit"]')); /* DBEdit */ },
				get faci2() { return $(':not([elem-identifier="Multiform"]) > #ValLastfacilitie[pers-cs-area="Faci2"]', $('[data-form="Entit"]')); /* DBEdit */ },
		};

		window.Form_Entit.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Entit.Data.RelationKeys = {
			get formContext() { return window.Form_Entit; },
			get faci1() { return (this.formContext.Data.RelationKeysSelector.faci1.getQControl() || {}).Value; /* DBEdit */ },
			set faci1(newValue) { (this.formContext.Data.RelationKeysSelector.faci1.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get faci2() { return (this.formContext.Data.RelationKeysSelector.faci2.getQControl() || {}).Value; /* DBEdit */ },
			set faci2(newValue) { (this.formContext.Data.RelationKeysSelector.faci2.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Entit.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>

		@* Recalculate formulas (server side) *@
		$(qForm).bind('VALFOUNDED_CHANGE VALCODENTIT_CHANGE', function(event, eTarget) {
				window.Form_Entit.RecalculateFormulas();
		});
		





	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Entit.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitEntit(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Entit.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteEntit(event) {
			event.preventDefault();
			window.Form_Entit.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS ENTIT]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Entit"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
