@model GenioMVC.ViewModels.Entit.Entix_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS ENTIX]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Entix !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Entix = new QForm($('[data-form="Entix"]'), "Form_Entix");
		}
		window.Form_Entix.baseArea = "entit";
		window.Form_Entix.FormMode = '@Model.GetFormMode';
		window.Form_Entix.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Entix.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Entit")',
			GetDependants:		'@Url.Action("GetDependants", "Entit")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Entix", "Entit")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Entix", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Entit", "Entit")',
		};
		window.Form_Entix.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Entix.formSaveEdit = '@Url.Action("Entix_SaveEdit", "Entit")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Entix.formAction = 'Entix_New';
		window.Form_Entix.formRedirectURL = '@Url.Action("Entix_New_Redirect", "Entit")';
		window.Form_Entix.formSubmitURL = '@Url.Action("Entix_New", "Entit")';
		window.Form_Entix.formRedirectURL_repeatInsertion = '@Url.Action("Entix_New_Redirect", "Entit", new { repeatInsertion = true})';
		window.Form_Entix.formSubmitURL_repeatInsertion = '@Url.Action("Entix_New", "Entit", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Entix.formAction = 'Entix_Edit';
		window.Form_Entix.formRedirectURL = '@Url.Action("Entix_Edit_Redirect", "Entit")';
		window.Form_Entix.formSubmitURL = '@Url.Action("Entix_Edit", "Entit")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Entix.formAction = 'Entix_Duplicate';
		window.Form_Entix.formRedirectURL = '@Url.Action("Entix_Duplicate_Redirect", "Entit")';
		window.Form_Entix.formSubmitURL = '@Url.Action("Entix_Duplicate", "Entit")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Entix.formAction = 'Entix_Delete';
		window.Form_Entix.formRedirectURL = '@Url.Action("Entix_Delete_Redirect", "Entit")';
		window.Form_Entix.formSubmitURL = '@Url.Action("Entix_Delete", "Entit")';
	</text>
}
		window.Form_Entix.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_ENTIX___PSEUDNOVOGR01 = new QGenericControl($('[data-identifier="IFF_ENTIX___PSEUDNOVOGR01"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITNAME____ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITNAME____"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITFOUNDED_ = new QDateControl($('[data-identifier="LED_ENTIX___ENTITFOUNDED_"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITINITIALS = new QTextControl($('[data-identifier="LED_ENTIX___ENTITINITIALS"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITREGISTRA = new QTextControl($('[data-identifier="LED_ENTIX___ENTITREGISTRA"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITTAXNUMBE = new QTextControl($('[data-identifier="LED_ENTIX___ENTITTAXNUMBE"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITIBAN____ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITIBAN____"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITPHONENUM = new QTextControl($('[data-identifier="LED_ENTIX___ENTITPHONENUM"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITOWNER___ = new QCheckBoxControl($('[data-identifier="LED_ENTIX___ENTITOWNER___"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITCARRIER_ = new QCheckBoxControl($('[data-identifier="LED_ENTIX___ENTITCARRIER_"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITSUPPLIER = new QCheckBoxControl($('[data-identifier="LED_ENTIX___ENTITSUPPLIER"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITMANUFACT = new QCheckBoxControl($('[data-identifier="LED_ENTIX___ENTITMANUFACT"]', this.element), window.Form_Entix);

			this.Controls.IFF_ENTIX___PSEUDNOVOGR05 = new QGenericControl($('[data-identifier="IFF_ENTIX___PSEUDNOVOGR05"]', this.element), window.Form_Entix);

			this.Controls.IFF_ENTIX___PSEUDNOVOGR02 = new QGenericControl($('[data-identifier="IFF_ENTIX___PSEUDNOVOGR02"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITTELEPHON = new QTextControl($('[data-identifier="LED_ENTIX___ENTITTELEPHON"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITFAX_____ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITFAX_____"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITEMAIL___ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITEMAIL___"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITWEBSITE_ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITWEBSITE_"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITPERSON__ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITPERSON__"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITCONTACT_ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITCONTACT_"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITLANGUAGE = new QTextControl($('[data-identifier="LED_ENTIX___ENTITLANGUAGE"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITCURRENCY = new QTextControl($('[data-identifier="LED_ENTIX___ENTITCURRENCY"]', this.element), window.Form_Entix);

			this.Controls.IFF_ENTIX___PSEUDNOVOGR03 = new QGenericControl($('[data-identifier="IFF_ENTIX___PSEUDNOVOGR03"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITBUILDING = new QTextControl($('[data-identifier="LED_ENTIX___ENTITBUILDING"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITSTREET__ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITSTREET__"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITTOWN____ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITTOWN____"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITCOUNTY__ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITCOUNTY__"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITSTATE___ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITSTATE___"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITPOSTALCO = new QTextControl($('[data-identifier="LED_ENTIX___ENTITPOSTALCO"]', this.element), window.Form_Entix);

			this.Controls.LED_ENTIX___ENTITPOBOX___ = new QTextControl($('[data-identifier="LED_ENTIX___ENTITPOBOX___"]', this.element), window.Form_Entix);

			this.Controls.IFF_ENTIX___PSEUDNOVOGR04 = new QGenericControl($('[data-identifier="IFF_ENTIX___PSEUDNOVOGR04"]', this.element), window.Form_Entix);

			this.Controls.IFF_ENTIX___FACI1NAME____ = new QDbeditControl($('[data-identifier="IFF_ENTIX___FACI1NAME____"]', this.element), window.Form_Entix);

			this.Controls.IFF_ENTIX___FACI2NAME____ = new QDbeditControl($('[data-identifier="IFF_ENTIX___FACI2NAME____"]', this.element), window.Form_Entix);

			this.Controls.IFF_ENTIX___PSEUDNOVOGR06 = new QGenericControl($('[data-identifier="IFF_ENTIX___PSEUDNOVOGR06"]', this.element), window.Form_Entix);

			this.Controls.IFF_ENTIX___PSEUDFACILITE = new QTableListControl($('[data-identifier="IFF_ENTIX___PSEUDFACILITE"]', this.element), window.Form_Entix);




			this.Controls.LED_ENTIX___ENTITCODENTIT = new QHiddenControl($('[data-identifier="LED_ENTIX___ENTITCODENTIT"]', this.element), window.Form_Entix);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Entix.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Entix.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI ENTIX]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Entix.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS ENTIX]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Entix.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS ENTIX]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Entix.Data.RelationKeysSelector = {
				get faci1() { return $(':not([elem-identifier="Multiform"]) > #ValFirstfacilitie[pers-cs-area="Faci1"]', $('[data-form="Entix"]')); /* DBEdit */ },
				get faci2() { return $(':not([elem-identifier="Multiform"]) > #ValLastfacilitie[pers-cs-area="Faci2"]', $('[data-form="Entix"]')); /* DBEdit */ },
		};

		window.Form_Entix.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Entix.Data.RelationKeys = {
			get formContext() { return window.Form_Entix; },
			get faci1() { return (this.formContext.Data.RelationKeysSelector.faci1.getQControl() || {}).Value; /* DBEdit */ },
			set faci1(newValue) { (this.formContext.Data.RelationKeysSelector.faci1.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get faci2() { return (this.formContext.Data.RelationKeysSelector.faci2.getQControl() || {}).Value; /* DBEdit */ },
			set faci2(newValue) { (this.formContext.Data.RelationKeysSelector.faci2.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Entix.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>

		@* Recalculate formulas (server side) *@
		$(qForm).bind('VALFOUNDED_CHANGE VALCODENTIT_CHANGE', function(event, eTarget) {
				window.Form_Entix.RecalculateFormulas();
		});
		





	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Entix.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitEntix(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Entix.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteEntix(event) {
			event.preventDefault();
			window.Form_Entix.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS ENTIX]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Entix"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
