@model GenioMVC.ViewModels.Facty.Facty_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS FACTY]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Facty !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Facty = new QForm($('[data-form="Facty"]'), "Form_Facty");
		}
		window.Form_Facty.baseArea = "facty";
		window.Form_Facty.FormMode = '@Model.GetFormMode';
		window.Form_Facty.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Facty.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Facty")',
			GetDependants:		'@Url.Action("GetDependants", "Facty")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Facty", "Facty")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Facty", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Facty", "Facty")',
		};
		window.Form_Facty.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Facty.formSaveEdit = '@Url.Action("Facty_SaveEdit", "Facty")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Facty.formAction = 'Facty_New';
		window.Form_Facty.formRedirectURL = '@Url.Action("Facty_New_Redirect", "Facty")';
		window.Form_Facty.formSubmitURL = '@Url.Action("Facty_New", "Facty")';
		window.Form_Facty.formRedirectURL_repeatInsertion = '@Url.Action("Facty_New_Redirect", "Facty", new { repeatInsertion = true})';
		window.Form_Facty.formSubmitURL_repeatInsertion = '@Url.Action("Facty_New", "Facty", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Facty.formAction = 'Facty_Edit';
		window.Form_Facty.formRedirectURL = '@Url.Action("Facty_Edit_Redirect", "Facty")';
		window.Form_Facty.formSubmitURL = '@Url.Action("Facty_Edit", "Facty")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Facty.formAction = 'Facty_Duplicate';
		window.Form_Facty.formRedirectURL = '@Url.Action("Facty_Duplicate_Redirect", "Facty")';
		window.Form_Facty.formSubmitURL = '@Url.Action("Facty_Duplicate", "Facty")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Facty.formAction = 'Facty_Delete';
		window.Form_Facty.formRedirectURL = '@Url.Action("Facty_Delete_Redirect", "Facty")';
		window.Form_Facty.formSubmitURL = '@Url.Action("Facty_Delete", "Facty")';
	</text>
}
		window.Form_Facty.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_FACTY___FACTYTYPE____ = new QTextControl($('[data-identifier="LED_FACTY___FACTYTYPE____"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYLAYRNAME = new QTextControl($('[data-identifier="LED_FACTY___FACTYLAYRNAME"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYICONURL_ = new QTextControl($('[data-identifier="LED_FACTY___FACTYICONURL_"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYSHADOWUR = new QTextControl($('[data-identifier="LED_FACTY___FACTYSHADOWUR"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYICONANCX = new QNumericControl($('[data-identifier="LED_FACTY___FACTYICONANCX"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYICONANCY = new QNumericControl($('[data-identifier="LED_FACTY___FACTYICONANCY"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYICONHEIG = new QNumericControl($('[data-identifier="LED_FACTY___FACTYICONHEIG"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYICONWID_ = new QNumericControl($('[data-identifier="LED_FACTY___FACTYICONWID_"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYPOPUPANX = new QNumericControl($('[data-identifier="LED_FACTY___FACTYPOPUPANX"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYPOPUPANY = new QNumericControl($('[data-identifier="LED_FACTY___FACTYPOPUPANY"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYSHADOWAX = new QNumericControl($('[data-identifier="LED_FACTY___FACTYSHADOWAX"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYSHADOWAY = new QNumericControl($('[data-identifier="LED_FACTY___FACTYSHADOWAY"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYSHADOWHE = new QNumericControl($('[data-identifier="LED_FACTY___FACTYSHADOWHE"]', this.element), window.Form_Facty);

			this.Controls.LED_FACTY___FACTYSHADOWWI = new QNumericControl($('[data-identifier="LED_FACTY___FACTYSHADOWWI"]', this.element), window.Form_Facty);




			this.Controls.LED_FACTY___FACTYCODFACTY = new QHiddenControl($('[data-identifier="LED_FACTY___FACTYCODFACTY"]', this.element), window.Form_Facty);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Facty.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Facty.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI FACTY]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Facty.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS FACTY]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Facty.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS FACTY]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Facty.Data.RelationKeysSelector = {
		};

		window.Form_Facty.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Facty.Data.RelationKeys = {
			get formContext() { return window.Form_Facty; },
		};
	</text>
}

	window.Form_Facty.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Facty.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitFacty(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Facty.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteFacty(event) {
			event.preventDefault();
			window.Form_Facty.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS FACTY]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Facty"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
