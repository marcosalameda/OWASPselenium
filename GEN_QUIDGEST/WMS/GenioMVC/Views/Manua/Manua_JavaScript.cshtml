@model GenioMVC.ViewModels.Manua.Manua_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS MANUA]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Manua !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Manua = new QForm($('[data-form="Manua"]'), "Form_Manua");
		}
		window.Form_Manua.baseArea = "manua";
		window.Form_Manua.FormMode = '@Model.GetFormMode';
		window.Form_Manua.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Manua.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Manua")',
			GetDependants:		'@Url.Action("GetDependants", "Manua")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Manua", "Manua")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Manua", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Manua", "Manua")',
		};
		window.Form_Manua.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Manua.formSaveEdit = '@Url.Action("Manua_SaveEdit", "Manua")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Manua.formAction = 'Manua_New';
		window.Form_Manua.formRedirectURL = '@Url.Action("Manua_New_Redirect", "Manua")';
		window.Form_Manua.formSubmitURL = '@Url.Action("Manua_New", "Manua")';
		window.Form_Manua.formRedirectURL_repeatInsertion = '@Url.Action("Manua_New_Redirect", "Manua", new { repeatInsertion = true})';
		window.Form_Manua.formSubmitURL_repeatInsertion = '@Url.Action("Manua_New", "Manua", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Manua.formAction = 'Manua_Edit';
		window.Form_Manua.formRedirectURL = '@Url.Action("Manua_Edit_Redirect", "Manua")';
		window.Form_Manua.formSubmitURL = '@Url.Action("Manua_Edit", "Manua")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Manua.formAction = 'Manua_Duplicate';
		window.Form_Manua.formRedirectURL = '@Url.Action("Manua_Duplicate_Redirect", "Manua")';
		window.Form_Manua.formSubmitURL = '@Url.Action("Manua_Duplicate", "Manua")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Manua.formAction = 'Manua_Delete';
		window.Form_Manua.formRedirectURL = '@Url.Action("Manua_Delete_Redirect", "Manua")';
		window.Form_Manua.formSubmitURL = '@Url.Action("Manua_Delete", "Manua")';
	</text>
}
		window.Form_Manua.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_MANUA___KINDEDESIGNAT = new QDbeditControl($('[data-identifier="IFF_MANUA___KINDEDESIGNAT"]', this.element), window.Form_Manua);

			this.Controls.LED_MANUA___MANUANAME____ = new QTextControl($('[data-identifier="LED_MANUA___MANUANAME____"]', this.element), window.Form_Manua);

			this.Controls.LED_MANUA___MANUADIGDOCUM = new QTextControl($('[data-identifier="LED_MANUA___MANUADIGDOCUM"]', this.element), window.Form_Manua);

			this.Controls.LED_MANUA___MANUANOTES___ = new QGenericControl($('[data-identifier="LED_MANUA___MANUANOTES___"]', this.element), window.Form_Manua);




			this.Controls.LED_MANUA___MANUACODMANUA = new QHiddenControl($('[data-identifier="LED_MANUA___MANUACODMANUA"]', this.element), window.Form_Manua);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Manua.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Manua.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI MANUA]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Manua.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS MANUA]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Manua.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS MANUA]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Manua.Data.RelationKeysSelector = {
				get kinde() { return $(':not([elem-identifier="Multiform"]) > #ValCodkinde[pers-cs-area="Kinde"]', $('[data-form="Manua"]')); /* DBEdit */ },
		};

		window.Form_Manua.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Manua.Data.RelationKeys = {
			get formContext() { return window.Form_Manua; },
			get kinde() { return (this.formContext.Data.RelationKeysSelector.kinde.getQControl() || {}).Value; /* DBEdit */ },
			set kinde(newValue) { (this.formContext.Data.RelationKeysSelector.kinde.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Manua.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Manua.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitManua(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Manua.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteManua(event) {
			event.preventDefault();
			window.Form_Manua.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS MANUA]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Manua"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
