@model GenioMVC.ViewModels.Messa.Messa_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS MESSA]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Messa !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Messa = new QForm($('[data-form="Messa"]'), "Form_Messa");
		}
		window.Form_Messa.baseArea = "messa";
		window.Form_Messa.FormMode = '@Model.GetFormMode';
		window.Form_Messa.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Messa.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Messa")',
			GetDependants:		'@Url.Action("GetDependants", "Messa")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Messa", "Messa")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Messa", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Messa", "Messa")',
		};
		window.Form_Messa.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Messa.formSaveEdit = '@Url.Action("Messa_SaveEdit", "Messa")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Messa.formAction = 'Messa_New';
		window.Form_Messa.formRedirectURL = '@Url.Action("Messa_New_Redirect", "Messa")';
		window.Form_Messa.formSubmitURL = '@Url.Action("Messa_New", "Messa")';
		window.Form_Messa.formRedirectURL_repeatInsertion = '@Url.Action("Messa_New_Redirect", "Messa", new { repeatInsertion = true})';
		window.Form_Messa.formSubmitURL_repeatInsertion = '@Url.Action("Messa_New", "Messa", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Messa.formAction = 'Messa_Edit';
		window.Form_Messa.formRedirectURL = '@Url.Action("Messa_Edit_Redirect", "Messa")';
		window.Form_Messa.formSubmitURL = '@Url.Action("Messa_Edit", "Messa")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Messa.formAction = 'Messa_Duplicate';
		window.Form_Messa.formRedirectURL = '@Url.Action("Messa_Duplicate_Redirect", "Messa")';
		window.Form_Messa.formSubmitURL = '@Url.Action("Messa_Duplicate", "Messa")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Messa.formAction = 'Messa_Delete';
		window.Form_Messa.formRedirectURL = '@Url.Action("Messa_Delete_Redirect", "Messa")';
		window.Form_Messa.formSubmitURL = '@Url.Action("Messa_Delete", "Messa")';
	</text>
}
		window.Form_Messa.DeclareControls = function () {
			@*Form controls*@
			this.Controls.LED_MESSA___MESSAIDNOTIF_ = new QTextControl($('[data-identifier="LED_MESSA___MESSAIDNOTIF_"]', this.element), window.Form_Messa);

			this.Controls.LED_MESSA___MESSAIDMSG___ = new QTextControl($('[data-identifier="LED_MESSA___MESSAIDMSG___"]', this.element), window.Form_Messa);

			this.Controls.LED_MESSA___MESSAMAILSENT = new QCheckBoxControl($('[data-identifier="LED_MESSA___MESSAMAILSENT"]', this.element), window.Form_Messa);

			this.Controls.LED_MESSA___MESSAMAILERR_ = new QTextControl($('[data-identifier="LED_MESSA___MESSAMAILERR_"]', this.element), window.Form_Messa);

			this.Controls.IFF_MESSA___ENTITNAME____ = new QDbeditControl($('[data-identifier="IFF_MESSA___ENTITNAME____"]', this.element), window.Form_Messa);

			this.Controls.IFF_MESSA___PERSONAME____ = new QDbeditControl($('[data-identifier="IFF_MESSA___PERSONAME____"]', this.element), window.Form_Messa);

			this.Controls.LED_MESSA___MESSADOCUM_NR = new QNumericControl($('[data-identifier="LED_MESSA___MESSADOCUM_NR"]', this.element), window.Form_Messa);

			this.Controls.LED_MESSA___MESSADESIGNAT = new QTextControl($('[data-identifier="LED_MESSA___MESSADESIGNAT"]', this.element), window.Form_Messa);

			this.Controls.LED_MESSA___MESSAEMAIL___ = new QTextControl($('[data-identifier="LED_MESSA___MESSAEMAIL___"]', this.element), window.Form_Messa);

			this.Controls.LED_MESSA___MESSAMESSAGE_ = new QGenericControl($('[data-identifier="LED_MESSA___MESSAMESSAGE_"]', this.element), window.Form_Messa);

			this.Controls.LED_MESSA___MESSACREATOPE = new QTextControl($('[data-identifier="LED_MESSA___MESSACREATOPE"]', this.element), window.Form_Messa);

			this.Controls.LED_MESSA___MESSACREATDAT = new QDateControl($('[data-identifier="LED_MESSA___MESSACREATDAT"]', this.element), window.Form_Messa);




			this.Controls.LED_MESSA___MESSACODMESSA = new QHiddenControl($('[data-identifier="LED_MESSA___MESSACODMESSA"]', this.element), window.Form_Messa);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Messa.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Messa.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI MESSA]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Messa.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS MESSA]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Messa.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS MESSA]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Messa.Data.RelationKeysSelector = {
				get entit() { return $(':not([elem-identifier="Multiform"]) > #ValCodentit[pers-cs-area="Entit"]', $('[data-form="Messa"]')); /* DBEdit */ },
				get perso() { return $(':not([elem-identifier="Multiform"]) > #ValCodperso[pers-cs-area="Perso"]', $('[data-form="Messa"]')); /* DBEdit */ },
		};

		window.Form_Messa.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Messa.Data.RelationKeys = {
			get formContext() { return window.Form_Messa; },
			get entit() { return (this.formContext.Data.RelationKeysSelector.entit.getQControl() || {}).Value; /* DBEdit */ },
			set entit(newValue) { (this.formContext.Data.RelationKeysSelector.entit.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get perso() { return (this.formContext.Data.RelationKeysSelector.perso.getQControl() || {}).Value; /* DBEdit */ },
			set perso(newValue) { (this.formContext.Data.RelationKeysSelector.perso.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Messa.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Messa.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitMessa(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Messa.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteMessa(event) {
			event.preventDefault();
			window.Form_Messa.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS MESSA]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Messa"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
