@model GenioMVC.ViewModels.Produ.Produ_ValOutputsd_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
<div class="row">
	<div class="col">
		<div elem-identifier="List"  help-id="IFF_PRODU___PSEUDOUTPUTSD" style="padding-top: 0.5rem;"><label class = "i-text__label table-title c-table__title" > <h2 list-header>@Resources.OUTPUTS47833</h2 list-header> </label></div>
	</div>
</div>

@* USE /[MANUAL GQT TABLE_FOR PRODU_OUTPUTSD]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Dilin;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Dilin>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCoddilin, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN PRODU_OUTPUTSD_DILIN_INSTANT]/
		column.Expression(p => p.ValInstant).Title(@Resources.INSTANT35907).Field("ValInstant").Size(16);
// USE /[MANUAL GQT CUSTOM_COLUMN PRODU_OUTPUTSD_DISPA_DISPANR]/
		column.Expression(p => p.Dispa.ValDispanr).Title(@Resources.DISPATCH_NUMBER23616).Field("Dispa.ValDispanr").Size(10);
// USE /[MANUAL GQT CUSTOM_COLUMN PRODU_OUTPUTSD_ENTIT_NAME]/
		column.Expression(p => p.Dispa.Entit.ValName).Title(@Resources.ENTITY62049).Field("Dispa.Entit.ValName").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN PRODU_OUTPUTSD_DILIN_LINENUMB]/
		column.Expression(p => p.ValLinenumb).Title(@Resources.LINE27983).Field("ValLinenumb").Size(6);
// USE /[MANUAL GQT CUSTOM_COLUMN PRODU_OUTPUTSD_DILIN_ORDERED]/
		column.Expression(p => p.ValOrdered).Title(@Resources.ORDERED04034).Field("ValOrdered").Size(10);
// USE /[MANUAL GQT CUSTOM_COLUMN PRODU_OUTPUTSD_DILIN_DELIVERE]/
		column.Expression(p => p.ValDelivere).Title(@Resources.DELIVERED26597).Field("ValDelivere").Size(10);
// USE /[MANUAL GQT CUSTOM_COLUMN PRODU_OUTPUTSD_DILIN_OUTSTAND]/
		column.Expression(p => p.ValOutstand).Title(@Resources.OUTSTANDING36400).Field("ValOutstand").Size(10);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Dilin")
        .Id("ValOutputsd")
        .AjaxRequest("Produ_ValOutputsd")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: false)
        .RequestLink(@Url.Action("Produ_ValOutputsd", new { id = Model.ValCodprodu, partialView = "Produ_ValOutputsd"}))
        .Form("Dilin", false, false)
        .SetFollowUp("Dilin_Show", "Dilin" , (Func<GenioMVC.Models.Dilin, object>)(item => new { id = item.ValCoddilin }), isRoutine: false, isSpecificPaths: false, isAjaxAction: false, htmlAttributes: new { }, openInPopup: false)
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@tbl.ToHtml()

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qValOutputsd, #ValOutputsd_complex_filter th input, #ValOutputsd_filters input").change(function() {
                    $("#pValOutputsd").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#ValOutputsd_tableFilters').val(false);")
                    @Html.Raw("$('#ValOutputsd_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#ValOutputsd_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $('#ValOutputsd_complex_filter button#applyComplexFilter').click();
                    @:      event.preventDefault();
                    @:  }
                    @:});
                }
                @Html.Raw("$('#ValOutputsd_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_ValOutputsd').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });


                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValOutputsd']"));
                    InitSpecialControls($('#ValOutputsd'));
                    $('#ValOutputsd').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS PRODU_VALOUTPUTSD]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>