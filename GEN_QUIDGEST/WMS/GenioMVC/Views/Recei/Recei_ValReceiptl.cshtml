@model GenioMVC.ViewModels.Recei.Recei_ValReceiptl_ViewModel
@using GenioMVC.Models.Navigation;@* MH - Allows use the UserContext. *@
@using Newtonsoft.Json;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@

@*List properties DBEDIT BUTTONS*@
<div class="row">
	<div class="col">
		<div elem-identifier="List"  help-id="IFF_RECEI___PSEUDRECEIPTL" style="padding-top: 0.5rem;"><label class = "i-text__label table-title c-table__title" > <h2 list-header>@Resources.RECEIPT_LINES14292</h2 list-header> </label></div>
	</div>
</div>

@* USE /[MANUAL GQT TABLE_FOR RECEI_RECEIPTL]/ *@
@{ Dictionary<int, MvcHtmlString> renderings = new Dictionary<int, MvcHtmlString>(); }
@using GenioMVC.ViewModels.Relin;    
@{
    var data = Html.newTableFor<GenioMVC.Models.Relin>(edit: Model.IsEditableMode, canPage: true, canSort: true, countRec: false)
        .Columns(column =>
        {
            column.Expression(p => p.ValCoddilin, true).Hidden();

// USE /[MANUAL GQT CUSTOM_COLUMN RECEI_RECEIPTL_RELIN_LINENUMB]/
		column.Expression(p => p.ValLinenumb).Title(@Resources.LINE27983).Field("ValLinenumb").Size(6);
// USE /[MANUAL GQT CUSTOM_COLUMN RECEI_RECEIPTL_PRODU_SKU]/
		column.Expression(p => p.Produ.ValSku).Title(@Resources.SKU42303).Field("Produ.ValSku").Size(20);
// USE /[MANUAL GQT CUSTOM_COLUMN RECEI_RECEIPTL_PRODU_GTIN]/
		column.Expression(p => p.Produ.ValGtin).Title(@Resources.GTIN45487).Hidden().Field("Produ.ValGtin").Size(14);
// USE /[MANUAL GQT CUSTOM_COLUMN RECEI_RECEIPTL_PRODU_PRODUCT]/
		column.Expression(p => p.Produ.ValProduct).Title(@Resources.PRODUCT12880).Field("Produ.ValProduct").Size(30);
// USE /[MANUAL GQT CUSTOM_COLUMN RECEI_RECEIPTL_RELIN_ORDERED]/
		column.Expression(p => p.ValOrdered).Title(@Resources.ORDERED04034).Field("ValOrdered").Size(10);
// USE /[MANUAL GQT CUSTOM_COLUMN RECEI_RECEIPTL_RELIN_RECEIVED]/
		column.Expression(p => p.ValReceived).Title(@Resources.RECEIVED19242).Field("ValReceived").Size(10);
// USE /[MANUAL GQT CUSTOM_COLUMN RECEI_RECEIPTL_RELIN_OUTSTAND]/
		column.Expression(p => p.ValOutstand).Title(@Resources.OUTSTANDING36400).Field("ValOutstand").Size(10);
            column.Expression(p => p.ValZzstate).Field("ValZzstate").Hidden();
        })
        .SetLimits(this.Model.tableLimits)
        .SetUserColumns(this.Model.UserColumns, "Relin")
        .Id("ValReceiptl")
        .AjaxRequest("Recei_ValReceiptl")
        .DataSource(this.Model.Menu.Elements)
        .Sort(this.Model.Menu.Sort)
        .Pager(this.Model.Menu.Pagination)
        .Focus(this.Model.Menu.FocusOnRecord);

    var tbl = data
        .TableList(hasFilters: false)
        .RequestLink(@Url.Action("Recei_ValReceiptl", new { id = Model.ValCodrecei, partialView = "Recei_ValReceiptl"}))
        .Form("Relin", false, true)
        .SetFollowUp("Relin_Show", "Relin" , (Func<GenioMVC.Models.Relin, object>)(item => new { id = item.ValCoddilin }), isRoutine: false, isSpecificPaths: false, isAjaxAction: false, htmlAttributes: new { }, openInPopup: false)
        .Permissions(canView: true, canInsert: true, canEdit: true, canDelete: true, canDuplicate: true)
		;
        
}

@tbl.ToHtml()

<script>


$(document).ready(function() {

    try {
        if (jQuery) {
            try {
                $("#qValReceiptl, #ValReceiptl_complex_filter th input, #ValReceiptl_filters input").change(function() {
                    $("#pValReceiptl").val("1");
                });
                @{ var disable = (Model.IsEditableMode && Model.Menu.Elements.Count() == 0 && Model.Menu.Filters.FiltersValues.Count == 0); }


                @if(@disable && Model.Menu.Filters.ShowTableFilters) {
                    @Html.Raw("$('#ValReceiptl_tableFilters').val(false);")
                    @Html.Raw("$('#ValReceiptl_simple_filter').show();")
                }
                @if (!disable) {
                    @:$('#ValReceiptl_complex_filter input').keypress(function (event) {
                    @:  var keycode = (event.keyCode ? event.keyCode : event.which);
                    @:  if(keycode == '13' || keycode == '10'){
                    @:      $('#ValReceiptl_complex_filter button#applyComplexFilter').click();
                    @:      event.preventDefault();
                    @:  }
                    @:});
                }
                @Html.Raw("$('#ValReceiptl_extra').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                @Html.Raw("$('#exportBtn_ValReceiptl').prop(\"disabled\", " + @disable.ToString().ToLower() + ");")
                //Hide 2nd date fields
                $("[hiddable]").hide();
                $(".filter-date-cell *").focus(function () {
                    $("[hiddable]").show();
                });


                // TODO: FIX THIS!!!
                @* $(document).ready(function () { loaded(); });*@
                $(document).ready(function () {
                    loaded($("[id='ValReceiptl']"));
                    InitSpecialControls($('#ValReceiptl'));
                    $('#ValReceiptl').trigger('SET_QCONTROL_LOADED', [true]);
                    HideTableEmptyFooter();
                    StickAdvancedSearch();

                });





// USE /[MANUAL GQT LISTING_JS RECEI_VALRECEIPTL]/
 
            } catch (err) {
                //function still not defined, proceed
            }
        }
    } catch (err) {
        //ajax request loaded from cache, so jquery is not loaded yet
        window.location.pathname = location.pathname;
    }
})
</script>