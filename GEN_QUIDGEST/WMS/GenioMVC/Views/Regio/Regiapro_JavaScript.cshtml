@model GenioMVC.ViewModels.Regio.Regiapro_ViewModel
@using GenioMVC.Models.Navigation;
@using JavaScriptConversion = GenioMVC.ViewModels.JavaScriptConversion;
@{ var Navigation = Model.Navigation; } @* MH - Allows unificate the generated code. And re-use some methods of the GenioAPI. *@
@Scripts.Render("~/Scripts/validateJs")
 
@* USE /[MANUAL GQT FORM_INCLUDEJS REGIAPRO]/ *@
<script  type="text/javascript">
	@formDataScript()
	$(document).ready(function () {
		var alreadyInitializedForm = false;
		if (window.Form_Regiapro !== undefined) {
			alreadyInitializedForm = true;
		}
		else {
			window.Form_Regiapro = new QForm($('[data-form="Regiapro"]'), "Form_Regiapro");
		}
		window.Form_Regiapro.baseArea = "regio";
		window.Form_Regiapro.FormMode = '@Model.GetFormMode';
		window.Form_Regiapro.HelpStyle = '@GenioMVC.LayoutConfig.config.helpStyle';
		window.Form_Regiapro.UrlAction = {
			ReloadDBEdit:		'@Url.Action("ReloadDBEdit", "Regio")',
			GetDependants:		'@Url.Action("GetDependants", "Regio")',
			RecalculateFormulas:'@Url.Action("RecalculateFormulas_Regiapro", "Regio")',
			ChangeFormMode:		'@Url.Action("ChangeFormMode_Regiapro", new { m = Model.GetQSModes() })',
			ReloadControl:		'@Url.Action("Regio", "Regio")',
		};
		window.Form_Regiapro.FormButtons = {
			Modes: [
@if(Model.CanView)
{
	<text>
				QFormMode.show,
	</text>
}
@if(Model.CanEdit)
{
	<text>
				QFormMode.edit,
	</text>
}
@if(Model.CanDelete)
{
	<text>
				QFormMode.delete,
	</text>
}
@if(Model.CanDuplicate)
{
	<text>
				QFormMode.duplicate,
	</text>
}
@if(Model.CanInsert)
{
	<text>
				QFormMode.new
	</text>
}
			],
		};
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Regiapro.formSaveEdit = '@Url.Action("Regiapro_SaveEdit", "Regio")';
	</text>
}
@if(Model.FormMode == FormMode.New)
{
	<text>
		window.Form_Regiapro.formAction = 'Regiapro_New';
		window.Form_Regiapro.formRedirectURL = '@Url.Action("Regiapro_New_Redirect", "Regio")';
		window.Form_Regiapro.formSubmitURL = '@Url.Action("Regiapro_New", "Regio")';
		window.Form_Regiapro.formRedirectURL_repeatInsertion = '@Url.Action("Regiapro_New_Redirect", "Regio", new { repeatInsertion = true})';
		window.Form_Regiapro.formSubmitURL_repeatInsertion = '@Url.Action("Regiapro_New", "Regio", new { repeatInsertion = true })';
	</text>
} else if(Model.FormMode == FormMode.Edit)
{
	<text>
		window.Form_Regiapro.formAction = 'Regiapro_Edit';
		window.Form_Regiapro.formRedirectURL = '@Url.Action("Regiapro_Edit_Redirect", "Regio")';
		window.Form_Regiapro.formSubmitURL = '@Url.Action("Regiapro_Edit", "Regio")';
	</text>
} else if(Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Regiapro.formAction = 'Regiapro_Duplicate';
		window.Form_Regiapro.formRedirectURL = '@Url.Action("Regiapro_Duplicate_Redirect", "Regio")';
		window.Form_Regiapro.formSubmitURL = '@Url.Action("Regiapro_Duplicate", "Regio")';
	</text>
} else if(Model.FormMode == FormMode.Delete)
{
	<text>
		window.Form_Regiapro.formAction = 'Regiapro_Delete';
		window.Form_Regiapro.formRedirectURL = '@Url.Action("Regiapro_Delete_Redirect", "Regio")';
		window.Form_Regiapro.formSubmitURL = '@Url.Action("Regiapro_Delete", "Regio")';
	</text>
}
		window.Form_Regiapro.DeclareControls = function () {
			@*Form controls*@
			this.Controls.IFF_REGIAPROCNTRYCOUNTRY_ = new QDbeditControl($('[data-identifier="IFF_REGIAPROCNTRYCOUNTRY_"]', this.element), window.Form_Regiapro);

			this.Controls.LED_REGIAPROREGIOREGIAO__ = new QTextControl($('[data-identifier="LED_REGIAPROREGIOREGIAO__"]', this.element), window.Form_Regiapro);

			this.Controls.IFF_REGIAPROPAIS1COUNTRY_ = new QDbeditControl($('[data-identifier="IFF_REGIAPROPAIS1COUNTRY_"]', this.element), window.Form_Regiapro);

			this.Controls.IFF_REGIAPROPSEUDIMOVEISS = new QTableListControl($('[data-identifier="IFF_REGIAPROPSEUDIMOVEISS"]', this.element), window.Form_Regiapro);

			this.Controls.IFF_REGIAPROPSEUDIMOVEISL = new QTableListControl($('[data-identifier="IFF_REGIAPROPSEUDIMOVEISL"]', this.element), window.Form_Regiapro);

			this.Controls.IFF_REGIAPROPSEUDIMOVEISG = new QTableListControl($('[data-identifier="IFF_REGIAPROPSEUDIMOVEISG"]', this.element), window.Form_Regiapro);




			this.Controls.LED_REGIAPROREGIOCODREGIA = new QHiddenControl($('[data-identifier="LED_REGIAPROREGIOCODREGIA"]', this.element), window.Form_Regiapro);

	@* Hidden Required Form Fields Controls for Formulas *@

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
	</text>
}

		window.Form_Regiapro.Glob = {};


		};
		@* In case the form is already initialized, we only need to reinitialize the controls. (Just because of the "InitData" method) *@
		if (alreadyInitializedForm) {
			return true;
		}

@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		window.Form_Regiapro.OnPreValida = function (mode, target) {
			var deferred = $.Deferred();
			var onPreValidaCode = function () {
				var deferred = $.Deferred();
				var stopReturn = false;

// USE /[MANUAL GQT PREVALI REGIAPRO]/

				@*Return é obrigatorio to execução sincrona do código*@
				if (!stopReturn) deferred.resolve(true);
				return deferred.promise();
			};

			$.when(onPreValidaCode(), { deferred: deferred, mode: mode }).done(function (resManual, rData) {
				if (resManual) {
						rData.deferred.resolve(true);
				}
				else rData.deferred.resolve(false);
			});

			return deferred.promise();
		};

		window.Form_Regiapro.OnBeforeSave = function () {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT BEFORE_SAVE_JS REGIAPRO]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};

		window.Form_Regiapro.OnAfterSave = function (data) {
			var stopReturn = false;
			var deferred = $.Deferred();
// USE /[MANUAL GQT AFTER_SAVE_JS REGIAPRO]/
			@*Return é obrigatorio to execução sincrona do código*@
			if(!stopReturn) deferred.resolve(true);
			return deferred.promise();
		};
	</text>
}
@{
	<text>
		window.Form_Regiapro.Data.RelationKeysSelector = {
				get cntry() { return $(':not([elem-identifier="Multiform"]) > #ValCodcntry[pers-cs-area="Cntry"]', $('[data-form="Regiapro"]')); /* DBEdit */ },
				get pais1() { return $(':not([elem-identifier="Multiform"]) > #ValCodpais1[pers-cs-area="Pais1"]', $('[data-form="Regiapro"]')); /* DBEdit */ },
		};

		window.Form_Regiapro.getAllForeignKeySelectors = function () {
			@* Get all selectors of foreign keys present on the form *@
			return this.Data.RelationKeysSelector;
		};

		window.Form_Regiapro.Data.RelationKeys = {
			get formContext() { return window.Form_Regiapro; },
			get cntry() { return (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value; /* DBEdit */ },
			set cntry(newValue) { (this.formContext.Data.RelationKeysSelector.cntry.getQControl() || {}).Value = newValue; /* DBEdit */ },
			get pais1() { return (this.formContext.Data.RelationKeysSelector.pais1.getQControl() || {}).Value; /* DBEdit */ },
			set pais1(newValue) { (this.formContext.Data.RelationKeysSelector.pais1.getQControl() || {}).Value = newValue; /* DBEdit */ },
		};
	</text>
}

	window.Form_Regiapro.QFormulas = function () {
		var qForm = $(this.element);
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
		@*Edit mode Fields Formulas*@
	<text>






	</text>
}

@if(!Model.IsEditableMode)
{
	@*Generate blockWhen on Show mode only for buttons*@
	<text>


	</text>
}





	};@* End of the "QFormulas" *@


		window.Form_Regiapro.Init();


		// Element we want to become "sticky"
		var header = $('#c-stickyHeader');
		var threshold_el = $('.c-header').length ? $('.c-header') : $('nav').first();
		var breadcrumbs = $('#breadcrumbs').length ? $('#breadcrumbs') : null;

		window.onscroll = function() {
			ToggleStickyHeader(header, threshold_el, breadcrumbs);
		};
	});


@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
	<text>
		function submitRegiapro(event, repeatInsertion) {
			event.preventDefault();
			window.Form_Regiapro.Submit(repeatInsertion);
		};
	</text>
}

@if(Model.FormMode == FormMode.Delete)
{
	<text>
		function submitDeleteRegiapro(event) {
			event.preventDefault();
			window.Form_Regiapro.DeleteSubmit();
		};
	</text>
}

@helper formDataScript()
{
	<text>
		window.QData = window.QData || { Arrays: { GetArray: function (arrayName) { return this[arrayName] || []; } }, GetArray: function(arrayName) { return this.Arrays.GetArray(arrayName); } };
		@* Declare form Arrays *@
		@* --- --- --- --- --- *@
	</text>
}
@if(Model.FormMode == FormMode.New || Model.FormMode == FormMode.Edit || Model.FormMode == FormMode.Duplicate)
{
    <text>
    var ajaxCallsRemaining = $(".flash object embed").length;
    $('form [elem-identifier="ButtonPrimary"]').click(function() {
        var flashObjects = $(".flash object embed");
        ajaxCallsRemaining = flashObjects.length;
        if(flashObjects.length == 0)
            return true;
        else {
            flashObjects.each(function() {
               var flashObj = GetFlash($(this).attr('id'));
               flashObj.UsedFlash.TriggerSave(flashObj);
            });
            return false;
        }
    });
    </text>
}
// USE /[MANUAL GQT FORM_CODEJS REGIAPRO]/



 

    $($('[elem-identifier="FormActions"]'), $('[data-form="Regiapro"]')).click(function(event){
        var form = $(this).closest('[data-form]').data('form');
        var curLocalStorage = QLocalStorage.getLocalStorage('lastActiveElement');
        delete curLocalStorage[form];
        QLocalStorage.setLocalStorage('lastActiveElement', curLocalStorage);
    });
</script>
