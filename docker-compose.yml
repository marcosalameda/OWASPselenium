services:
  zap:
    image: zaproxy/zap-stable
    user: root
    command: >
      zap.sh -daemon
      -host 0.0.0.0
      -port 8080
      -config api.disablekey=true
      -config api.addrs.addr.name=.*
      -config api.addrs.addr.regex=true
      -config network.connection.maxResponseSize=104857600
    ports:
      - "8080:8080"
    volumes:
      - ./zap-reports:/zap/wrk
    networks:
      - zap-net
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/JSON/core/view/version/"]
      interval: 5s
      timeout: 3s
      retries: 20

  selenium-hub:
    image: selenium/standalone-chrome:latest
    shm_size: 2gb
    networks:
      - zap-net
    environment:
      # BLINDAJE CRÍTICO: Desactivamos la telemetría de Google que pesa 33MB 
      # y bloquea la base de datos de ZAP (límite 16MB).
      - SE_NODE_CHROME_ARGS=--ignore-certificate-errors --disable-features=OptimizationGuideModelDownloading,OptimizationHints --disable-component-update --disable-background-networking --disable-sync --disable-default-apps --no-first-run
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:4444/status"]
      interval: 5s
      timeout: 3s
      retries: 20

  selenium:
    build:
      context: .
      dockerfile: Dockerfile.selenium
    working_dir: /tests
    volumes:
      - ./GEN_QUIDVUE/HORIZONTAL_VUE/CsUITest:/tests
      - ./zap-reports:/reports
    depends_on:
      zap:
        condition: service_healthy
      selenium-hub:
        condition: service_healthy
    networks:
      - zap-net
    environment:
      - SELENIUM_REMOTE_URL=http://selenium-hub:4444/wd/hub
      - ZAP_PROXY=http://zap:8080
      - selenium_baseurl=https://jenkinsvm.quidgest.pt/gqt_horizontal_vue/
      - selenium_explicitwait=60000
    command:
      - bash
      - -c
      - |
          echo "=== 0. LIMPIEZA Y RESTORE INTEGRAL ==="
          # Limpiamos binarios para evitar conflictos de versiones.
          dotnet clean || true
          rm -rf bin/ obj/
          dotnet restore --force
          dotnet build --no-restore

          echo "=== 1. WARM UP (Sugerencia Administrador SVN) ==="
          # Despertamos la web antes de los tests para evitar Timeouts iniciales.
          curl -sf https://jenkinsvm.quidgest.pt/GQT_Horizontal_Vue/ || true

          echo "=== 2. CONFIGURACIÓN DE CONTEXTO ZAP ==="
          until curl -sf http://zap:8080/JSON/core/view/version/ >/dev/null; do sleep 2; done
          curl -sf "http://zap:8080/JSON/context/action/newContext/?contextName=mi-app" || true
          curl -sf "http://zap:8080/JSON/context/action/includeInContext/?contextName=mi-app&regex=https://jenkinsvm.quidgest.pt/gqt_horizontal_vue.*" || true

          echo "=== 3. EJECUCIÓN DE LOS 65 TESTS ==="
          # Usamos --nologo como se indica en los archivos del SVN.
          dotnet test --no-build --nologo --logger:"console;verbosity=normal" || true

          echo "=== 4. GENERACIÓN DE REPORTE OWASP ZAP ==="
          curl -sf "http://zap:8080/JSON/reports/action/generate/?title=OWASP+ZAP+Report&template=traditional-html&reportFileName=zap-report&reportDir=/zap/wrk" || true
          echo "✅ Proceso completado. Revisa el informe en la carpeta zap-reports."

networks:
  zap-net:
    driver: bridge
